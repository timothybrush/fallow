---
paths:
  - "crates/core/**"
---

# Core constraints

- Read `crates/core/AGENTS.md`, `docs/development/repo-map.md`, and the
  relevant implementation reference.
- Keep detection conservative and deterministic.
- Keep project discovery and analysis free of user-project code execution.
- Preserve workspace, suppression, baseline, cache, and output parity.
- Add focused fixtures and real-project evidence for behavior changes.
