---
paths:
  - "crates/core/src/analyze/**"
  - "crates/extract/src/visitor/**"
  - "crates/graph/src/graph/**"
  - "crates/graph/src/resolve/**"
---

# Detection capabilities

Non-obvious implementation details for each detection feature. These are NOT discoverable from reading the code alone.

## Parser-level
- **HTML entry files**: `<script src="...">` (module and classic) and `<link rel="stylesheet" href="...">` / `<link rel="modulepreload" href="...">` create graph edges from HTML to referenced assets. Remote URLs skipped. HTML comments stripped before matching. HTML files exempt from unused-file detection.
- **JSX resource attributes are ignored**: generic lowercase intrinsic `<script src="...">` and `<link rel="stylesheet|modulepreload" href="...">` inside JSX/TSX files do NOT create `SideEffect` imports by default. These literals are often runtime HTML metadata or serializer test values, not bundler-resolved module specifiers, and treating them as imports caused false `unresolved-imports` clusters in Hono's JSX tests. Framework-specific JSX asset semantics should be added as constrained opt-in logic rather than broad JSX attribute extraction. HTML files and bare `html` tagged templates still have dedicated asset extraction. See issues #105 and #640.
- **Pino transport target dependencies**: JS/TS extraction credits static Pino transport target packages when the call is made through a proven `pino` import or `const pino = require("pino")` binding. Supported shapes include `pino({ transport: { target: "pino-pretty" } })`, simple conditional branches around the transport object, and `transport.targets` arrays of static objects. The extractor records those strings as side-effect dynamic imports so existing resolver and package-usage accounting handles package names and subpaths. Local functions named `pino`, shadowed bindings, dynamic target expressions, and arbitrary object literals are ignored to avoid broad string-based dependency credit. See issue #954.
- **Package path resolution dependency credit**: JS/TS extraction credits packages used through high-confidence package path resolution patterns. Supported shapes include direct package roots such as `resolveModuleDir("ffmpeg-static")`, direct package manifests such as `require.resolve("pkg/package.json")`, and a single static value flowing into `` require.resolve(`${packageName}/package.json`) ``. Static values may come from const strings, const string arrays, or object-property tables iterated with `Object.values(...)` / `Object.entries(...)`. Arbitrary dynamic strings and unrelated subpaths such as `pkg/asset.css` are ignored to avoid broad string-based dependency credit. See issue #952.
- **napi-rs optional prebuild dependency credit**: packages with `package.json#napi.targets` credit matching platform packages listed in the same manifest's `optionalDependencies`. Package names are derived from `napi.packageName` first, then package.json `name`; `binaryName` is not used as the npm package base because it names the native binary file. Unknown targets and absent optional dependency entries are ignored, so unrelated optional dependencies still report. Covers root packages and workspace packages through the plugin package.json metadata hook. See issue #1164.
- **`html` tagged template asset references**: `` html`<script src="..."></script>` `` and `` html`<link rel="stylesheet|modulepreload" href="..."/>` `` inside any JS/TS file create `SideEffect` imports via the regex-based HTML asset scanner (`crate::html::collect_asset_refs`). Only the bare identifier tag `html` is matched — `css`, `sql`, `gql`, `styled.div`, and member/call expressions are deliberately ignored to avoid misreading unrelated tagged templates as HTML. Each template quasi is scanned independently, so an asset reference split across an interpolation boundary (`` html`<script src="${base}/app.js">` ``) is skipped rather than producing a garbled unresolvable specifier. Needed for SSR helpers like `hono/html`, `lit-html`, and `htm` where layout components emit HTML via tagged templates instead of JSX. See issue #105 (till's follow-up comment).
- **GraphQL document imports**: `.graphql` and `.gql` files are discovered as source files. Lines matching `#import "./fragment.graphql"` or `# import '../shared/fragment.gql'` create `SideEffect` imports so fragment/schema documents reachable only through GraphQL import comments stay connected in the module graph. Only relative string specifiers (`./`, `../`) are extracted; package-style imports are ignored. Extensionless relative imports probe `.graphql` and `.gql` through the resolver extension list. See issue #250.
- **MDX import/export extraction**: `.mdx` files extract only top-level JavaScript/TypeScript `import` and `export` statements, including multi-line brace imports. Fenced Markdown code blocks using backtick or tilde fences are skipped entirely, so documentation examples with virtual `// file:` boundaries do not create graph edges or `unresolved-import` findings. Extracted statement bytes carry a source map back to the original MDX file, so diagnostics on imports after prose report the original line and column rather than the compacted synthetic buffer line. Real top-level MDX imports remain analyzed. See issues #639 and #445.
- **Fumadocs MDX content roots**: the Fumadocs plugin activates on `fumadocs-mdx`, `fumadocs-core`, `fumadocs-ui`, or `source.config.*`. It treats `source.config.*` and generated `.source/**/*.{ts,tsx,js,jsx,mts,mjs,cts,cjs}` files as framework-used, traverses `.source` despite the hidden-directory default skip, suppresses generated `fumadocs-mdx:*` virtual imports, credits packages imported by `source.config.*`, and extracts literal `dir` values from `defineCollections`, `defineDocs`, and direct `defineConfig({ collections })` object entries as Markdown/MDX/JSON/YAML content roots. This prevents docs content under configured roots from surfacing as unused files while avoiding a broad `content/**` fallback. See issue #633.
- **Contentlayer content roots**: the Contentlayer plugin activates on `contentlayer`, `contentlayer2`, `next-contentlayer`, `next-contentlayer2`, or `contentlayer.config.{ts,js,mts,mjs}`. It treats the config and generated `.contentlayer/**/*.{ts,tsx,js,jsx,mts,mjs,cts,cjs}` files as framework-used, traverses `.contentlayer` only for active packages, credits packages imported by the config, and extracts static `contentDirPath` plus same-file `defineDocumentType` `filePathPattern` literals as Markdown/MDX/JSON/YAML content roots. Dynamic config and cross-file document type evaluation are intentionally skipped to avoid over-crediting. See issue #610.
- **Firebase Messaging service worker**: the Firebase plugin activates on the exact `firebase` dependency and marks `public/firebase-messaging-sw.js` plus nested `**/public/firebase-messaging-sw.js` files as always used. It does not credit the `firebase` package by itself, so unused dependency reporting still depends on source imports or other package usage. See issue #873.
- **Velite content roots**: the Velite plugin activates on the `velite` dependency or a root/workspace `velite.config.{ts,mts,cts,js,mjs,cjs}` file. It keeps the config and generated `.velite/**` output always-used, traverses the hidden `.velite` directory for active packages so app imports of generated collections resolve, credits packages imported by the config and `velite` itself as a tooling dependency, and parses `defineConfig` / `defineCollection` to model content roots. Each collection `pattern` glob (string or array) is joined to the top-level `root` (default `content`, resolved config-relative) and emitted as a support entry point, so Velite-managed Markdown / MDX content is not reported as unused; negation globs (`!private/**`) are skipped and a config with no recoverable pattern falls back to `<root>/**/*.{md,mdx,yml,yaml,json}`. A non-default `output.data` directory is credited config-relative. Patterns are scoped to the config file's package, so a monorepo app's content does not leak project-wide. See issue #609.
- **Mintlify docs content roots**: the Mintlify plugin activates on the `mint` or `mintlify` dependency, or a root/workspace `docs.json` or `mint.json` file. It keeps `docs.json` / `mint.json` always-used, credits `mint` and `mintlify` as tooling dependencies (so the CLI does not surface as unused even when invoked only through a `mint dev` script), and marks `{md,mdx}` under the directory that holds the config file as runtime-used support entry points. The content pattern is scoped to the config file's directory (workspace runs prefix it back to the project root), so docs MDX no longer needs manual `dynamicallyLoaded` entries while non-Mintlify MDX outside the docs root stays governed by other plugins. The whole docs-root tree is credited rather than parsing the `navigation` graph, which avoids false positives across Mintlify's many navigation shapes. See issue #626.
- **RedwoodSDK worker entrypoints**: the RedwoodSDK plugin activates only from `rwsdk` and marks `src/worker.{ts,tsx,js,jsx,mts,mjs}` as runtime entrypoints for that workspace. It does not claim `vite.config.*` because the existing Vite plugin owns Vite config reachability and import-credit parsing. The worker convention is workspace-scoped, so plain Vite sibling workspaces with their own `src/worker.tsx` do not inherit it. Legacy `@redwoodjs/*` packages do not activate this plugin. See issue #632.
- **OpenNext Cloudflare config**: the OpenNext Cloudflare plugin activates from `@opennextjs/cloudflare` in package dependencies or devDependencies, or from package scripts invoking `opennextjs-cloudflare`. It keeps root and nested `open-next.config.{ts,js,mjs,cjs}` files reachable and credits packages imported from those configs, matching `opennextjs-cloudflare build` consumption without broadening plain Next.js or Wrangler projects. See issue #613.
- **React Compiler dependency credit**: `babel-plugin-react-compiler` is credited only when React Compiler is explicitly enabled by framework config. Next.js `next.config.*` credits it for `reactCompiler: true` and object-valued `reactCompiler` options. Vite `vite.config.*` and electron-vite `electron.vite.config.*` credit it when it is wired through `@vitejs/plugin-react` or the standalone `@rolldown/plugin-babel`: explicit Babel plugin strings and tuples in `react({ babel: { plugins } })` / `babel({ ... })`, and the documented `reactCompilerPreset()` preset helper inside a `presets` array (e.g. `babel({ presets: [reactCompilerPreset()] })`, the canonical `@vitejs/plugin-react` 6.x shape). The preset call is credited only when `reactCompilerPreset` is a named import from `@vitejs/plugin-react` (provenance-checked) and the call sits directly inside a `presets` / `babel.presets` array of a `react()` / `babel()` plugin call; for electron-vite the same arrays are scanned inside the `main` / `preload` / `renderer` sections (the Vite plugin never sees `electron.vite.config.*`). The detection lives in the shared `crate::plugins::react_compiler` module used by both the Vite and Electron plugins. Disabled config, missing config, local functions with matching names, the namespace-import form (`import * as vr; vr.reactCompilerPreset()`), the variable-indirection form (`const p = reactCompilerPreset()`), a local re-export barrel that does not import from `@vitejs/plugin-react`, and arbitrary nested strings do not credit the dependency. See issues #623 and #751.
- **Storybook staticDirs and manager runtime**: the Storybook plugin parses statically recoverable `staticDirs` entries from `.storybook/main.{ts,js,mjs,cjs}`. String entries mount at `/`; object entries use `from` as a config-relative source directory and normalize `to` values such as `icons`, `/icons/`, and `/`. Resolver handling is intentionally limited to `.storybook/preview-head.html` and `.storybook/preview-body.html`; normalized HTML refs such as `tokens.css` / `./tokens.css` are treated as URL-root paths, query/hash suffixes are stripped for filesystem lookup, and the longest matching mount wins before the generic root-relative URL fallback. Mapped project files become graph edges and external existing files, such as files under `node_modules`, resolve as `ExternalFile` without being synthesized back into `unlisted-dependencies` findings. `.storybook/manager.{ts,tsx,js,jsx}` also has file-scoped runtime dependency credit for Storybook manager-provided imports (`react`, `react-dom/client`, `@emotion/*`, `storybook/manager-api`, `storybook/internal/*`, `storybook/theming`, `storybook/core-events`, `@storybook/components`, `@storybook/theming`). This is matched against original import specifiers so `storybook/internal/*` does not suppress every `storybook/*` import, and the allowance does not apply to ordinary source files. See issue #546.
- **SvelteKit remote functions**: SvelteKit 2.27+ remote functions are exported from `*.remote.{ts,js}` files (`export const getPosts = query(...)`, plus `command` / `form` / `prerender`) and invoked through framework-generated client/server bindings that fallow's import graph cannot see. The SvelteKit plugin marks `src/**/*.remote.{ts,js}` as an entry pattern (so a generated-binding-only remote file is not reported as `unused-file`) and adds a `used_exports` rule mapping the same pattern to `["*"]` (so the user-named remote-function exports are credited and never report as `unused-export`, including under `--include-entry-exports` where the entry-point skip is disabled). Remote-function export names are user-defined, so a fixed convention list cannot enumerate them; `"*"` is the conservative credit, mirroring the Storybook story-file precedent. Import edges for dependencies referenced inside remote files are preserved because the file is parsed as an entry point. Scoping is by the `.remote.` double-extension only, so ordinary `src/**/*.ts` files (and a genuinely-unused non-remote module) stay governed by normal detection; `src/lib/server/**` is not excluded because SvelteKit forbids remote files there and crediting a stray one is harmless. See issue #611.
- **SvelteKit layout-reset routes**: SvelteKit's layout-reset convention appends an `@` suffix to a page or layout component to break it out of its parent layout chain (`+page@.svelte` resets to the root layout, `+page@named.svelte` resets to a named layout). The SvelteKit plugin marks `src/routes/**/+page@*.svelte` and `src/routes/**/+layout@*.svelte` as entry patterns and adds `used_exports` rules crediting their `default` export, so layout-reset routes stop reporting as `unused-file` and their component default stays credited. The `*` covers both the empty (`@.svelte`) and named (`@named.svelte`) forms; `literal_separator(true)` keeps the glob within a single path segment. Only `.svelte` component files take the suffix: SvelteKit itself throws on `@` in `.ts`/`.js` route files (`+page@.ts` is invalid), and `+error.svelte` / `+server.{ts,js}` have no `@` variant, so no module-file or error/server patterns are added. See issue #791.
- **electron-vite `rollupOptions.input` entries**: the Electron plugin activates on `electron` / `electron-builder` / `@electron-forge/cli` / `electron-vite`. When `electron.vite.config.{ts,js,mjs}` is present it now parses each of the `main` / `preload` / `renderer` sections' `build.rollupOptions.input` (string, array, and object-of-values forms) and seeds each value as an entry point, normalized relative to the config file via `normalize_config_path` (so a monorepo subpackage config maps `src/renderer/index.html` to `apps/desktop/src/renderer/index.html`). Object values commonly use path helpers (`resolve(__dirname, 'src/renderer/index.html')`, `join(...)`, `fileURLToPath(...)`, `new URL(..., import.meta.url)`); the shared `config_parser::extract_config_string_or_array` evaluates those through `expression_to_path_string` in string / array / object positions (broadened in issue #604), so the Electron plugin reuses it directly rather than carrying its own extractor. Renderer HTML entries become entry points, so the `<script src>` source trees behind multi-window renderers stop reporting as `unused-files`. The static main/preload fallback globs also widen to `{ts,tsx,js,jsx,mts,mjs}`. Crediting is scoped to declared inputs (and per-section function-wrapped configs such as `renderer: defineConfig({...})` are out of scope, matching the Vite plugin's plain-object nesting requirement), so a renderer source file linked from no declared entry is still reported. See issue #600.
- **Bun `bunfig.toml` preload entries and test discovery**: the `bun` plugin now activates on `@types/bun` in addition to `bun-types` (activation reads declared `package.json` deps, and `@types/bun` is the canonical Bun types package even though it transitively depends on `bun-types`). When active, it marks Bun's documented default `bun test` file patterns (`*.test`, `*_test`, `*.spec`, and `*_spec` across js/jsx/ts/tsx) as test entry points. When `bunfig.toml` is present the plugin parses both the top-level `preload = [...]` array and the `[test] preload = [...]` array and seeds each referenced file as an entry pattern, normalized relative to the config file via `config_parser::normalize_config_path` (so a `./src/test-preload.ts` reference maps to `src/test-preload.ts`). It also parses `[test].root`; when set, the Bun default test patterns are scoped under that root instead of matching the whole project. Bun loads these files before every run / test without an inbound import edge, so without the seed they report as `unused-file`. Invalid TOML and non-array / non-string entries are skipped silently; entries are deduped. Both `bun-types` and `@types/bun` are credited as tooling dependencies. See issues #847 and #951.
- **Browser extension manifest entrypoints**: the `browser-extension` plugin activates without package dependencies by inspecting deduped `manifest.json` candidates at the project root and discovered-file ancestor directories. Activation requires numeric `manifest_version` 2 or 3 plus at least one extension runtime key (`background`, `content_scripts`, action/page/browser action, options, side panel, devtools, or `web_accessible_resources`) so ordinary PWA manifests stay inert. Config resolution seeds MV3 `background.service_worker`, MV2 `background.scripts`, `content_scripts[].js/css`, popup/options/devtools/side-panel HTML, MV2 string-array `web_accessible_resources`, and MV3 object `resources` arrays as runtime entry patterns, normalized relative to the manifest file. Remote/scheme URLs, protocol-relative URLs, empty strings, and parent-directory escapes are skipped; leading `/foo.js` is treated as extension-root-relative. HTML entries rely on the existing HTML asset extractor to keep their `<script>` and stylesheet graph reachable. See issue #616.
- **WXT entrypoints and config modules**: the `wxt` plugin activates from `wxt`, any non-empty `@wxt-dev/*` dependency, or a root `wxt.config.{ts,js,mts,mjs,cjs,cts}` file. It keeps WXT config files always used, credits their default export under `--include-entry-exports`, marks documented single-file `entrypoints/*` and directory `entrypoints/**/index.*` entrypoints (plus `src/entrypoints` variants) as runtime roots, and parses static `modules: [...]` strings from config as referenced dependencies. Package subpaths are normalized (`@wxt-dev/i18n/module` credits `@wxt-dev/i18n`); local, absolute, protocol, empty, and backslash-containing specifiers are ignored. The entry globs intentionally do not credit every descendant under an entrypoint directory, so unimported helper siblings stay reportable. See issue #612.
- **@sanity/pkg-utils build configs**: the `pkg-utils` plugin activates ONLY on an exact `@sanity/pkg-utils` dependency (not the broad `@sanity/` prefix the CMS `sanity` plugin uses, so a plain `@sanity/client` consumer does not get its build config auto-credited). `@sanity/pkg-utils` discovers `package.config.{ts,js,mts,mjs,cts,cjs}` (its build config) and the optional `package.bundle.{ts,js,mts,mjs,cts,cjs}` (Vite bundle config) on a filename convention rather than importing them from source, so without the plugin they surface as `unused-file`. The plugin marks both filenames as always-used at the project root AND nested (each pattern listed in bare and `**/`-prefixed form, because `always_used` matching uses `literal_separator(true)` with no automatic prefix; mirrors the `varlock` / `wuchale` precedent), covering monorepo `packages/<pkg>/package.config.ts` layouts, and credits `@sanity/pkg-utils` as a tooling dependency. It does not seed library source entries (`src/_exports/**`): the `exports.source` condition resolution plus the workspace-package fallback already keep them reachable, so parsing `package.bundle.ts`'s `build.lib.entry` would be redundant. The credit is scoped to the two build-config filenames, so an unreferenced ordinary source file still reports. Lets pkg-utils projects drop the manual `entry` array for these files (adopter: sanity-io/sdk).
- **Obsidian plugin entrypoints and lifecycle members**: the `obsidian` plugin activates from an `obsidian` dependency or a conservative Obsidian-shaped `manifest.json` (`id`, `name`, `version`, and `minAppVersion` as strings, with no browser-extension `manifest_version`). It marks `src/main.{ts,js}`, `main.{ts,js}`, and root-level `cdp.js` as runtime entrypoints, keeps `manifest.json` and `styles.css` reachable, and credits documented lifecycle overrides only on direct Obsidian API bases: `Plugin.onload` / `Plugin.onunload`, `Modal.onOpen` / `Modal.onClose`, and `ItemView` / `View` methods `getViewType`, `getDisplayText`, `getIcon`, `onOpen`, `onClose`, and `onPaneMenu`. Aliased imports and project-local transitive base classes are intentionally not resolved by the scoped member rule, so similarly named methods on ordinary classes still report. See issue #617.
- **Oxlint `jsPlugins` config dependencies**: the Oxlint plugin activates on `oxlint`, treats `.oxlintrc.json`, `oxlint.json`, and `oxlint.config.ts` as config files, and parses top-level `jsPlugins` entries as referenced dependencies. String entries and tuple first elements credit package specifiers via package-name extraction without ESLint shorthand expansion; alias-object entries credit the `specifier` value, not the display `name`; relative or absolute plugin paths are recorded as support setup files instead of package dependencies. Built-in `plugins` names remain ignored because they are Oxlint-native plugin selectors, not npm package references. The plugin also credits known oxlint CLI tooling packages by exact name via `tooling_dependencies`: `oxlint-tsgolint` (the type-aware companion the oxlint binary loads at runtime via `--type-aware` / `options.typeAware`, never imported and never in `jsPlugins`). This credit is honored for both prod and dev dependency categories, so an `oxlint-tsgolint` declared in prod `dependencies` (where the general `oxlint` tooling prefix, which only covers devDependencies, does not apply) is no longer reported as unused. The list is exact-name, not an `oxlint-*` prefix, so an unrelated `oxlint-`-prefixed prod dependency still reports. See issues #607 and #753.
- **ESLint meta-preset plugin crediting**: meta-presets such as `@antfu/eslint-config` are invoked as a factory (`antfu({...})`) and load their plugins dynamically or via optional `peerDependencies`, so the flat-config `plugins` object-key credit names nothing and the generic required-peer-dependency closure (`required_peer_dependency_names`, which skips optional peers) leaves the framework plugins reported as unused. When the ESLint plugin sees an import whose package name matches a preset shape (`eslint-config-*` or any `@scope/eslint-config[-*]`, detected by `is_eslint_preset`), it reads that preset's own `package.json` from node_modules and credits every entry in `dependencies` / `peerDependencies` / `optionalDependencies` that is eslint-ecosystem-shaped (`is_eslint_ecosystem_dependency`: `eslint-plugin-*`, `@scope/eslint-plugin[-*]`, `eslint-config-*`, `@scope/eslint-config`, `eslint-import-resolver-*`, `*-eslint-parser`, `@scope/parser`). General-purpose runtime deps the preset also declares (`globals`, `ansis`, `cac`) are excluded so a genuinely-unused general dep the user declared independently is not masked. Crediting is one level deep and gated to preset imports, so a plain plugin import (`eslint-plugin-import`) does not pull in its own dependency fan. See issue #754.
- **Package scaffold template assets**: `package.json#files` entries that clearly name runtime-copied template or scaffold roots are treated as support entry patterns outside framework plugin activation. Supported shapes include `template-*`, `templates`, `templates/**`, `scaffold/**`, and `scaffolds/*`, with workspace package patterns prefixed from the monorepo root. Generic publish entries such as `dist`, `index.js`, and `README.md` are ignored, and packages with negated `files` entries skip this derivation because support entry patterns do not model exclusions. See issue #635.
- **OpenCode project plugins**: the OpenCode plugin activates on `opencode.json`, `.opencode/`, or any `@opencode-ai/*` dependency. It traverses `.opencode` despite the hidden-directory default skip, treats `.opencode/plugins/**/*.{js,ts,mjs,cjs,mts,cts}` as support entry files, keeps `opencode.json` and `.opencode/package.json` reachable, and credits npm plugin packages declared in `opencode.json#plugin`. String entries and tuple first elements such as `["opencode-wakatime", { ... }]` are normalized through package-name extraction; local, absolute, protocol-like, empty, and backslash-containing specifiers are ignored for package credit. `@opencode-ai/plugin` is also treated as a tooling dependency so local plugin type imports do not surface as unused. See issue #629.
- **Varlock schema plugins**: the Varlock plugin activates on `varlock`, any `@varlock/*` dependency, a root `.env.schema`, or discovered nested `.env.schema` files. It keeps `.env.schema` files reachable, credits package-like `@plugin(...)` schema decorator arguments as referenced dependencies, strips npm version selectors before matching, and treats `varlock` plus `@varlock/vite-integration` as tooling dependencies. Local paths, URL/protocol references, malformed values, and backslash-containing specifiers are ignored for dependency credit. See issue #622.
- **k6 load-test scripts**: the k6 plugin activates from a `k6` dependency or any discovered `*.k6.{js,ts,mjs,cjs,mts,cts}` file. It marks `**/*.k6.{js,ts,mjs,cjs,mts,cts}` and `load/*.k6.{js,ts,mjs,cjs,mts,cts}` as test entry points so load-test scripts do not report as unused, and treats `k6` as a tooling dependency when package scripts invoke the CLI. Runtime imports are handled separately by the platform-builtin predicate: exact `k6` and `k6/*` are provided by the k6 runtime, not npm resolution. Similar package names such as `k6-tools`, `k6ish`, `k6x/http`, and `@types/k6` stay ordinary dependencies. See issue #625.
- **Danger dangerfiles**: the Danger plugin activates from a `danger` dependency OR from any discovered `dangerfile.{js,ts,mjs,cjs}` (root or workspace). Danger is commonly run straight from CI (`npx danger ci`, an Earthfile, a GitHub Action) without ever listing `danger` in package.json, so the dependency enabler alone left a root `dangerfile.js` reported as `unused-file`. It keeps `dangerfile.{js,ts,mjs,cjs}` (and the `**/`-prefixed workspace form) always-used and credits `danger` as a tooling dependency when declared. Activation uses an exact filename match, so `predangerfile.js`, `dangerfile.json`, and `dangerfiles.js` stay inert. The `.js` extension was always in the allowlist (globset expands the `{...}` alternation); the gap was activation, not the extension. See issue #758.
- **JSDoc `import()` type references**: JSDoc comments matching `import('./path').Member` inside `@param`, `@returns`, `@type`, `@typedef`, `@callback`, or other tag contexts are extracted as type-only imports. Supports single/double quotes, bare specifiers, parent-relative paths, union annotations with multiple `import()` expressions per comment, and nested member access (first segment wins). Side-effect-only `import('./types')` without `.Member` is recorded as `SideEffect` to keep the file reachable. Prevents false `unused-export` reports for types referenced only from JSDoc annotations in untyped JS files. Only `/** */` block comments are scanned (single-star `/* */` is not JSDoc). See issue #105 (till's comment).
- **`typeof import('./path').X` in TypeScript type positions**: `TSImportType` AST nodes are extracted as type-only `ImportInfo` entries. A flat qualifier (`typeof import('./x').foo`) credits a `Named("foo")` import; a nested qualifier (`typeof import('./x').A.B`) walks to the leftmost identifier and credits `Named("A")`; no qualifier and the indexed-access form (`typeof import('./x')['default']`) record a `SideEffect` import so the target file stays reachable. Fires inside any type position, including the bodies of `declare global { ... }` and `declare module 'pkg' { ... }` ambient declarations produced by unplugin-auto-import (`auto-imports.d.ts`) and unplugin-vue-components (`components.d.ts`). Pairs with the graph-level promotion of every `.d.ts` / `.d.mts` / `.d.cts` file to an overall reachability root (`ModuleGraph::build_with_reachability_roots` augments `entry_point_ids` with declaration files), so the new edges propagate through BFS even when the d.ts file is not in the user's `entry` config. Side effect: `is_entry_point()` returns `true` for declaration files, which auto-skips them in `unused-exports` / `unused-types` detection (preventing false positives on module-augmentation interfaces TypeScript consumes via global type resolution); pass `--include-entry-exports` to re-enable. Runtime / test reachability is unchanged because declaration files have no runtime side effects. See issues #396 and #397.
- **`new URL('./', import.meta.url)` directory specifiers skipped**: the worker / asset detector for `new URL(literal, import.meta.url)` admits relative specifiers (`./`, `../` prefixes) but rejects trailing-slash forms (`./`, `../`, `./foo/`) and marks extensionless specifiers (e.g. `./services`, `./bin`) as speculative. Trailing-slash forms were excluded in #399 because they are definitionally directory URLs. Extensionless specifiers added in #840: they lack a file extension and are commonly used to obtain a directory URL (`fileURLToPath(new URL('./services', import.meta.url))`); when the resolver cannot find a module at those paths they are silently dropped rather than reported as `unresolved-import`. File-pointing specifiers with an extension (`./worker.js`, `./assets/foo.svg`) keep `is_speculative = false` so genuinely missing files are still reported. See issues #399 and #840.
- **Vue/Svelte SFC**: handles `>` in quoted attributes like `generic="T extends Foo<Bar>"`, Vue `<script src="...">` external script support, Svelte script `src` metadata parsing, and HTML comment filtering. Script bodies are parsed as extracted fragments whose spans are remapped back to original `.vue` / `.svelte` byte offsets before module info is finalized; Vue `<script src>` and style `src` references carry the `src` value span when available. Svelte markup-level `<script src>` references do not create graph edges because Svelte treats them as runtime HTML. Template-visible import tracking: imports used only in markup (`{formatDate(x)}`, `utils.formatDate()`) are credited as used, preventing false unused-import/export reports. Vue credits only `<script setup>` bindings; Svelte excludes both `<script context="module">` (Svelte 4) and the bare `<script module>` (Svelte 5, via `SVELTE_MODULE_ATTR_RE` mirroring the `setup` attr anchor) scripts from template visibility, so a module script's imports are not wrongly credited as template-visible. Svelte 5 `$props()` destructure is harvested into `ModuleInfo.component_props` (reusing the Vue `ComponentProp` IR + abstain flags: rest element / bare-identifier / nested destructure abstain), and the `unused-component-prop` detector now processes `.svelte` files (gated on `svelte` / `@sveltejs/kit`) alongside `.vue` / `.astro`, so a Svelte prop read nowhere in its own component is flagged. Each `ComponentProp` carries both the public key (`name`) and the local destructure alias (`local`, e.g. `_stage`, equal to `name` when there is no alias); the opt-in `unusedComponentProps.ignorePattern` (issue #1648) matches its regex against `local`. Namespace member access in templates (`utils.formatDate`) tracked as member usage. Svelte 5 `$derived(new Class())` / `$derived.by(() => new Class())` bindings propagate template member access to the derived class instance, while `$effect(...)` and `$inspect(...)` are ordinary script calls whose referenced imports are credited by the JS visitor. Security scanning emits existing `dangerous-html` sink candidates for non-literal Svelte `{@html ...}` and Vue `v-html="..."` bindings, anchored to the original SFC source, while literal bindings stay quiet. See issues #445, #835, and #883.
- **CSS Modules class extraction skips `@layer` and `@import` preludes**: `extract_css_module_exports` runs `CSS_AT_RULE_PRELUDE_RE = @(?:layer|import)\b[^;{]*` over the source after string and `url()` stripping and before the class regex, so dot-separated cascade-layer names (`@layer foo.bar { ... }`, `@layer foo.bar, foo.baz;`) and parenthesised layer references on `@import` (`@import url("x.css") layer(theme.button)`) never reach the class scan. Block bodies are preserved because the strip stops at the opening `{`. The allowlist is intentionally narrow: `@scope (.foo) to (.bar) { ... }` keeps its existing behavior because that prelude IS a selector list and `.foo` / `.bar` are real class references. `@media`, `@supports`, `@keyframes`, `@-webkit-keyframes`, and every other at-rule are also untouched because their preludes do not contain dotted identifiers that the class regex would otherwise capture. Bumps `CACHE_VERSION` because pre-fix cache entries carry phantom exports on `.module.css` files using cascade-layer syntax. As of #550 this prelude strip is the SCSS and parse-failure fallback path: standard-CSS class membership now comes from the CSS AST (next bullet), which excludes these prelude identifiers natively, so `CSS_AT_RULE_PRELUDE_RE` only runs when `extract_css_module_exports` is given no parsed class set. See issues #540 and #549.

- **CSS Modules class set from a real CSS AST** (#550): for standard `.module.css`, `extract_css_module_exports` parses the source with lightningcss (CSS Modules mode, `error_recovery` on) and walks `CssRule::Style` selectors, `@scope` selector lists, and every grouping rule (`@media` / `@supports` / `@container` / `@layer {}` / `@document` / `@starting-style` / nesting) to collect the authoritative set of class-selector names. Nested `:is()` / `:where()` / `:not()` / `:has()` / `:any()` / `::slotted()` / `:host()` / `:nth-child(... of ...)` and the CSS Modules `:local()` / `:global()` wrappers are recursed into. The existing byte-offset scanner then emits one `ExportInfo` per distinct name (first occurrence wins) with a span pointing at the post-dot identifier, so spans stay byte-identical to the prior regex scanner; the parser owns membership (excluding `@layer` / `@import layer()` names, `@keyframes` step selectors, ids and element selectors natively) and the scanner owns only span location. `.module.scss` keeps the regex scanner because Sass syntax is not standard CSS and does not parse, and any CSS the parser rejects falls back to the regex scanner with the `@layer` / `@import` prelude strip. Import extraction (`@import` / `@use` / `@forward` / `@plugin` / `@apply` / `@tailwind`) stays on the regex scanner: those at-rules are Sass / Tailwind extensions that lightningcss does not model. Byte-identical issue counts on all eight benchmark fixtures (including vite and next.js CSS Modules). Bumps `CACHE_VERSION` 150 to 151. See issue #550.

- **Structural CSS analytics** (`fallow health --css`, opt-in): `fallow_extract::compute_css_analytics(source)` walks the parsed CSS AST and returns a `CssAnalytics` of stylesheet aggregates (rule count, declaration count, `!important` count, empty-rule count, max nesting depth) plus a bounded list of `CssRuleMetric` for rules crossing a structural floor (an id selector `specificity_a >= 1`, an over-complex selector with `complexity > 4` components, any `!important` declaration, or `nesting_depth >= 3`). Per-rule it records specificity `(a, b, c)` from the selector AST (max across the rule's selector list), selector component complexity (recursing `:is()`/`:where()`/`:not()`/`:has()`/`:nth-child(... of ...)`), declaration and `!important` counts, and nesting depth. It also collects design-token-sprawl values: every distinct color (via the lightningcss `visitor` feature's `visit_color`, so colors nested inside shorthands / gradients are caught) plus distinct `font-size`, `z-index`, `box-shadow`, `border-radius`, and `line-height` declaration values, in authored form (distinct notations of the same color count separately, since inconsistent notation is itself a sprawl signal; shadow / radius / line-height are serialized at the property level since only their distinct count is surfaced). The CLI reads each `.css` file (SCSS skipped: Sass is not standard CSS), honoring the same `health.ignore` / `--changed-since` / `--workspace` filters as the rest of `fallow health`, unions the per-file token sets into project-wide `unique_colors` / `unique_font_sizes` / `unique_z_indexes` / `unique_box_shadows` / `unique_border_radii` / `unique_line_heights` counts (the shadow / radius / line-height axes render on a `value sprawl (cont.)` line only when present), and surfaces a `css_analytics` block (JSON) plus a `CSS health` section (human). It also collects custom-property definitions (`Property::Custom`), `var()` references (the visitor's `visit_variable` with `VisitTypes::VARIABLES`), `@keyframes` definitions (`CssRule::Keyframes`), and animation-name references (`Property::AnimationName` / `Property::Animation`), and the health layer reports `custom_properties_unreferenced` / `keyframes_unreferenced` as project-wide CLEANUP CANDIDATES (defined somewhere, referenced in no stylesheet). These are deliberately framed as candidates, NOT dead-code findings, because a custom property or keyframes name can still be used from JavaScript (`setProperty` / `getComputedStyle`) or inline HTML, so a hard delete-this finding would be false-positive-prone without cross-language analysis. The health layer additionally scans Vue `.vue` SFCs (`fallow_core::extract::scoped_unused_classes`) for `<style scoped>` classes whose name appears nowhere else in the same component (template, script, or dynamic binding) and reports them as `scoped_unused` candidates: Vue does not warn on these (Svelte's own compiler does, so Svelte's auto-scoped blocks are left to it). The token test is whole-identifier (`-`/`_` are identifier chars) and any occurrence outside the `<style>` blocks counts as a use, so the signal errs toward "used"; blocks with `:global` / `:deep` / `::v-deep` / `@apply` and `lang="scss"` blocks are skipped wholesale. SFC `<style>` blocks (any scoping) are ALSO folded into the standard metric path via `fallow_core::extract::sfc_virtual_stylesheet` (each block body is blank-line-padded to its real SFC line so rule locations map back), so a Vue/Svelte component's styles contribute to the specificity / `!important` / value-sprawl aggregates the same as a `.css` file instead of being silently excluded. Unreferenced `@keyframes` are emitted as a LOCATED `unreferenced_keyframes[{name, path}]` list (low-cardinality, rarely JS-set), while the higher-cardinality, JS/cross-app-prone `custom_properties_unreferenced` stays a count. The INVERSE "used-but-undefined" direction is also reported (the unresolved-reference bucket applied to CSS): an `animation` / `animation-name` referencing a `@keyframes` defined in NO stylesheet anywhere is a LOCATED `undefined_keyframes[{name, path, actions}]` candidate (usually a typo or a removed animation; the residual non-typo case is a `@keyframes` defined in CSS-in-JS the parser never sees, which the `verify-undefined` action names), while `var()` references with no CSS definition stay a COUNT (`custom_properties_undefined`) because they are dominated by JS-set design tokens (locating them would be net-noise, the same FP reasoning as the unreferenced direction). It also surfaces DUPLICATE DECLARATION BLOCKS (fallow's duplication signal applied to CSS): each style rule's declaration set is fingerprinted (`css_metrics::declaration_block_fingerprint`, each declaration serialized via lightningcss with the `!important` flag, sorted for order-insensitivity, joined, xxh3-hashed) when it has at least `MIN_BLOCK_DECLARATIONS` (4) declarations, and the health layer groups identical fingerprints across files into `duplicate_declaration_blocks[{declaration_count, occurrence_count, estimated_savings, occurrences:[{path, line}], actions}]` (sorted by estimated savings descending). The 4-declaration floor keeps the signal a strong copy-paste indicator: a small block (`display: flex; align-items: center`) repeats legitimately, a 4+ block repeated is almost always copy-paste; `estimated_savings = (occurrence_count - 1) * declaration_count`. Per-file fingerprints are an internal `#[serde(skip)]` staging field on `CssAnalytics` (never serialized), capped at `MAX_DECLARATION_BLOCKS` per file. It also surfaces TAILWIND ARBITRARY-VALUE BYPASSES (`fallow_extract::tailwind::scan_tailwind_arbitrary_values`, a regex over markup for `prefix-[value]` tokens like `w-[13px]` / `bg-[#abc]` / `grid-cols-[1fr_2fr]`): Tailwind utilities that hardcode a one-off value instead of a configured scale token (design-token bypass that AI edits over-produce and no per-rule linter aggregates). The health layer scans `.jsx` / `.tsx` / `.html` / `.astro` / `.vue` / `.svelte` markup (honoring the same filters), aggregates by token into `tailwind_arbitrary_values[{value, count, path, line}]` (sorted by use count descending, first location) plus `summary.tailwind_arbitrary_values` / `tailwind_arbitrary_value_uses`. The scan is GATED on the root `package.json` declaring a `tailwindcss` / `@tailwindcss/*` dependency (`project_uses_tailwind`), since the `prefix-[value]` shape is Tailwind-specific in practice but not formally exclusive; on a non-Tailwind project the scan never runs. It also reports UNUSED CSS AT-RULE ENTITIES (the set-difference engine applied to more entity types): an `@property --x` registered (`CssRule::Property`) but never read via `var()` in any stylesheet, and an `@layer` declared (`CssRule::LayerStatement` names or a named `CssRule::LayerBlock`) but never populated by a block. Both are located in `unused_at_rules[{type, name, path, actions}]` (`type` = `property-registration` / `layer`) with `summary.unused_property_registrations` / `unused_layers` counts. Conservative candidates: an `@property` can be read from JavaScript, and a layer can be populated via `@import layer()`, so the verify action names those out-of-CSS consumers. `summary.notable_truncated_files` surfaces per-file `notable_rules` truncation at the summary level; the human renderer labels the value-sprawl line "value sprawl" (not "design tokens", which would overclaim) and tags every deadness line as a candidate to verify. This is a health SIGNAL, not a dead-code finding; it never appears under bare `fallow`, `audit`, or `dead-code`. See issue #550.
- **CSS-in-JS tagged-template lifting (Phase 3b, `fallow health --css`)**: a styled-components / emotion / linaria app writes its CSS as the body of a tagged template (`` styled.div`...` ``, `` styled(Btn)`...` ``, `` css`...` ``, `` keyframes`...` ``, `` createGlobalStyle`...` ``, `` injectGlobal`...` ``), which previously reached NONE of the structural analytics (a CSS-in-JS app got `null` `css_analytics`). `fallow_extract::css_in_js_virtual_stylesheet(source)` (`crates/extract/src/css_in_js.rs`) is the tagged-template analogue of `sfc_virtual_stylesheet`: a lexical byte scanner (NOT an AST pass) finds CSS-in-JS tagged templates via the `CSS_IN_JS_TAG_RE` anchor (tag immediately followed by a backtick), scans each template body to its matching backtick (handling `${...}` interpolations with brace depth, nested template literals, and string literals so a `}`/`` ` `` inside an interpolation does not close early), masks every `${...}` interpolation to a CSS-valid identifier placeholder (`fallowinterp`, newline count preserved so lines after a multi-line interpolation stay aligned), wraps each body in a synthetic `.fallow-css-in-js{ ... }` rule so top-level declarations are counted, and blank-line-pads each lifted block to its real source line so metric line numbers map back. It returns `None` when the source has no CSS-in-JS template. The engine admits a JS/TS arm in `css_report_scan_target` (`crates/engine/src/health/execute.rs`) as `CssScanKind::CssInJs`, GATED on `project_uses_css_in_js(root)` (root `package.json` declaring `styled-components` / `@emotion/styled` / `@emotion/react` / `@emotion/css` / `@linaria/core` / `@linaria/react`), so a non-CSS-in-JS project NEVER scans `.js` / `.ts` files (no `files_analyzed` inflation, output byte-identical to pre-3b) and `css_report_stylesheet_source` routes the JS/TS source through the lifter. The lifted virtual stylesheet flows through the SAME `walk_css_files` accumulator as `.css` files and SFC `<style>` blocks, so CSS-in-JS automatically contributes to `summary.files_analyzed`, the project-wide token sets, duplicate-declaration-block fingerprints, structural metrics, and therefore styling-health (the `MIN_CONFIDENT_DECLARATIONS` floor marks a sparse CSS-in-JS surface low-confidence automatically). Masking leans on `compute_css_analytics`'s `error_recovery: true`: an interpolation a placeholder cannot make valid is dropped rather than emitted as a garbled/false structural finding, and a real authored `!important` is preserved and counted. Scope is TEMPLATE-LITERAL form only; the object form (`css({...})` / `styled.div({...})`, vanilla-extract `style({...})`) is JS-object-to-CSS serialization and is deferred, as is the `styled.div.attrs(...)` chain whose backtick does not immediately follow the tag. Health-time-only over file SOURCE (re-read in `walk_css_files`), so NOTHING is added to `crates/extract/src/cache/types.rs` and there is NO `CACHE_VERSION` bump; styling-health stays descriptive-only (never gates an exit code or badge). Validated non-null with sane structural numbers on the Next.js `with-styled-components` and `with-emotion` example apps (both `null` before). See `.plans/css-in-js-first-class.md` (3b).

- **Unused Tailwind v4 `@theme` design tokens** (`fallow health --css`, opt-in): a Tailwind v4 `@theme` token (`--color-brand`, `--radius-card`) defines a design token whose `<name>` becomes the suffix of a generated utility (`--color-brand` -> `bg-brand`/`text-brand`; `--radius-card` -> `rounded-card`; `--font-poppins` -> `font-poppins`). `fallow_extract::scan_theme_blocks` brace-matches `@theme` / `@theme inline` / `@theme static` blocks (comment + string masked) and returns the top-level `--ident` tokens plus the `var()` reads inside the block interior (lightningcss skips the unknown `@theme` at-rule, so these are tracked separately and never pollute the shared `referenced_custom_properties` set); `extract_apply_tokens` returns `@apply` body utilities. The health layer (`scan_unused_theme_tokens`) classifies each token against `crates/cli/data/tailwind-theme.toml` (the versioned namespace list, longest-prefix match so `font-weight-bold` resolves to namespace `font-weight` / name `bold`, NOT a code constant) and flags a token as USED via any of: a `*-<name>` utility suffix found among class-shaped tokens scanned from non-CSS source (markup `class`/`className`, `clsx` args, CSS-in-JS, `@apply` bodies); or a `var(--<ns>-<name>)` read in any stylesheet including a `@theme` token backing another. The source scan captures the dashed SHAPE (`-<name>`), never a bare word, so a dictionary-word token name (`brand`/`card`/`muted`) is not credited just because the word appears in a `.tsx` file. Set-difference yields `unused_theme_tokens[{token, namespace, path, line, actions}]` + `summary.unused_theme_tokens`. False-negative-leaning (every test credits usage). Heavily gated (panel BLOCKs, validated zero-FP on the Next.js bundle-analyzer / next-saas-starter / tailwindcss docs site / shadcn-ui, surfacing genuinely-dead shadcn `chart-*` / `sidebar-*` tokens): emits only on a Tailwind v4 project (`tailwindcss` dependency + at least one `@theme` block); hard-abstains on a `@plugin` directive or a `tailwind.config.*` `plugins[]` (a plugin consumes tokens via `theme()`/`addUtilities` invisibly to the scan, the DI blind spot; project-wide, so one `@plugin` in a monorepo abstains the whole run, the safe direction); abstains per-stylesheet when the `@theme` file is a published package surface (`published_css_paths`, a public token API); and abstains on a partial-scope (`--changed-since` / `--workspace`) run. The `--breakpoint-*` / `--container-*` variant namespaces (their `<name>:` / `@<name>:` variant usage needs a dedicated variant parser) and the Tailwind `--<token>--<property>` modifier form (`--font-sans--font-feature-settings`, an option ON a token, no standalone utility, real-world-smoke FP) are excluded from candidacy. Candidates, never gated findings, each with a read-only namespace-qualified verify command. `CssAnalytics` is recomputed per run (not cached), so no `CACHE_VERSION` bump. Health SIGNAL only; never under bare `fallow`, `audit`, or `dead-code`. The SAME gated candidate set also powers the `token_consumers` reverse index (CSS program Phase 2): `build_token_consumers` (`execute.rs`) inverts the consumer signals into a per-token `consumer_count` + a capped located `consumers[{path, line, kind}]` sample (`kind` = `theme-var` / `css-var` / `utility` / `apply`), so an agent can read a token's blast radius before changing it. It threads `(path, line)` through the var-read scan (`scan_theme_blocks` `theme_var_reads` now carries the line; `extract_css_var_reads_located` locates regular-CSS reads OUTSIDE `@theme` interiors so the two var kinds never double-count) plus a located markup / `@apply` class scan, all ADDITIVE fields on `CssTokenSets` (the existing name-only sets and the `scan_unused_theme_tokens` detector are byte-unchanged). `consumer_count` is a static lower bound (a computed class name `bg-${c}` is not counted), so `token_consumers` is DESCRIPTIVE context with no `actions` array; the authoritative dead-token action stays on `unused_theme_tokens`. Engine-local, no `CACHE_VERSION` bump.
- **CSS Modules class extraction records real source positions**: every strip pass (block comments, SCSS line comments, quoted strings plus `url(...)`, the `@layer` / `@import` prelude strip) replaces matches with ASCII spaces of equal byte length instead of collapsing the range, so `CSS_CLASS_RE` capture offsets in the masked buffer index 1:1 back into the original source. `extract_css_module_exports` emits `Span::new(start, end)` pointing at the bare class identifier (no leading dot) on each `ExportInfo`, matching how oxc spans identifiers elsewhere. CSS/SCSS `@import`, `@use`, `@forward`, and `@plugin` imports also carry spans for the authored specifier when one exists; implicit `tailwindcss` imports synthesized from `@apply` / `@tailwind` intentionally remain unpositioned. Downstream `compute_line_offsets` resolves the real declaration line and column, so LSP "go to source", hover anchors, SARIF regions, and `--diff-file` line-overlap classification all land on the actual selector or directive rather than line:1 col:0. The function signature carries `is_scss: bool` so the SCSS-line-comment strip only fires for `.module.scss`. See issues #549 and #445.
- **Vue/Svelte SFC template asset references**: static markup asset references (`<img src="./logo.png">`, `<source src="...">`, `<video poster="...">`, plus `audio` / `track` / `embed` `src`) in `.vue` / `.svelte` markup emit `SideEffect` imports via the regex scanner `collect_template_asset_refs` (`crates/extract/src/sfc.rs`), so a genuinely-missing relative asset surfaces as `unresolved-import` while an existing one resolves to `ExternalFile` (no finding) on the trusted resolver path (`resolve_filesystem_path` disk-checks `path.exists()`). Scoped to genuine asset ELEMENTS (`img` / `source` / `video` / `audio` / `track` / `embed`) so a custom component's `src` PROP (`<MyImage src="./x">`) is never read as an asset edge. Conservative literal-only: only plain relative literals (`./` / `../`) are captured; dynamic bindings (`:src`, `v-bind:src`, `bind:src`, `src={...}`, `data-src`), alias-prefixed (`@/`), root-relative (`/foo`), remote, interpolated (`{{ }}` / `{ }`), and query/hash-suffixed (`./x.png?inline`) values all abstain (the value class excludes `{`, `?`, `#`, whitespace, angle brackets, and the alternation anchors on a leading `./`/`../`). `<script>` / `<style>` blocks and HTML comments are masked to equal-length spaces before scanning so code strings never contribute (and offsets still map for line/col). No new finding type or config rule: this reuses `unresolved-import` (the resolver + finding path), so the only behavior change is missing template assets joining that category. `srcset` lists and CSS `url()` in `<style>` are out of scope for the first cut. `CACHE_VERSION` bumped (the SFC import set changes). Verified zero-FP on the svelte + vue-core benchmarks (one true-positive `./icon.svg` in a Svelte validator test sample, vue-core byte-identical).
- **Vue/Svelte SFC `<style>` blocks**: `<style lang="scss">` and `<style lang="sass">` block bodies are scanned for `@import` / `@use` / `@forward` / Tailwind CSS `@plugin` directives via the same regexes used for plain `.scss` files (issue #195 Case B). Plain `<style>` (no `lang`) and `<style lang="css">` are scanned for `@import` and `@plugin`. `<style src="./theme.scss">` external style references emit a `SideEffect` import on the SFC. Each style-context import is tagged with `from_style: true` on `ImportInfo` so the resolver routes through the SCSS fallback chain (CSS-extension probe, partial convention `_filename`, include paths, node_modules) BEFORE the standard JS resolver. Without that routing, `./Foo` from a `Foo.vue` style block would resolve to the SFC itself (`.vue` precedes `.scss` in the resolver's extension list); the CSS-restricted probe selects the sibling `Foo.scss` instead. Languages other than CSS / SCSS / Sass (`postcss`, `less`, `stylus`) only have their `<style src>` reference seeded; their body syntax differs (e.g., Less `@import (reference)` modifier) and is out of scope.
- **Glimmer component files**: `.gts` and `.gjs` files are discovered as source files. `.gts` parses with TypeScript source semantics and `.gjs` parses with JavaScript module semantics. `<template>...</template>` blocks are stripped before Oxc parsing while preserving byte offsets and line numbers, so imports before/around templates are still extracted and resolved through the same tsconfig path-alias path as `.ts` importers. Two replacement strategies are used so the post-strip source stays syntactically valid in both canonical GTS contexts: class-body templates (`class X { <template>...</template> }`) blank to spaces, leaving an empty class body; module-level template expressions (`const X = <template>...</template>`) and other expression-position templates (function argument, ternary branch, decorator-call argument) are replaced with a byte-length-preserving parenthesized template literal `` (`...`) ``. Without the expression-context branch, a file mixing both shapes parses to `const x = ;`, oxc bails, and every import in the file is dropped. Expression position is detected by the previous non-whitespace byte being one of `=`, `,`, `(`, `?`, `:`, plus an identifier-byte lookback that matches the expression-prefix keyword set `{default, return, throw, yield, await, new}` so the canonical template-only-component shape `export default <template>...</template>` (no `const` wrapper) does not blank to `export default ;`. User identifiers ending in a keyword suffix (`mydefault`, `$return`) walk back the FULL identifier and do not match. See issues #270, #375, #379.
- **Namespace destructuring**: `const { a, b } = ns` → member accesses. Rest patterns (`const { foo, ...rest } = ns`) → conservative whole-object use. Works with static/dynamic imports and require.
- **Node `child_process.fork()` entrypoints**: module-scope `fork()` calls emit `DynamicImportInfo` entries only when the callee is proven to come from `node:child_process` / `child_process`. Supported provenance forms are `import { fork }`, `import * as childProcess`, `const { fork } = require(...)`, and `const childProcess = require(...)`. Supported targets are local string/template literals without expressions, `new URL('./runner.js', import.meta.url)` bindings, and `path.resolve(filename, '../runner.js')` where `filename` is bound to `fileURLToPath(import.meta.url)` and `path` is from `node:path` / `path`. Unresolved identifiers, non-local specifiers, computed templates, nested shadowed calls, and unrelated `fork` functions are ignored. See issue #638.
- **Node Module Customization Hooks (`register()`)**: `register('@swc-node/register/esm', pathToFileURL('./'))` calls (Node.js `module.register()`) credit the first-argument specifier as a `DynamicImportInfo`. The package referenced by the specifier is no longer reported as unused. Gated by an imports-table lookup (`is_node_module_register`) against the Node `module` builtin in BOTH source forms (`node:module` AND bare `module`). Four shapes engage: (1) named import + literal call (`import { register } from 'node:module'; register('pkg', ...)`), (2) aliased local name (`import { register as reg } from 'node:module'; reg('pkg', ...)` resolves the local binding through the imports table), (3) namespace-call form (`import * as Module from 'node:module'; Module.register('pkg', ...)` matches a `StaticMemberExpression` callee whose object is a `Namespace` import of `node:module` / `module`), (4) template-literal first arg (`` register(`tsx/esm`) ``, no-substitution only). Non-string specifiers (`register(varName, ...)`), empty-string specifiers, interpolated template literals, and `register` from any other source are skipped. Routed through the existing `dynamic_imports` resolution pipeline, so bare specifiers like `@swc-node/register/esm` resolve to `ResolveResult::NpmPackage("@swc-node/register")` and credit `package_usage`. See issues #293 (initial fix) and #296 (broadened to namespace + template-literal forms; switched vehicle from `require_calls` to `dynamic_imports`).
- **Vitest auto-mocks**: `vi.mock('./services/api')` synthesizes a dynamic namespace import of `./services/__mocks__/api`, crediting the auto-mock sibling file (and all its exports) as used so vitest's `__mocks__/<file>` convention does not surface as `unused-file`. The visitor ALSO records the `vi.mock` target itself as a side-effect dynamic import so a test file that mocks a module (with or without a factory) does not flag the target as `unused-file`, while unused exports in that target remain reportable (issue #311). Handles string-literal sources, expressionless template literals, and the `vi.mock(import('./api'))` argument form. Skipped when the source is empty, contains a URL scheme, starts with `data:`, already contains a `__mocks__` segment (avoids `__mocks__/__mocks__/...`), or is bare-package only with no `/` separator. Path-alias prefixes survive: `vi.mock('@/src/foo/bar')` synthesizes `@/src/foo/__mocks__/bar`, which then resolves through the importer's tsconfig path aliases. **Factory form (`vi.mock(spec, factory)`)**: vitest does NOT consult the `__mocks__/<file>` sibling when a factory is provided, so fallow suppresses the synthesis whenever the second argument is an arrow function, function expression, or any parenthesized variant of either; the target itself is still credited as side-effect reachability. Options-form `vi.mock(spec, { spy: true })` (object literal as second argument) is treated as auto-mock-with-options and DOES still synthesize, matching vitest's runtime behavior (issue #311). The `vi` callee is matched by name without a vitest-import gate (parallel to how `require` is matched); false positives outside test files are inert because the synthesized specifier just fails to resolve, no exports are fabricated and no member accesses are added. **No-sibling case** (issue #378): vitest's auto-mock works in-memory and does not require a `__mocks__/<file>` directory the way Jest does, so the synthesised dynamic import is marked `is_speculative: true` on `DynamicImportInfo`. At resolution time, `resolve_single_dynamic_import` drops speculative entries silently when their target is `Unresolvable`; the credit path (target exists) is unchanged. Without the drop, projects that rely on Vitest's in-memory auto-mocking surface a spurious `unresolved-import` finding pointing at a path they never wrote. Bare-package mocks (`vi.mock('axios')` paired with a project-root `__mocks__/axios.ts`) and Jest's `jest.mock(...)` are intentionally out of scope. See issues #251, #311, #378.
- **Vitest `test.alias` / `resolve.alias` (all surfaces)**: Vitest merges `test.alias` AND `resolve.alias` (top-level and per `test.projects[*]`) at test time, and the config can live in `vitest.config.{ts,js,mts,mjs}`, embedded in `vite.config.{ts,js,mts,mjs}` (the common `defineConfig({ test, resolve })` shape), or in a `vitest.workspace.{ts,js}` array file (`defineWorkspace([...])`). The shared `crate::plugins::test_alias` module funnels every surface through one `process_test_alias`: the Vitest plugin handles `vitest.config.*` top-level `test.alias` + `resolve.alias`, `test.projects[*].test.alias` + `test.projects[*].resolve.alias`, and the workspace-array file (each element's `test.alias` + `resolve.alias`; `find_config_object` returns None for an array default export, so the array is walked directly); the Vite plugin reuses `apply_test_block_aliases` to handle the `test.alias` + projects surfaces inside `vite.config.*` (which never reaches the Vitest plugin, whose `config_patterns` are `vitest.config.*` / `vitest.workspace.*` only). `resolve(...)` / `new URL(...)` / `new URL(...).pathname` / `fileURLToPath(...)` replacements all normalize (the `.pathname` accessor on a `new URL(...)` is unwrapped in `expression_to_path_string`). Each entry feeds three cooperating mechanisms: (A) the alias is pushed into `PluginResult.path_aliases` so a virtual-module / alias-only import (`vscode` -> `./mock/vscode.js`, where `vscode` is not an npm dependency) resolves instead of surfacing as `unresolved-import` / `unlisted-dependency`; (B) when the replacement names a local source FILE (extension `ts,tsx,js,jsx,mjs,cjs,mts,cts`), it is seeded as a support entry point via `setup_files` so an aliased `__mocks__` file keeps ALL its exports credited even when the original package resolves through `node_modules` (the production import never reaches the path-alias fallback in that case, so (A) alone is insufficient; this is the load-bearing fix for the real-package mock case); (C) a bare-package alias KEY is credited as a referenced dependency so redirecting its import through the alias (only happens when `node_modules` is absent) does not regress into a false `unused-dependency`. **Package-to-package vs directory alias is discriminated FILESYSTEM-FREE**: the package-to-package special case (`'lodash-es' -> 'lodash'`: credit both packages, emit no path alias) fires ONLY when the replacement is a plain bare STRING LITERAL (`expression_to_alias_pairs_kinded` flags this), never a path expression (`path.resolve(__dirname, 'src')`, `fileURLToPath(new URL('./src', ...))`) or a `./`-prefixed string. So a directory alias `@` -> `path.resolve(__dirname,'src')` deterministically takes the path-alias branch on every machine (no `is_dir()` probe, which would flip on sparse checkouts / Docker layers / npm tarballs where `src/` is absent). Residual: a bare-string `'@': 'src'` (no path builder) is ambiguous and treated as package-to-package; rare and non-idiomatic. Aliases apply globally (fallow has no per-file alias scoping; matches Vite); production-graph safety holds because the standard resolver, including tsconfig path aliases, runs BEFORE the plugin path-alias fallback, so a `test.alias` on a tsconfig-aliased prefix does not shadow the real source. Non-goals (silently skipped, diagnosable via `RUST_LOG=debug`): RegExp `find` keys and function `replacement` values (a syntactic analyzer cannot turn a regex into a prefix or execute a function); `mergeConfig(viteConfig, defineConfig({test}))` and imported-and-spread base configs (`find_config_object` cannot statically reach them, so a `debug` line fires); nested `test.projects` INSIDE a `vitest.workspace.*` array element (one level only). Known trade-offs: top-level `resolve.alias` in `vite.config.*` keeps the Vite plugin's path-alias-only (A) extraction (no B/C) to leave pure-Vite projects unchanged, so a MOCK declared in `vite.config.*` top-level `resolve.alias` (rather than `test.alias`) is under-credited (B/C not applied); a genuinely dead export inside an aliased mock (or a non-mock local-file alias target) is hidden by (B); a dependency that remains an alias key but is no longer imported is masked from `unused-dependency` by (C). All are conservative no-false-positive choices matching the Vitest auto-mock precedent. The resolver match is segment-aware (`try_path_alias_fallback`), so an exact key like `@scope/sdk` does not capture `@scope/sdk-extra`, and an exact-key alias to a file resolves without a trailing slash. Complements the parser-level **Vitest auto-mocks** entry above (`vi.mock` synthesis); the two compose without conflict. See issue #601 and its follow-up.
- **Vite and Vitest React Babel plugin dependencies**: Vite and Vitest config parsing credits static Babel plugin and preset entries passed through `@vitejs/plugin-react` options: `plugins: [react({ babel: { plugins, presets } })]`. The extractor is gated on an import from `@vitejs/plugin-react`, supports default imports and `default as` aliases, reads plain string entries plus tuple first elements, and normalizes Babel's `module:` prefix before package-name extraction. Relative, absolute, protocol-like, and dynamic entries are skipped. This covers packages consumed by the React transform pipeline, such as `@preact/signals-react-transform`, without broadening ordinary Vite plugin-array behavior. See issue #619.
- **Indirect alias definitions (imported identifier / spread)**: the shared alias extractor (`config_parser::extract_config_aliases[_kinded]`, used by the Vite `resolve.alias`, Webpack `resolve.alias`, Nuxt `alias`, SvelteKit `kit.alias`, AdonisJS `aliases`, TypeScript `compilerOptions.paths`, and Vitest `test.alias` consumers) now recovers aliases when the value is not an inline literal. An identifier resolves to a local `const NAME = [...] | {...}` or one hop through a relative import (`import { sharedAliases } from "./vite.shared.js"`; the sibling is read and parsed for `export const NAME`, `export default`, or a re-exported local `const NAME`). Array spread elements (`[...a, ...b]`) and object spread properties (`{ ...a, "@": "./src" }`) are expanded, each spread source resolved recursively. This fixes the common DRY pattern of sharing one alias array across `vite.config` / `vitest.config` / Storybook via an imported module, which previously dropped every alias and cascaded aliased imports into false `unresolved-import` / `unused-file` / `unused-export` findings. Bounded by an identifier-hop cap plus a per-file visited set (import cycles terminate); bare-package imports of an alias module, regex `find` keys, and dynamically-built arrays stay out of scope (not statically evaluable). See issue #811.
- **Config object passed as a named const to a wrapper call**: `config_parser::find_config_object` (the shared resolver behind `extract_config_string_array` / `extract_config_object` and friends for every config-parsing plugin) already resolved a bare `export default config` identifier; it now also resolves the config when it is passed as an identifier argument to a wrapper call. `export default withMDX(nextConfig)` (the official `@next/mdx` docs idiom), `module.exports = createJestConfig(customConfig)` (`next/jest`), nested `withMDX(withFoo(nextConfig))`, and curried `compose(...)(nextConfig)` (`next-compose-plugins`) all follow the named `const NAME = {...}` (or `const NAME = defineConfig({...})`) to extract the real config, scanning the wrapper call's arguments for the first that resolves and recursing through nested wrapper calls and TS `satisfies`/`as`. This fixes the `page.mdx` / `page.md` unused-files false positive on Next.js App Router projects that wrap their config (the plugin never saw `pageExtensions`), and the same class for any wrapped config (Vite/Webpack/Jest/etc.). An inline object argument is still handled by the pre-existing `extract_object_from_expression` (tried first). The first argument that resolves to an object wins (mirroring `extract_object_from_expression`'s inline-object precedence), so a wrapper where the config is NOT the first object-resolving argument stays out of scope regardless of whether the preceding argument is an inline object or a named const (e.g. `wrapper(namedOpts, nextConfig)` resolves `namedOpts`); no known real-world wrapper uses that order (config is first or curried). Cross-file imported config consts are also out of scope. Byte-identical issue counts on all eight benchmark fixtures. See issue #1642.
- **Unused import bindings**: via `oxc_semantic` scope-aware symbol analysis. Dead imports don't count as references, improving unused-export precision.
- **TypeScript overload dedup**: `export function foo(): void; export function foo(x: string): string;` treated as single export
- **`declare` ambient class properties excluded from members**: `extract_class_members` skips a `PropertyDefinition` whose `declare` flag is set (`declare readonly __input?: I`), alongside the existing `private` / `protected` accessibility skip. A `declare` field is a type-only ambient annotation that emits no JavaScript and cannot be value-referenced, so it can never be a real `unused-class-member`; extracting it produced false findings on classes that annotate injected or framework-provided state with `declare`. Class methods and non-`declare` fields on the same class are still extracted and reported when genuinely unused. Bumps the extract `CACHE_VERSION` because the persisted `CachedExport.members` shape changes (phantom `declare` members drop). See issue #839.
- **Import-then-reexport pattern**: `import { X } from './a'; export { X };` is detected as a re-export (semantically equivalent to `export { X } from './a';`) instead of a local export. Without this, the bottom export collides with the original in duplicate-export detection and is never reached by re-export chain propagation. Handles renames on either side (`import { X as Foo }; export { Foo }` or `import { X }; export { X as Y }`), default imports (re-exported with `imported_name: "default"`), and mixed local + re-export in one statement. Source-order-independent (PR #427): `export { X }; import { X } from './a';` (export before import) is also recognized via a pending-specifier finalize step that resolves the `export { X }` against this module's imports and local declarations after the full AST walk. A local declaration named `X` in the same module shadows the import and keeps the export local. Star re-export propagation mirrors direct named-import type/value selection for duplicate same-named exports, so a merged `export declare namespace X` plus `export const X` credits the value side when a reachable consumer imports `{ X }` through `export *`, while type-only usage stays on the namespace side (issue #1373). Class-inheritance and interface-implementer propagation in `find_unused_members` walks `walk_re_export_origins` for both the parent and the interface key, so `extends BaseShape` / `implements RenderableShape` resolved through a barrel re-exports the parent's `this.*` accesses to the actual defining file. Namespace imports (`import * as ns; export { ns }`) stay as local exports.
- **Class instance members**: `const svc = new MyService(); svc.greet()` and `this.service = new MyService(); this.service.doWork()` track the method as used. Chained `this.field` access uses synthetic `"this.field"` keys in `instance_binding_names`. Factory initializers through wrapper calls are also tracked: `const [svc] = useState(() => new MyService())` binds the first array-destructured element as an instance. The non-destructured `const svc = useMemo(() => new MyService())` form (issue #844) is also bound, but only when the callee is `useMemo` / `React.useMemo` (`is_value_returning_memo_callee`): `useMemo` returns the factory's product directly, whereas `useState` returns a `[value, setter]` tuple (so its non-destructured binding is the tuple, not the instance) and an arbitrary wrapper does not necessarily return the instance, so binding those generically would over-credit and hide genuinely-unused members. Bumps the extract `CACHE_VERSION` because the emitted `member_accesses` change. Typed parameter / property chains through nested object types are also tracked: `constructor(private deps: { foo: FooClass }) { ... } this.deps.foo.foo()` registers `this.deps.foo -> FooClass` via `extract_nested_type_bindings` walking the `TSTypeLiteral`, then `static_member_object_name` records the full dotted-path access (`this.deps.foo`) which resolves through `binding_target_names` so `FooClass.foo` is credited. Same propagation runs through explicit `this.deps = deps` assignments via `copy_nested_binding_targets`, covering the no-accessibility-modifier form. Local typed function calls also credit structural class member use when a direct `new Class()` argument or constructor-bound identifier flows into the exact typed parameter index and that parameter reads members, for example `main(new DurationMS())` with `main(dur: DurationI) { dur.toMs() }` credits only `DurationMS.toMs`; imported callees and wrong argument positions do not credit. Angular 14+ field-initializer DI (`private readonly inner = inject(InnerService)`) is recognized: `extract_angular_inject_target` reads the call's `inject<T>()` type argument or first identifier argument and registers `this.inner -> InnerService` so `this.inner.member` chains credit `InnerService.member`. For exported component classes, the same named-import `inject()` initializer also records `ClassHeritageInfo.instance_bindings`, so external templates can credit `service.member()` through untyped fields. Aliased named imports like `inject as ngInject` are supported by import provenance. The `inject` callee is gated by a named-import check against `@angular/core`, so a same-named `inject` from any other module is intentionally ignored (false positives over false negatives). **Static-factory call results** (issue #346): when a static method's body's last top-level statement is `return new this()` or `return new <SameClassName>()`, `extract_class_members` flags the matching `MemberInfo` with `is_instance_returning_static`. The visitor records every `const <local> = <ID>.<method>()` shape into a `FactoryCallCandidate` list, and a finalize step resolves each candidate against this module's `local_class_exports` (same-file: bind `local -> ClassName` directly so `resolve_bound_member_accesses` re-emits accesses against the class) or `imports` (cross-file: bind `local -> FACTORY_CALL_SENTINEL:ID:method`). The analyze-layer `propagate_factory_call_accesses` pass parses the sentinel out of `access.object`, resolves `ID` via `build_local_to_export_keys`, walks `walk_re_export_origins` for barrel chains, and credits `access.member` on the origin class export only when its `MemberInfo` for `method` has `is_instance_returning_static`. Conservative: chained `.build()`, ternary returns, and named-target factories (`Wrapper.getBuilder() { return new MyClass(); }`) stay out of scope. Globals like `Math.floor(...)` match neither a local class nor an import and are dropped without effect. Scope-unaware, false matches produce false negatives, not false positives. See issues #244 and #911 (Angular `inject()` field), #246 (Playwright fixtures, see next entry), #346 (static-factory methods), #910 (local structural calls).
- **Constructor-rooted fluent chains and `this` return types** (issue #953): `new Class().a().b().done()` credits `b` and `done` only when every prior chain step is self-returning. `extract_class_members` treats both explicit same-class return annotations (`a(): Class`) and TypeScript polymorphic `this` return annotations (`a(): this`) as self-returning, matching the compiler contract even when the method body returns through a helper such as `return this.setIfProvided(...)`. A bare body heuristic still recognizes an unannotated final `return this`. Terminal methods such as `build()` do not need to be self-returning themselves, but all intermediate methods before them do. Bumps the extract `CACHE_VERSION` because the cached `is_self_returning` flags change.
- **Typed instance chains** (issues #386, #388): `ClassHeritageInfo.instance_bindings` are extracted for all classes, not only Angular-decorated classes. The binding set includes non-private typed constructor parameters, typed properties, nested object-type properties, and typed getters, so a non-Angular chain like `factory.service.query()` credits `Service.query` when `factory` is a `Factory` instance and `Factory.service` is declared as `Service`. Reflective whole-object uses such as `Object.keys(factory.service)` propagate through the same typed chain and mark the target class as whole-object used. This covers getter-backed Playwright fixture teardown factories without suppressing genuinely unused service methods. **Generic-constraint substitution** (#388): on `visit_class` enter, the visitor pushes a `<TParam, Option<Constraint>>` map collected via `collect_class_type_param_constraints` onto `class_type_param_constraints` (popped on exit). `record_typed_binding` consults the top of the stack: a constrained parameter substitutes its constraint (`class BaseService<TClient extends BaseClient>` with `constructor(client: TClient)` registers `this.client -> BaseClient`); an unconstrained parameter drops the binding (no resolvable class). The substitution flows through `extract_nested_type_bindings` so nested object-type properties typed as a generic parameter also resolve correctly. `extract_class_instance_bindings` applies the same substitution when populating `ClassHeritageInfo.instance_bindings`.
- **Typed destructure bindings** (issue #752): a destructured `let`/`const` binding or destructured function parameter that carries a type annotation populates `binding_target_names` so subsequent `<local>.<member>` accesses resolve onto the referenced class. Covers the Svelte typed-prop shape `let { resultState }: Props = $props()` where `interface Props { resultState: ResultState }`, the inline-literal form `let { resultState }: { resultState: ResultState } = ...`, object type-alias references, rename forms (`let { resultState: rs }: Props = ...` binds `rs`), and destructured params (`function render({ resultState }: Props) { resultState.pin() }`). Interface / object-type-alias property types are collected at module scope (`collect_object_type_property_types`) and resolved in the `resolve_typed_destructure_bindings` finalize step so the type may be declared after the binding (interfaces hoist); inline literals resolve in place during the walk. Only bare `TSTypeReference`-to-single-name property types are recorded (`extract_type_annotation_name`); union / array / generic-wrapped property types, nested destructures with dotted keys, and rest elements do not resolve (conservative, no false negatives in the over-crediting direction). The finalize step runs first in `into_module_info` / `merge_into` and is also called in `sfc.rs::merge_script_into_module` before the template-visible `binding_target_names()` read so the Svelte/Vue template scanner credits markup member access (`{#if resultState.labelMessage}`, `bind:value={resultState.labelInput}`, `onclick={() => resultState.pin(...)}`). Resolution is type-driven, so it is framework-agnostic (plain TS, Vue, Svelte). Cross-file crediting works through type-only imports because `build_local_to_export_keys` includes resolved type-only imports. Bumps the extract `CACHE_VERSION` (the resulting `member_accesses` change). See issue #752.
- **`instanceof` narrowing member credit** (issue #845): an `if (<local> instanceof <Class>)` guard populates `binding_target_names` with `<local> -> <Class>` so method calls on the narrowed local inside (or after) the guard body (`<local>.<member>()`) resolve onto the class via the same `resolve_bound_member_accesses` path as typed-destructure bindings. Recorded in `visit_if_statement` via `collect_instanceof_narrowings`, which walks the test condition, recurses through `&&`-chained `LogicalExpression`s and `ParenthesizedExpression`s, and collects only simple `Identifier instanceof Identifier` pairs (complex left-hand expressions and member-expression class operands are skipped conservatively). Insertion uses `entry(local).or_insert(class)`, so a pre-existing binding for the same local (a real instance binding, typed-destructure target, etc.) wins over the narrowing. Bindings are module-scoped rather than strictly block-scoped, which can credit a member outside the guard's true narrowed region: this over-credits in the false-negative direction only (a member that is genuinely unused but shares a local name with an instanceof guard could be missed), never the false-positive direction, matching fallow's conservative posture. Genuinely-unused members on the same class still report. Bumps the extract `CACHE_VERSION` (107 to 108) because the resulting `member_accesses` change, structurally identical to the #752 typed-destructure bump. See issue #845.
- **Fluent-builder chain credit** (issue #387): static-builder chains like `EventBuilder.create().setX(1).setY(2).build()` credit each chained method on the originating class. `MemberInfo.is_self_returning` flags instance methods whose declared return type matches the enclosing class name OR whose body's last statement is `return this`. `is_instance_returning_static` widened to also cover static methods whose declared return type matches the class name (covers `static createWithDefaults(): EventBuilder { return chain; }` where the body is too complex to inspect syntactically). At extract time the visitor records one `FLUENT_CHAIN_SENTINEL` `MemberAccess` per call expression chained off a previous call, encoded as `{sentinel}{root_local}:{root_method}:{chain_prefix}` with `member` set to the method being called now; `chain_prefix` is the comma-separated list of intermediate methods walked since the root (empty when the credited member is the first chained call). The analyze-layer `propagate_fluent_chain_accesses` pass resolves the root through each consumer's `local_to_export_keys`, walks each origin via `walk_re_export_origins`, validates the root method has `is_instance_returning_static` and every intermediate chain step is `is_self_returning`, then credits `access.member`. Chains stop at non-self-returning methods, so `.build().toString()` correctly does NOT credit `toString` on the builder. Conservative on mismatch: imports that turn out not to name a class drop silently. Cache version bumped 80 to 81. **Constructor roots** (issue #605): the same chain credit covers chains rooted at a `new` expression (`new OptionBuilder().addDefault(x).addFromCli(y).build()`), the shape two public adoption PRs had to suppress inline (everr#144, graphql-markdown#2949). `static_member_object_name` gains a `NewExpression` arm returning the bare `Identifier` callee name UNCONDITIONALLY (no builtin-name guard: distinguishing a global `new Map()` from a user `class Map {}` is the analyze layer's job, where a global resolves to no user export and drops while a user class named `URL` / `Request` / `Map` is credited; guarding on `is_builtin_constructor` here would silently drop the access for such user classes and re-introduce the false positive), so a direct `new Repo(client).search(data)` and the FIRST method off any constructor chain are credited as plain `Class.member` accesses through the existing `local_to_export_keys` path. Downstream chained methods carry a distinct `FLUENT_CHAIN_NEW_SENTINEL` encoded as `{sentinel}{class}:{chain}` (no root method, since a constructor always returns a `class` instance); the analyze-layer `propagate_fluent_chain_new_accesses` pass resolves `class`, walks re-export origins, and requires every `chain` step to be an `is_self_returning` `ClassMethod` before crediting `member`. The #387 self-returning safety check still applies (a member reached after a non-self-returning step stays flagged), and genuinely-unused members on the constructed class are still reported. Member-expression constructor callees (`new ns.Builder()`) are out of scope. Cache version bumped 98 to 99.
- **Playwright POM fixtures**: typed `base.extend<MyFixtures>({ ... })` definitions are correlated with their callback-side member uses so POM class methods consumed only through fixtures are credited. The visitor records two synthetic-object-prefix `MemberAccess` flavors keyed by the test's local name: `__fallow_playwright_fixture_def__:test:fixture` (member = the declared POM type name) and `__fallow_playwright_fixture_use__:test:fixture` (member = the accessed method name). Definitions accept the fixture map as a named type alias (`type MyFixtures = { adminPage: AdminPage }; base.extend<MyFixtures>(...)`), an inline type literal (`base.extend<{ adminPage: AdminPage }>(...)`), an intersection of either, or any parenthesized form; alias bodies are pre-collected into `playwright_fixture_types` so a referenced alias resolves to the same `(fixture, type_name)` pairs as an inline literal. The `base.extend` callee is gated against an `@playwright/test` named-import (`base` aliased from `test`), so `someOther.extend<T>(...)` is ignored. Use-side collection runs at every call expression: when the callee root identifier resolves to a Playwright test object, the first arrow / function argument's object-pattern parameter (`{ adminPage }`, with rename support `{ adminPage: alias }`) seeds a per-call `fixture_by_local` map, and a sub-visit over the callback body emits one use-sentinel per `<local>.<method>` static-member access. Nested fixture types (`{ pages: { adminPage: AdminPage; userPage: UserPage } }`) and nested usage are both supported: type traversal recurses through `TSTypeLiteral` / aliased `TSTypeReference` properties under a dotted path so the def-side sentinel keys become `pages.adminPage`, etc. Use-side handles two consumption styles with the same dotted-path key: nested destructure (`{ pages: { adminPage } }` → `adminPage.method()`) maps the local to its full path, and chained access on a top-level destructure (`{ pages }` → `pages.adminPage.method()`) walks the static-member chain and concatenates the leftmost local's resolved path with the remaining segments. The matched chain is not re-walked, so intermediate member exprs do not emit spurious `(pages, adminPage)` accesses. See issue #268. **Helper-function fixtures** (issue #491): when `base.extend<T>(...)` is returned from a helper instead of bound directly to a `const`, the same sentinel correlation runs keyed by the helper's name. Three declarator shapes capture the helper: `function appTest() { return base.extend<T>(...); }`, `const appTest = () => base.extend<T>(...)`, and `const appTest = function () { return base.extend<T>(...); }`. The body must be a single `return <call>` statement (multi-statement bodies and conditional returns stay out of scope to keep matching conservative). A finalize-time pass (`ModuleInfoExtractor::resolve_playwright_factory_call_definitions`) gates each captured helper on `base` resolving to a `test` named import from `@playwright/test` and emits the def-sentinel `__fallow_playwright_fixture_def__:appTest:<fixture>` per type binding. Same-file helper chains (`function appTest() { return setupTestFixture(); } function setupTestFixture() { return base.extend<T>(...); }`) propagate via a capped fixed-point pass over `(caller, callee)` aliases captured at the same site, so the outer name inherits the inner helper's bindings. Cross-file chains are intentionally out of scope: the propagation is per-module. Use-side recognition extends `playwright_test_callee_name` with a `CallExpression` arm that recurses into the inner call's callee, so `appTest()(...)` and `appTest(opts)(...)` extract the helper name on the def side. Safe because def sentinels gate the analyzer; use sentinels for unmatched helper names produce no credit. The analyzer (`propagate_playwright_fixture_accesses` in `unused_members.rs`) walks both sentinel flavors, resolves the test identifier through each importing file's `local_to_export_keys` so cross-file imports / aliased imports (`import { test as fixtureTest } from './fixtures'`) match the same export key on both sides, follows re-export chains for the POM type via `walk_re_export_origins`, and credits the accessed method into `accessed_members` on the defining file's class export. Runs BEFORE `propagate_accesses_through_re_exports` so barrel-re-exported POMs are also covered.
- **Imported Playwright fixture type aliases** (issue #1190): Playwright fixture definitions can import object type aliases from orchestrator / factory modules (`app: AppFixture`) and still map nested fixture paths like `app.assert.messageChecks` to the concrete POM class (`MessageChecks`). Extraction emits an internal fixture-type sentinel for imported alias bindings, and the analyzer expands those aliases before correlating fixture definitions with callback-side member uses. This covers fixture setup that steps through a separate class orchestrator while keeping unmatched sentinels conservative.
- **Playwright fixture wrapper aliases** (issue #1210): `mergeTests(...)` wrappers and chained wrapper `.extend(...)` calls inherit fixture definitions from their wrapped Playwright test objects before callback-side member uses are correlated. Extraction emits `__fallow_playwright_fixture_alias__:<alias>:` member accesses for Playwright's named `mergeTests` import, including `mergeTests as merge`, and for `.extend(...)` calls whose base is not the direct `@playwright/test` `test` import. The analyze layer resolves both alias and base through `build_local_to_export_keys`, expands alias targets transitively with a bounded fixed-point pass, and dedupes fixture targets, so cycles or repeated wrappers do not loop. Same-named local helpers such as `function mergeTests(...)` are ignored, and unmatched wrapper aliases produce no credit.
- **Playwright fixture branch aliases** (issue #1270): callback-side fixture member usage is credited through simple local aliases that resolve directly to destructured fixtures or dotted fixture paths, including ternary selections and all-fixture `if/else` or `switch` assignments. Same-file local `base.extend<T>(...)` fixture constants can now feed an exported `mergeTests(...)` wrapper without being treated as public exports. The logic is Playwright-only, order-sensitive, and conservative on shadowing, nested callback aliases, unknown reassignment, and mixed fixture/non-fixture branches.
- **Type-level member access**: `TSQualifiedName` (e.g., `type X = Status.Active`) tracked as member access. Mapped type constraints (`{ [K in Enum]: ... }`, `{ [K in keyof typeof Enum]: ... }`) and `Record<Enum, T>` mark all enum members as used via whole-object use.
- **TypeScript namespace exports**: `export namespace Foo { export function bar() {} }` extracts `Foo` as a single export with inner declarations as `NamespaceMember` entries, not as separate top-level exports. Runtime namespaces (no `declare`) are NOT type-only. `declare namespace`/`declare module` remain type-only. Nested namespaces flatten members into the outermost namespace.
- **Lit / Web Components registration** (knip #1394): classes registered through a runtime side effect at module load are credited as used even when no other file imports the class identifier. Two patterns are recognized: (1) Lit `@customElement('tag')` decorator on a class declaration, in both named-import (`import { customElement }`, including aliases) and namespace-call (`@decorators.customElement('tag')`) forms, only after the decorator binding is verified to come from `lit/decorators.js` or `lit/decorators/custom-element.js`; (2) `customElements.define('tag', ClassRef)` call expressions at any depth where the second argument is an `Identifier`. The visitor records the local class name into `ModuleInfoExtractor.side_effect_registered_class_names` and a post-walk finalizer (`apply_side_effect_registrations`) flips `ExportInfo.is_side_effect_used = true` for every export whose `local_name` is in the set, covering `export class X {}`, `class X {}; export { X }`, and `export default X` patterns. Anonymous `export default @customElement('x') class extends LitElement {}` is also covered by tracking the pending Default export slot and validating the decorator import after the full walk. The graph layer copies the flag to `ExportSymbol.is_side_effect_used`; `find_unused_exports` and `find_unused_members` OR it with `!references.is_empty()` so reachable side-effect-used exports survive both unused-export and skip-whole-export-dead checks. Persisted across cache rounds via `CachedExport.is_side_effect_used`. Lit lifecycle members (`render`, `updated`, etc.) are allowlisted by the new `lit` plugin via heritage-scoped `UsedClassMemberRule::Scoped` rules keyed on `LitElement` / `ReactiveElement`; native Custom Elements lifecycle members (`connectedCallback`, `observedAttributes`, etc.) are built-in heritage-scoped allowlists for `HTMLElement` subclasses so they work without a Lit dependency. Non-lifecycle methods on Web Component classes are still reported.
- **Angular component metadata**: Full `@Component`/`@Directive` decorator metadata extraction prevents `unused-class-members` false positives. External templates (`templateUrl`) emit `SideEffect` imports and scan HTML for Angular syntax. Inline templates (`template:` string/template literals) are scanned in-place. `host:` object binding expressions extract referenced member identifiers. `inputs:`/`outputs:` string arrays mark listed members as framework-managed (handles `'name: alias'` syntax). Angular 17+ signal APIs (`input()`, `input.required()`, `output()`, `outputFromObservable()`, `model()`, `viewChild()`, `viewChildren()`, `contentChild()`, `contentChildren()`) set `has_decorator` on the property when the class has an Angular decorator. All template scanning supports `{{ }}`, `[prop]`, `(event)`, `[(ngModel)]`, `*ngIf`, `*ngFor`, `@if`, `@else if`, `@for`, `@switch`, `@defer (when expr)`, `@let name = expr;`, pipes. `*ngFor` and `@for` bindings are tracked as locals. `@let` variables are added as template-local bindings. `@defer` `when`/`prefetch when`/`hydrate when` conditions are scanned for member refs. Security scanning emits existing `dangerous-html` sink candidates for non-literal `[innerHTML]="..."` bindings in external templates and inline `template:` strings, with literal bindings skipped. **Inherited members and DI-injected service access** (issues #174, #911): child-component template refs propagate up through `extends` chains so a member only defined on a base class is credited when referenced from a child's template. Member-access chains on unresolved identifiers (`dataService.getTotal()` in the template) are resolved through the importing component's typed instance bindings: constructor parameters with accessibility modifiers (`constructor(public dataService: DataService)`), non-private typed property declarations, and named-import Angular `inject(Service)` property initializers are captured into `ClassHeritageInfo.instance_bindings` and used at analysis time to credit the target class's member as used. Inline templates reuse the visitor's existing `binding_target_names` resolver, external templates resolve in the analysis bridge. **`inject(InjectionToken<Interface>)` template members** (issues #920, #923): when a template member call resolves through a field bound to an `InjectionToken` const (`readonly greeter = inject(GREETER)`, where `GREETER = new InjectionToken<Greeter>(...)`), the binding lands on the memberless token, not a class, so #913's concrete-class crediting could not reach the implementation. Extraction records `new InjectionToken<I>(...)` declarations imported from `@angular/core` as `(token_export_name, interface_name)` pairs on `ModuleInfo.injection_tokens` (extract `CACHE_VERSION` bumped for #920; a token with no type argument carries no interface and is skipped, and a same-named non-`@angular/core` `InjectionToken` is ignored by import provenance). The analyze-layer template-chain bridge resolves the token export key (through `export_key_with_origins`, so a barrel-re-exported token works) to its interface, then credits the accessed member on EVERY class that `implements` that interface. External `templateUrl` chains come from the imported HTML module; inline `template:` chains are consumed from the component's own module before the same bridge runs. The explicitly interface-typed field form (`x: Greeter = inject(TOKEN)`) is covered by treating the binding's declared name as a candidate interface directly. Crediting all implementers is the safe over-credit direction: a genuinely-dead method on a DIFFERENT class implementing the same interface is silently credited (a false negative, never a false positive), matching the existing `interface_to_implementers` propagation. Interface matching is by global name, so two unrelated same-named interfaces conflate (also over-credit). Provider wiring (`{ provide: TOKEN, useExisting: Class }`) is NOT parsed; the implements-based credit covers the repro and both the typed and untyped field forms. See issue #883.

## Resolution-level
- **Per-file size skip (issue #1086)**: source files strictly larger than `ResolvedConfig.max_file_size_bytes` (default 5 MB; `--max-file-size`/`FALLOW_MAX_FILE_SIZE`, `0` = unlimited) are dropped at discovery before parsing, so they contribute no imports/exports/findings. `.d.ts`/`.d.mts`/`.d.cts` are exempt (reachability roots for global types). A skipped file still exists on disk, so a relative import of it resolves to `ExternalFile` (NOT `unresolved-import`); but a skipped re-export BARREL stops propagating, which can surface its forwarded files as `unused-file`/`unused-export` (residual, mitigated by the high default + `.d.ts` exemption + loud reporting). See `walk.rs::partition_by_size`; details in `core-crate.md`.
- **Package.json `exports` subpath**: output dirs (dist/build/out/esm/cjs) mapped back to src/ with source extension fallback, including nested subdirs
- **Pnpm virtual store**: `.pnpm` paths mapped back to workspace source files. Handles injected deps, scoped/unscoped packages, peer dependency suffixes.
- **Package.json `imports` (`#subpath`)**: simple mappings, wildcard patterns, conditional exports. Per-package scoping.
- **Export / import condition names**: `oxc_resolver` is configured with the baseline condition set `["development", "import", "require", "default", "types", "node"]`. `"development"` ships unconditionally so packages that declare a `development` branch in `exports` / `imports` (common pattern: source files for dev, compiled output for prod; used by Vite, Vitest, esbuild, Rollup) resolve deterministically to source. When the React Native or Expo plugin is active, `"react-native"` and `"browser"` are prepended. Users extend the list via the `resolve.conditions` config key; those entries prepend ahead of everything else. See issue #135 and <https://nodejs.org/api/packages.html#community-conditions-definitions>.
- **React Native platform extensions**: `.web.ts`, `.ios.ts`, `.android.ts`, `.native.ts` resolved alongside standard extensions
- **Web-root-relative paths**: `/src/main.tsx` or `/static/style.css` resolved as a web convention (Vite/Parcel/Hono). Converts to `./src/main.tsx` and resolves from the source file's parent directory first, then falls back to project root. For real `.html` files only, a final `public/` fallback maps `/js/app.js` to `<root>/public/js/app.js`, matching Vite-style static public assets without broadening JS/TS root-relative imports. This handles monorepo workspace members where `site/index.html` referencing `/src/main.tsx` resolves to `site/src/main.tsx`, not `<root>/src/main.tsx`. Applied to HTML, JSX, TSX, JS/TS, and Glimmer source files (extensions `.html`, `.jsx`, `.tsx`, `.js`, `.ts`, `.mjs`, `.cjs`, `.mts`, `.cts`, `.gts`, `.gjs`) because HTML-like asset extraction can originate from HTML files or from `html` tagged templates in JS/TS-family files. Generic JSX resource attributes are intentionally ignored by default. See issues #105, #640, and #915.
- **CSS scoped package imports**: CSS `@import` with scoped npm packages (`@fontsource/monaspace-neon/400.css`, `@company/design-system/styles.css`) are kept as bare specifiers for node_modules resolution. Unscoped bare imports with CSS/SCSS extensions are normalized to relative paths (`app.css` becomes `./app.css`).
- **External Sass package entrypoints**: When a project stylesheet imports a package stylesheet through a package `sass` or `style` export, the external style scanner credits the owner package and recursively follows package-local relative `@use` / `@forward` / `@import` imports from that external file. The fallback is only active while scanning an existing stylesheet under `node_modules`, and uses Sass-style extension, underscore partial, and `_index` / `index` lookup, so imports like `@use "@angular/material" as mat;` do not cascade into `unresolved-import` or `unused-dependency` findings when Angular Material forwards package-local SCSS files. Missing project-local SCSS imports still report normally. See issue #871.
- **Tailwind CSS `@plugin` directives**: CSS/SCSS `@plugin "pkg"` targets are extracted as default imports so package dependencies are credited and relative plugin modules have their default export marked used. Unlike SCSS `@use`, extensionless package names such as `daisyui` stay bare even in `.scss`/`.sass` files; otherwise they would be misread as local partials.
- **Iconify static icon strings credit `@iconify-json/<prefix>` packages**: icon components and Nuxt UI surfaces consume icon sets through build-time string names rather than JavaScript imports, so the `@iconify-json/<prefix>` package supplying that collection can be reported as an unused dependency. The extraction layer scans markup file kinds (`.astro`, `.jsx`, `.tsx`, `.svelte`, `.vue`, `.html`/`.htm`, `.mdx`) for static `icon=` / `name=` props whose value is `<prefix>:<name>` (HTML comments stripped first; capture is the collection prefix only) and records the deduped prefixes on `ModuleInfo.iconify_prefixes`. It also scans Vue SFC source for static script-side object properties named `icon` whose value is a Nuxt UI class icon string such as `i-simple-icons-github`, recording the deduped class suffixes on `ModuleInfo.iconify_icon_names`. The analysis bridge (`crates/core/src/analyze/iconify.rs`) credits only declared `@iconify-json/*` packages: exact prefixes from markup must match a declared package, and Nuxt UI class suffixes use the longest declared collection prefix that leaves a non-empty icon segment. Crediting is GATED on the project declaring an Iconify-ecosystem dependency (`astro-icon`, `unplugin-icons`, `@iconify/*`, `@iconify-json/*`, `@iconify-icons/*`) across the root or any workspace `package.json`. Because the credit only exempts an already-declared package from the unused report and never creates a finding, a stray non-icon `name="foo:bar"` is harmless. Plain `.js`/`.ts` data objects, dynamic bindings (`:name=`, `name={expr}`), JSX brace-string form, and arbitrary strings not attached to an `icon` property remain out of scope. Bumps the extract `CACHE_VERSION` for #608 and #955 because Iconify extraction fields change serialized module state. See issues #608 and #955.
- **Tsconfig path aliases**: per-file discovery resolves `@/utils` by finding nearest tsconfig.json per file
- **Broken tsconfig chain fallback**: when `resolve_file` fails with a tsconfig-loading error (broken `extends` chain, circular extend, self-referencing project reference, malformed JSON, IO error), fall back to `resolve(dir, specifier)` which bypasses `TsconfigDiscovery::Auto`. A solution-style `tsconfig.json` with a broken sibling reference no longer poisons relative/bare-import resolution for files covered by a healthy sibling. A single `tracing::warn!` per unique error surfaces the broken chain to the user. Path aliases in the broken workspace remain unresolvable (they were already broken in `tsc`). See issue #97.
- **TanStack Router generated route tree imports**: active TanStack Router or Start projects suppress unresolved relative imports ending in `/routeTree.gen`, matching the generated `src/routeTree.gen.ts` artifact commonly imported as `./routeTree.gen` from app routers. The suppression is plugin-gated and does not hide ordinary missing relative imports. See issue #646.
- **TanStack Start virtual runtime modules**: active TanStack Router or Start projects treat `tanstack-start-manifest:` and `tanstack-start-injected-head-scripts:` specifiers as framework virtual module prefixes, covering runtime imports such as `tanstack-start-manifest:v` without hiding ordinary package names that lack the colon suffix. The suppression is plugin-gated and applies to both `unlisted-dependency` and `unresolved-import` reporting through `virtual_module_prefixes()`. See issue #636.
- **rspress `@theme` virtual module**: the `rspress` plugin activates on `rspress` or any `@rspress/`-scoped dependency (e.g. `@rspress/core`) and contributes `@theme/` and `@theme-original/` as `virtual_module_prefixes()`. rspress resolves the bare `@theme` import and `@theme/<component>` subpaths to the active theme layer at build time, so the suppression covers both `unlisted-dependency` and `unresolved-import` for those specifiers. The trailing-slash prefix also covers the bare `@theme` specifier via the exact-bare branch of `matches_virtual_prefix`, while real scoped packages such as `@theme-ui/core` are not swallowed. For Docusaurus projects the existing `@docusaurus` plugin already covers bare `@theme` the same way (no change needed there); the gap was that rspress had no plugin. Content roots (docs `.mdx` reachability) are out of scope. See issue #756.
- **React Router and Remix generated route type imports**: active React Router or Remix projects suppress unresolved type-only relative imports starting exactly with `./+types/`, matching generated route type modules such as `./+types/root`. The suppression is plugin-gated and type-only, so runtime imports under the same prefix still report as unresolved and projects without either plugin keep the normal missing-import finding. See issue #645.
- **SCSS partial resolution**: `@use 'variables'` and `@import '../styles/variables'` resolve to `_variables.scss` per SCSS convention. Extensionless bare specifiers in `.scss`/`.sass` files (excluding `sass:*` built-ins) are normalized to relative paths at extraction time. Resolution fallback tries: (1) `_filename` partial prefix, (2) `specifier/_index` directory index, (3) `specifier/index` plain index. Prevents false unresolved-imports, unlisted-dependencies, and unused-files from SCSS partial references.
- **SCSS include paths**: framework plugins contribute absolute directories to `PluginResult.scss_include_paths`. The Angular plugin reads `projects.*.architect.build.options.stylePreprocessorOptions.includePaths` from `angular.json`; the Nx plugin reads the same key from `project.json`. Nx workspace tokens (`{projectRoot}`, `{workspaceRoot}`) in include paths are expanded before resolution: `{projectRoot}` is computed from the config file's parent relative to the workspace root, `{workspaceRoot}` is stripped (paths are already resolved from workspace root). See issues #103, #114. Bare SCSS specifiers that fail the file-local `try_scss_partial_fallback` retry against each include dir, applying `<name>`, `_<name>`, `<name>/_index`, `<name>/index` candidates. Only triggers for `.scss`/`.sass` importers and normalized bare specifiers (those prefixed with `./` at extraction time) — explicit parent-relative paths like `../shared/vars` are left alone.
- **ng-packagr library entry file**: the Angular plugin reads `lib.entryFile` from `ng-package.json` / `ng-package.prod.json` and credits it as a package entry point, resolved relative to the directory containing the config (via `normalize_config_path`, so `packages/angular/ng-package.json` with `entryFile: "src/public-api.ts"` yields the `packages/angular/src/public-api.ts` entry in a monorepo, prefixed back by the registry). When `lib.entryFile` is omitted (or `lib` absent / the body is malformed), it falls back to ng-packagr's schema default `src/public_api.ts` (underscore, per `ng-package.schema.json`; modern hyphenated `public-api.ts` libraries set `entryFile` explicitly); a non-existent default simply matches nothing. Secondary entry points live in nested `ng-package.json` files in subdirectories that the non-recursive config discovery never reaches, so the primary config's `resolve_config` additionally scans the package subtree (bounded depth, skipping `node_modules` / `dist` / `out` / `tmp` / `coverage` / hidden dirs; same-directory siblings left to discovery) and resolves each nested config's entry file relative to its own directory. The entry file becomes a reachability root, so its re-export chain (`export { useHead } from './composables'`) keeps the public API surface alive instead of reporting it as unused files / unused exports, while files outside that graph stay flagged. The plugin also activates on a `ng-packagr` dependency (in addition to `@angular/core`); enabler detection reads `peerDependencies`, so an ng-packagr library that lists `@angular/core` only as a peer dep still activates. See issue #606.
- **SCSS node_modules resolution**: bare SCSS/Sass specifiers that fail both the local-partial and includePaths fallbacks are retried against `node_modules/` by walking up from the importing file to the filesystem root (matching Node.js / Sass semantics). `try_scss_node_modules_fallback` probes `<nm>/<pkg>/<path>.{scss,sass,css}`, `<nm>/<pkg>/<parent>/_<name>.{scss,sass}` (partial convention), and `<nm>/<pkg>/<path>/{_index,index}.{scss,sass}` (directory index). The CSS-extension append handles Sass's fallback for files like `animate.css/animate.min` → `animate.css/animate.min.css`. Because `node_modules/` files are outside fallow's file index (excluded by default ignore patterns), candidates are probed via `is_file()` on disk and a successful hit returns `ResolveResult::NpmPackage` (extracted via `extract_package_name_from_node_modules_path`), keeping `unused-dependencies` / `unlisted-dependencies` accurate. Scoped packages (`@scope/pkg/path`) are not prefixed with `./` by `normalize_css_import_path`, so they flow through `oxc_resolver`'s bare-module path directly and never reach this fallback. See issue #125.
- **SCSS importer JS/TS rejection**: when the importing file is `.scss` / `.sass` AND the standard resolver returns a path with a JS/TS-family extension (`.tsx`, `.ts`, `.mts`, `.cts`, `.gts`, `.js`, `.jsx`, `.mjs`, `.cjs`, `.gjs`), the result is rejected and resolution re-routes through the SCSS-aware fallback chain (CSS-extension probe, partial convention, include paths, node_modules). When those also fail, the import is reported as `Unresolvable` rather than falling through to the JS/TS extension list. Sass's resolution algorithm only ever considers `.css` / `.scss` / `.sass` files, so a sibling `.tsx` cannot legally satisfy a Sass `@use` / `@import` even though `oxc_resolver`'s extension list places `.tsx` before `.scss`. Without this guard, `@use 'Widget'` from a `.scss` file resolved to `Widget.tsx` whenever both files existed, creating phantom 3-file circular dependencies in standard CSS-modules / Angular `styleUrls` patterns where `.tsx` components import their own `.scss` and a sibling `.scss` shares variables/mixins via `@use`. See issue #245.
- **Entry point source fallback for ignored output dirs**: when `package.json` `main`/`module`/`exports` resolves inside an output directory (`dist`, `build`, `out`, `esm`, `cjs`) and the direct `src/` mirror in `try_output_to_source_path` does not exist, `resolve_entry_path` probes `src/index.*`, `src/main.*`, `index.*`, `main.*` in the package root. Prevents the entire `src/` tree from becoming unreachable in libraries whose compiled entry (e.g. `dist/esm2022/index.js`) has no one-to-one source mirror. Emits a single `tracing::info!` per fallback. See issue #102.
- **Workspace package self-reference & cross-workspace fallback**: when a bare specifier's package name matches the root package or a workspace package and normal resolution either fails or lands inside `node_modules` for that same package, `try_workspace_package_fallback` first honors matching `package.json` `exports` entries and maps project-relative output targets (`dist`, `build`, `out`, `esm`, `cjs`) back to tracked source files. For workspace manifests with no `exports` map, it also tries the stripped package subpath directly against the source layout before trusting legacy `main`, `module`, or `types` fields that may point at missing prebuild output. It then falls back to resolving the stripped subpath (`button`, `internal/base`, etc.) against a synthetic importer inside the package root, using `oxc_resolver`'s `resolve_file` so directory indices, source extensions, and the workspace's own `tsconfig.json` aliases still apply. Package `imports` (`#...`) use only the nearest owning package manifest, and unmatched hash aliases remain unresolved instead of becoming magical global aliases. Package `imports` / `exports` array targets preserve fallback order, and package `imports` targets that point at external packages credit the target dependency. Source-resolved package imports carry dependency usage metadata so static imports, dynamic literal imports, and re-exports keep `unused-dependencies` and `unlisted-dependencies` accurate; unresolved re-export sources also feed `unresolved-import` diagnostics. Handles Node.js v12+ self-referencing (`import '@org/pkg/sub'` from inside `@org/pkg`, common in Angular libraries built with `ng-packagr`), prebuild workspace `exports`, no-exports workspace packages with prebuild legacy fields, and Nitro-style package `imports`. The fallback is also reached when a tsconfig `paths` entry maps a sibling-package specifier at unbuilt output (`"@scope/*": ["../*/dist/index.d.ts"]`, a common pre-build monorepo convention): the TypeScript plugin registers `@scope/` as a path alias, so the import matches `matches_plugin_alias` and `try_path_alias_fallback` runs first, but its dist target does not exist pre-build, so `resolve_specifier` falls through to `try_workspace_package_fallback` for a valid bare package specifier before reporting `unresolved-import`. Without that fall-through every consumer of such a package reported `unresolved-import` plus a false `unused-dependency`. See issues #106, #641, #725, #757, and PR #666.
- **Segment-aware plugin-alias gate**: the `matches_plugin_alias` check in `resolve_specifier` (which routes a specifier into the path-alias branch) now matches a registered alias prefix at a path-segment boundary via `specifier_matches_alias_prefix`, instead of a raw `starts_with(prefix)`. This mirrors `alias_match_remainder` in `fallbacks.rs`: a bare prefix (`@`, `~`) matches only on an exact hit or a `/`-delimited continuation, while a prefix that already ends in `/` matches any continuation. A config registering a bare `"@"` alias (e.g. `"@" -> "src"`) no longer captures every scoped dependency (`@radix-ui/react-checkbox`); those resolve as normal npm packages, while `@/foo` still enters the alias branch. Without the fix, a bare `"@"` key routed every `@scope/*` import into the alias fallback, which failed and cascaded into false `unresolved-import` + `unused-dependency`. See issue #838.
- **Deno import schemes (`jsr:`, `npm:`, URL)**: `resolve_specifier` treats `jsr:<spec>` and URL specifiers (`https://`, `http://`, `data:`) as external files, so Deno / Supabase Edge Function imports are never reported as `unresolved-import` nor misclassified as npm dependencies (previously `jsr:@std/path` collapsed to a bogus `jsr:@std` unlisted dependency). `npm:<pkg>[@version][/subpath]` is normalized by `normalize_npm_specifier` (scheme plus `@version` selector stripped, scope and subpath preserved: `npm:@supabase/supabase-js@2` becomes `@supabase/supabase-js`, `npm:preact@10/hooks` becomes `preact/hooks`) and resolved as an ordinary bare specifier, so a declared dependency is credited; a bare `npm:` with an empty body is treated as external. A package whose every import in a file used the `npm:` scheme is self-declaring (the Supabase / Deno convention of inlining the dependency in the specifier) and is not reported as `unlisted-dependency`, mirroring the per-file `bun:` carve-out (`package_imports_are_all_npm_scheme`); a package also imported via a bare specifier in any file stays reportable. These schemes never appear in Node projects, so non-Deno resolution is unchanged. See issue #624.
- **Playwright `webServer.command` CLI dependencies and script entries**: the Playwright plugin's `resolve_config` reads `webServer.command` from `playwright.config.{ts,js}` in both the object form (`webServer: { command, cwd }`) and the array form (`webServer: [{ command, cwd }, ...]`, via the command-pair config parser helper; the two forms are mutually exclusive at the AST level so no double-count). Each command is parsed by the shared `scripts::analyze_command`, the same primitive that analyzes package.json scripts, so invoked npm binaries (`srvx --port 3000` credits `srvx`; `pnpm exec srvx --port ${PORT}` in a template literal credits `srvx`; node runners like `tsx` / `ts-node` are credited as packages, `node` / `bun` excluded) flow into `referenced_dependencies`, and local file arguments (`tsx scripts/e2e-server.ts` seeds `scripts/e2e-server.ts`) are added to `setup_files` so the launched script stays reachable. Template literals are accepted only when their static command text is recoverable without splitting a static shell token; dynamic commands that start with interpolation and function-call commands are skipped. File arguments resolve relative to the config file's directory (matching Playwright's `webServer.cwd` default and the same base used for `globalSetup` / `globalTeardown`), so a nested `apps/web/playwright.config.ts` discovered from the project root resolves its paths under `apps/web`, not the repo root. A `webServer.cwd` (per object or per array element) overrides that base (resolved relative to the config directory; an absolute cwd replaces it). Binary-to-package resolution still uses the project root since it owns `node_modules`. Commands that delegate to a package manager (`npm run start`, `yarn dev`) credit nothing, since the underlying script's own dependencies are analyzed separately. Removes the need for manual `ignoreDependencies` entries for CLI-only packages invoked by the e2e dev server. Existing `globalSetup` / `globalTeardown` handling is unchanged. See issues #621 and #956.

## Graph-level
- **Type-only cycle filtering**: `import type` edges carry `is_type_only` through `ImportedSymbol` to cycle detection. Edges where all symbols are type-only are excluded from Tarjan's SCC successor list, preventing false circular dependency reports from type-only bidirectional imports.
- **`export *` chain propagation**: multi-level barrel file chains fully resolved for transitive usage tracking. The fixpoint loop in `crates/graph/src/graph/re_exports/mod.rs::resolve_re_export_chains` terminates naturally via the monotone dedup-by-`from_file` check in `propagate.rs`; a defensive safety backstop bounded by `re_export_info.len() + 1` iterations guards against future regressions. Chain depth is unbounded in practice (the old `max_iterations = 20` cap silently truncated barrels with deeper chains; removed for #442). Re-export cycles are detected up front via iterative Tarjan SCC over the (barrel, source) edge subgraph; cycles surface BOTH as one `tracing::warn!` per cycle naming the member paths (RUST_LOG=warn discoverability, unchanged from #442) AND as a user-visible `re-export-cycle` finding on `AnalysisResults.re_export_cycles` (#515). Self-re-exports (a barrel re-exporting from itself, often a rename leftover) carry `kind: "self-loop"`; multi-node SCCs carry `kind: "multi-node"`. The finding is file-scoped (no per-edge line/col anchor); suppression via `// fallow-ignore-file re-export-cycle` (or aliases `re-export-cycles` / `reexport-cycle` / `reexport-cycles`) on any member file drops the cycle from results, and per-file `overrides.rules.re-export-cycle` is a no-op because the cycle spans multiple files (resolver emits a load-time warn pointing at the working escape hatch). Synthetic bridge stubs created during multi-hop star propagation inherit `is_type_only` from the triggering re-export edge so multi-hop `export type *` chains tag the stub correctly; when both a type-only and a value star edge reach the same source for the same name, a `synthetic_stubs` side set tracks the stub identity so the value edge can downgrade `is_type_only: false`. Real `export type Foo` declarations on the source are never in the side set and stay type-only. Type-only re-export cycles (`export type * from './b'` paired symmetrically) still surface as findings, because chain propagation through the loop is the same no-op as for value cycles. See `decisions/005-re-export-chain-resolution.md`.
- **`export * as Foo from './bar'` namespace re-export propagation**: a barrel that namespace-re-exports a module records a `ReExportEdge { imported_name: "*", exported_name: "Foo" }` plus a synthesised stub `ExportSymbol` named `Foo` on the barrel. The Phase 2 narrowing path only handles `import * as ns` (direct namespace imports), and Phase 4's named-re-export propagation looks for a source export literally named `"*"` (never matches), so a consumer using the named import form (`import { Foo } from './barrel'; Foo.X`) would otherwise leave every export of `./bar` unreferenced. Phase 2c (`graph::namespace_re_exports::propagate_namespace_re_exports`) closes the gap: it iterates re-export edges with `imported_name == "*" && exported_name != "*"`, enumerates the reachable `(barrel_file, exported_name_at_barrel)` pairs via a forward BFS over named re-export edges (so multi-hop chains like `outer_barrel.Foo -> inner_barrel.Foo -> source` match), and for every consumer that imports a reachable name records each `<consumer_local>.<member>` access. Credits are applied with `mark_member_exports_referenced` plus `create_synthetic_exports_for_star_re_exports` on the namespace target file, mirroring `narrow_namespace_references`'s two-step shape so star barrels on the target (`./source/index.ts` doing `export * from './impl'`) get a synthetic stub the Phase 4 chain resolver carries through. Whole-object uses (`Object.values(Foo)`, spread, destructure-with-rest) credit every target export; entry-point barrels exposing the namespace externally also credit every target export, mirroring `propagate_entry_point_star`. Consumer bindings reported as unused (`unused_import_bindings`) are skipped. The walker intentionally does NOT follow `export * as ns from` edges forward at non-seed barrels because the original identifier is hidden behind `ns.<name>` rather than exposed directly. See issue #324.
- **CSS Modules**: default imports (`import styles from '...'`) resolve `styles.className` to named exports via graph-level narrowing. Spread/`Object.values` handled conservatively.
- **Convention auto-import resolution (Nuxt components, scripts, and Pinia stores)**: frameworks that expose components or script bindings by filesystem convention (`<Card001 />` resolving to `components/Card001.vue`, or `useUserStore()` resolving to `stores/user.ts`, with no `import`) are modeled by a first-class `Plugin::auto_imports(&self, root) -> Vec<AutoImportRule>` trait method. The architecture splits across two layers so the per-file extraction cache stays correct: (1) **capture** at extraction. The Vue SFC template scanner (`crates/extract/src/sfc_template/shared.rs::mark_binding_used`) records every PascalCase tag that matches no import and no local binding into `ModuleInfo.auto_import_candidates` (content-local, so it round-trips through the bitcode cache; native lowercase HTML elements and locals are skipped, kebab tags are normalized to their Pascal form). JS/TS and SFC script parsing also records unresolved value-reference identifiers such as `useCounter()`, `formatPrice()`, and `useUserStore()`, including files with zero explicit imports; locally declared, explicitly imported, and type-only names are not captured. (2) **resolve** at graph-build, never cached: `crates/graph/src/resolve/mod.rs::synthesize_auto_import_edges` runs after `apply_specifier_upgrades`, builds a `name -> FileId` table from the active plugins' `auto_imports` rules (mapped via the live file index), skips known JS/Web/Vue/Nuxt built-ins, and pushes a synthetic `ResolvedImport` for each captured name that matches. Name collisions over-credit every match (so `Comments.client.vue` + `Comments.server.vue`, both `<Comments>`, same-name composables in two convention dirs, or same-name stores in two supported dirs, stay reachable). The Nuxt plugin (`crates/core/src/plugins/nuxt.rs`) scans `components/` and `app/components/`, deriving the directory-prefixed PascalCase name with Nuxt's prefix-overlap dedup (`base/foo/Button.vue` -> `BaseFooButton`, `foo/Foo.vue` -> `Foo`, `base/BaseButton.vue` -> `BaseButton`), pre-expanding the implicit `Lazy`-prefixed variant, and stripping `.client` / `.server` / `.global` suffixes so paired files share one name. It scans top-level `composables/`, `app/composables/`, `utils/`, and `app/utils/`, plus recursive `shared/utils/` and `shared/types/`, emitting named-export rules and default-export rules keyed by the derived basename. The Pinia plugin (`crates/core/src/plugins/pinia.rs`) activates on `@pinia/nuxt` and scans direct files in `stores/` and `app/stores/`, emitting named-export rules only for value exports that match `use<Name>Store`; nested stores and custom `storesDirs` remain out of scope. Synthesis is unconditional and additive. The opt-in `autoImports: true` config field gates ONLY Nuxt entry-pattern removal: component fallbacks are dropped unless a root/workspace `nuxt.config` declares `components:`, and composable/util fallbacks are dropped unless it declares `imports:`. Pinia adds no entry-pattern fallback and does not require an extract `CACHE_VERSION` bump because it reuses existing script-level candidate capture. See issues #704, #739, and #740.
- **Nuxt `@nuxt/content` config entry**: `@nuxt/content` v3 reads a root `content.config.ts` (`export default defineContentConfig({...})`) at build time; nothing in app source imports it, so it surfaced as `unused-file` plus `unused-export`. When `@nuxt/content` appears in the nuxt.config `modules:` array, `crates/core/src/plugins/nuxt.rs::resolve_config` credits `content.config.{ts,js,mts,mjs,cts,cjs}` as a default-export entry (entry pattern + `default` used-export rule), resolved relative to the nuxt.config directory via `normalize_config_path` so nested/monorepo configs credit the sibling `content.config` rather than a root one. Gated on module registration, not mere package.json presence: an installed-but-unregistered `@nuxt/content` leaves a genuinely-orphan `content.config` correctly flagged. Scoped to the first-party `@nuxt/content` only; third-party Nuxt module config files are deliberately not hardcoded (declined in #138). No `CACHE_VERSION` bump (`resolve_config` is analysis-time, not extraction). See issue #792.

## Analysis-level
- **CSS-in-JS dead-binding crediting (Phase 3a, characterization, NO special-case code)**: a `styled` / `css` / `keyframes` / `createGlobalStyle` binding (styled-components, `@emotion/styled`, `@emotion/react`, `@linaria/core` / `@linaria/react`) and a vanilla-extract `.css.ts` `style({...})` export are ALL ordinary JS value exports, so `unused-export` already reports a dead one (`export const DeadCard = styled.div\`...\``, imported nowhere) and credits a live one with ZERO bespoke handling, and the CSS-in-JS libraries are imported as value bindings (`import styled from "styled-components"`, `import { css } from "@emotion/react"`, `import { style } from "@vanilla-extract/css"`), so normal package-usage crediting marks them used and they never surface as `unused-dependency`. There is NO dep gate, NO tooling credit, and NO new `IssueKind` for this case: characterization confirmed the existing dead-code path covers it end to end, so Phase 3a adds only a fixture (`tests/fixtures/css-in-js-styled`) and a no-regression integration test (`crates/core/tests/integration_test/css_in_js_styled.rs`) pinning that a dead styled binding stays reported and a CSS-in-JS library stays credited. This is the static dead-BINDING half of the CSS-in-JS story; lifting the CSS STRING out of the tagged template into the styling-health analytics pipeline is the separate Phase 3b work. See `.plans/css-in-js-first-class.md` (3a).
- **Same-file exported value dependencies**: unused-export analysis credits a same-file value export when it is referenced from the initializer of another reachable value export. This covers exported schema composition patterns such as `export const Parent = Schema.Struct({ child: Schema.Array(Child) })` plus `export const Child = Schema.Struct(...)`, including the common Effect Schema same-name value/type alias pattern (`export type Child = Schema.Schema.Type<typeof Child>`). The credit is transitive across exported value initializers, applies only to value exports, and does not credit siblings reachable only through an otherwise-unused parent, so unrelated dead schema exports still report. See issue #1304.
- **Unused Pinia store members (`unused-store-member`)**: a Pinia store member (an option-store `state` / `getters` / `actions` key, or a setup-store returned key) declared but accessed by ZERO consumers project-wide. Cross-graph dead-INPUT direction: the store binding is imported (so the module is reachable) yet a specific member is dead. This is the direction no linter or type-checker covers (eslint-plugin-vue/svelte/pinia are single-file; vue-tsc/svelte-check are caller-side excess-prop). Reuses the `unused_members.rs` cross-file member machinery with a new `MemberKind::StoreMember`. EXTRACTION (`crates/extract/src/visitor/visit_impl.rs`): `record_pinia_store` harvests members from `defineStore('id', {...})` option stores (keys of the `state` arrow's returned object literal + `getters` keys + `actions` keys) and `defineStore('id', () => ({...}))` setup stores (the last return statement's object-literal keys); a setup-store SPREAD return (`return { ...base, x }`) abstains entirely (no members harvested); Pinia's `$`-prefixed API (`$patch` / `$reset` / `$subscribe` / `$state` / `$id` / `$dispose` / `$onAction`) is excluded from the declared set. Members ride `ExportInfo.members` via the `enrich_store_exports` side-map + finalizer (run in BOTH `into_module_info` and `merge_into` for SFC `<script>` parity), so no new `ModuleInfo` field; `CACHE_VERSION` bumped. CONSUMPTION credit: `const s = useStore()` instance bindings (`binding_target_names[s] = useStore`, gated on `is_store_factory_call` = imported OR `use*Store`-named so Nuxt auto-imports work), `const { m } = useStore()` / `const { m } = storeToRefs(store)` / `toRefs` destructures, a param typed `ReturnType<typeof useStore>` (inline or via a local `type` alias; gated on the `use<Name>Store` convention so a non-store `ReturnType<typeof ...>` never masks a real unused member) bound to the store factory through `binding_target_names` so `store.member` / `props.store.member` / `const { m } = props.store` credit (issue #1489 Case 2), template `store.member` (existing SFC scanner), and intra-store `this.member` self-access for option stores (the `file_self_accesses` credit was extended from class-member-only to also fire for `StoreMember`, so a getter consumed only by a sibling getter is not falsely flagged). `mapState` / `mapGetters` / `mapActions` / `mapStores` mark every identifier store-factory argument as a WHOLE-OBJECT use (conservative abstain; v1 does not parse the string-array/object member lists). Crediting a member access against an import is INERT unless the export actually has that member, so the consumption signals cause ZERO drift on non-store code (verified byte-identical on all 8 benchmark fixtures). ACTIVATION GATE (`run_member_detectors`): emitted only when `pinia` or `@pinia/nuxt` is in `declared_deps`, so an unrelated user `defineStore`-named helper never fires. Default severity `warn` (not `error` like the closed-set class/enum member rules: a store has an OPEN declaration surface so confidence is lower). Suppress-only, no autofix (a store member can be accessed reflectively). Svelte stores and Vuex are deferred (Svelte `writable` is opaque and its value-shape is reshaped by `.set()`/`.update()`, so member-level detection would be FP-prone; the store-export level is already covered by `unused-export`).
- **Unprovided injects (`unprovided-inject`)**: a Vue `inject(KEY)` or Svelte `getContext(KEY)` whose symbol KEY is `provide`/`setContext`'d NOWHERE in the analyzed project (the injected-never-provided dead-half). The key is a symbol with cross-file identity (an imported const or a module-local symbol); at runtime a dead inject returns `undefined`, surfaced only at render. No static tool in the Vue/Svelte/Nuxt ecosystems catches this (Vue/Nuxt emit a runtime-only warning, Svelte's eslint proposal is unimplemented). EXTRACTION (`crates/extract/src/visitor/visit_impl.rs::record_di_key_site`): records identifier-keyed `provide(IDENT, _)` / `inject(IDENT[, default])` gated on a named import from `vue` / `@vue/runtime-core`, `setContext(IDENT, _)` / `getContext(IDENT)` gated on `svelte`, plus app-level `*.provide(IDENT, _)` 2-arg member calls (FN-preferring, no provenance gate, so `app.provide(KEY)` in `main.ts` is captured) onto `ModuleInfo.di_key_sites: Vec<DiKeySite>`. A provide keyed by an UNKNOWABLE key (a non-identifier, a spread, a computed expr, or a transient nested-scope local such as a `forEach` loop variable) sets `ModuleInfo.has_dynamic_provide`; a module-scope `const NAME = "literal"` is tracked and its DI sites are dropped at finalize (`finalize_di_key_sites`), because a string-literal-bound const has STRING identity, not symbol identity (a provider supplying the literal, often inside a package, matches it). `CACHE_VERSION` bumped (155). The SFC `<script>` merge path (`merge_into`) records both halves so Svelte components are covered. DETECTOR (`crates/core/src/analyze/unprovided_inject.rs`): a two-pass set-difference. Pass 1 builds the provided-key set LIBERALLY (resolving each provide key through `build_local_to_export_keys` + `export_key_with_origins` so a provide imported directly and an inject imported through a barrel canonicalize to the same defining-site `ExportKey`). Pass 2 emits a finding for each inject whose canonical key set is disjoint from the provided set. ABSTAIN ladder (zero-FP): dep-gated on `vue` / `@vue/runtime-core` / `svelte` in `declared_deps`; a key imported from an npm PACKAGE (external resolved target) abstains (the provide may live in that package's `node_modules` code); a key that is public API of THIS package (re-exported from, defined in, or `export *`-reachable from a non-private package entry point) abstains (a "bring-your-own-provider" library exports the key for a downstream consumer to provide); and if ANY reachable module has `has_dynamic_provide`, ALL inject findings abstain project-wide. Wired as `populate_unprovided_inject_findings` (mirrors the mixed-client-server-barrel populate site). Default severity `warn`, suppress-only (`// fallow-ignore-next-line unprovided-inject`), no autofix (the fix, provide the key or remove the inject, is a human decision). Nuxt `nuxtApp.provide`/`$x` (string-keyed, dynamic) and the provided-never-injected direction are deferred. Validated zero-FP on real Vue/Svelte projects (jsonforms-adapter-vue3, components-vue3, vue-core, svelte); the jsonforms `const KEY = "jsonforms"; inject(KEY)` string-const case was the real-world FP that drove the string-const abstain. ANGULAR ARM (W4.1, `DiFramework::Angular`, same rule/kind/suppress token, gated on `@angular/core`): flags a USER `InjectionToken` injected via `inject(TOKEN)` (named import from `@angular/core`) or an `@Inject(TOKEN)` constructor-param decorator whose token is supplied by no provider project-wide. EXTRACTION (`record_di_key_site` + `record_angular_param_inject` for `@Inject`, `record_angular_provider_object` on `visit_object_expression`, `record_angular_dynamic_providers` on calls, and `record_injection_token`): an `inject` Inject site is dropped on `inject(TOKEN, { optional: true })`; a `@Inject(TOKEN)` param site is dropped on a sibling `@Optional()`; a `{ provide: TOKEN, useClass|useValue|useFactory|useExisting: ... }` recipe object records a Provide keyed on the `provide:` identifier (the recipe-sibling gate is REQUIRED so Vue's Options-API `provide: {...}` / `provide()` option does not trip the shared `has_dynamic_provide`); a `new InjectionToken(..., { factory } | { providedIn })` records a self-Provide of its own const (tree-shakable token); and `importProvidersFrom(...)` / `makeEnvironmentProviders(...)` / a `...spread` in a `providers:` array / a non-identifier `provide:` value set the project-wide `has_dynamic_provide` abstain. `record_injection_token` now records EVERY `new InjectionToken(...)` into `injection_tokens` (interface name from a type-REFERENCE type arg, empty string for a primitive `<string>` / untyped token; the #920 template bridge harmlessly skips empty-interface entries), `CACHE_VERSION` 175 to 176. DETECTOR: the gate adds `@angular/core`; the load-bearing FP gate `is_known_injection_token` requires an Angular inject site's canonical token to be a known `InjectionToken` (its defining module lists the name in `injection_tokens`), so a class token (`inject(MyService)`, FP-prone via `providedIn:'root'` + third-party `provideX()`) abstains. Validated zero-FP on real Angular apps. Tokens of any type-argument shape are covered (interface-typed, primitive-typed, untyped, all record an `injection_tokens` entry); deferred shapes are bare-string injects (string identity, not symbol) and the provided-never-injected direction.
- **Unrendered components (`unrendered-component`)**: a Vue/Svelte single-file component (the default export of a `.vue` / `.svelte` file) that is reachable in the module graph but instantiated at NO render site project-wide. The component is kept alive by a barrel re-export, so `unused-file` (reachable) and `unused-export` (the re-export is a use) both miss it; eslint `vue/no-unused-components` is single-file and cannot see a barrel-reexported component rendered by a sibling. This reuses the graph rather than adding heavy extraction: the only new persisted state is `ModuleInfo.referenced_import_bindings` (the per-file complement of `unused_import_bindings`, DERIVED in `release_resolution_payload` from `imports` minus `unused_import_bindings` so it survives the release and is never cached). DETECTOR (`crates/core/src/analyze/unrendered_component.rs`): Pass 1 builds the "rendered/used" SFC set LIBERALLY: for each file, every binding in `referenced_import_bindings` plus every synthetic Nuxt auto-import (`<auto-import:Name>` source) plus every dynamic import is resolved through `credit_rendered_sfc_chain` (a re-export-chain walk that credits every `.vue`/`.svelte` source file in the chain, because SFCs have no default `ExportSymbol` for `walk_re_export_origins` to terminate on). Pass 2 collects SFC files re-exported by a REACHABLE barrel (the eligibility gate: a component reachable only through a dead direct import is left to `unused-import`, one reachable through nothing to `unused-file`). Pass 3 emits a reachable, non-entry-point, barrel-re-exported SFC whose default export is in neither the used set nor the public-API set. ABSTAIN ladder (zero-FP): dep-gated on `vue` / `@vue/runtime-core` / `nuxt` (`.vue`) or `svelte` / `@sveltejs/kit` (`.svelte`); entry-point components (route pages, layouts, `App.vue`, Nuxt `app.vue` / `error.vue`) abstain via the discover entry-point set; and `public_api_reexported_sfcs` walks the FULL re-export chain (any imported name incl. `*`, multi-hop) from every non-private package entry point and abstains every SFC reachable that way (a component library re-exporting its components through `entry -> export * -> sub-barrel -> export {default} from './X.vue'` is NOT flagged). Over-crediting in Pass 1 / the public-API walk only suppresses a finding, never creates one. Suppression anchors at the file head (line 1): both a line-1 inline ignore and a file-level ignore are honored. Default severity `warn`, suppress-only (`// fallow-ignore-next-line unrendered-component`), no autofix (a component can be rendered reflectively via a dynamic `<component :is>` from a non-literal value). Validated on a 10-project real-world corpus: 11 true positives (3 orphaned icons in huntabyte/bits-ui, 8 unused shadcn context-menu variants in vue-vben-admin) with zero false positives after the deep public-API-chain abstain; the multi-hop public-API gap was the FP class that drove the full-chain walk. Depends on the Vue `<template>`-scanner nesting fix (PR #1247): before it, components rendered after a nested `<template #slot>` were dropped from `referenced_import_bindings` and false-flagged. `credit_rendered_sfc_chain` ALSO follows namespace re-export edges (`export * as List from './components/List'`, encoded as `imported_name == "*"`, `exported_name == "List"`): a design system that exposes compound components as namespaces and renders members via dotted tags (`import { List } from '@/design-system'; <List.Root />`) credits every SFC the namespace target re-exports via `credit_all_reexported_sfcs` (name-agnostic, since the rendered member is syntactically unknowable through a namespace; liberal, mirroring the direct `import * as ns` handling). Before the fix the chain walk skipped namespace re-export edges in both its named-match and star loops, so every namespace-exposed member was false-flagged as rendered-nowhere; a namespace barrel that is NEVER consumed stays flagged. The same `credit_all_reexported_sfcs` also backs the whole-namespace-import arm (`import * as DS from "@/design-system"`; `<DS.List.Root>`): `credit_static_import`'s `ImportedName::Namespace` branch credits every SFC reachable from the imported module (was a per-edge `credit_rendered_sfc_chain(source, imported_name)` walk that re-walked each edge under the unmatched name `"*"` and so dropped any nested `export * as` / `export *` barrel; only a direct leaf named re-export was credited). A component reachable but OUTSIDE the imported namespace (e.g. behind a side-effect-imported sibling barrel) still flags. Note the public-API abstain (`add_exportless_package_source_indexes`) treats every `src/**/index.*` barrel as a public-API entry point when the root/workspace package has no `exports` map and is not `private`, which over-abstains namespace-barrel SFCs under `src/` (zero-FP direction); the bug surfaces on a `private` app package or a non-`src/` design-system. See issue #1351. ANGULAR ARM (W4.2, `framework: "angular"`, same rule, no new IssueKind): `find_unrendered_angular_components` flags an `@Component` whose ELEMENT selector (first cut: element selectors only; attribute/class selectors and `@Directive` abstained) is in no template's used-tag set project-wide, gated on `@angular/core`. Extraction harvests `@Component` selectors, used element-selector tags from inline + external templates, route `component:` / `.then(m => m.X)` `loadComponent` class refs, and `bootstrap`/`bootstrapApplication` refs onto `ModuleInfo`; a project-wide `has_dynamic_component_render` flag (`ViewContainerRef.createComponent` / `*ngComponentOutlet`) abstains every component. The FP-critical abstain for the dominant modern lazy-route form, the bare `loadComponent: () => import('./x.component')` / `loadChildren: () => import('./x.routes')` (which carry no class name in the route config and load a module's `export default class`), is handled NOT by name harvest but by abstaining a candidate whose module DEFAULT export is referenced (fallow's route-callback dynamic-import detection already credits the lazy target's default export), so a genuinely-orphan NAMED-export component stays flagged. Validated zero-FP on angular-realworld (7 lazy-route false positives -> 0). Selectors and used-tags ride new `ModuleInfo` fields (CACHE_VERSION 175); the default-export-referenced abstain reads existing graph refs (no bump for the FP fix).
  - Issue #1282 follow-up: `storeToRefs(useStore())` and `toRefs(useStore())` destructures are credited the same way as `storeToRefs(store)` and `toRefs(store)`, but only when the inline argument is a bare store-factory Identifier accepted by `is_store_factory_call`.

- **Astro framework health** (`.astro`): the frontmatter now runs the same `oxc_semantic` unused-binding pass as JS/TS (`parse_astro_to_module` in `crates/extract/src/astro.rs`), crediting names used in the markup via `collect_astro_template_used_names` (component tag roots `<Header>` / `<ui.Card>` and identifiers inside `{ ... }` expression regions, NOT raw prose text; `<script>` / `<style>` / comments masked). A frontmatter import used in NEITHER the script NOR a tag/expression position is now genuinely unused (refines `unused-export`), and `.astro` components join the `unrendered-component` rule as `SfcFramework::Astro` (gated on `astro`, reusing the Vue/Svelte barrel-reexported + public-API abstain machinery via `is_sfc_extension`). The `unused-component-prop` rule gains an Astro arm: `harvest_astro_props` (`sfc_props.rs`) harvests the frontmatter `interface Props { ... }` (or `type Props = { ... }`) and credits usage via `Astro.props` destructure, `Astro.props.<name>` member access, a whole-object `const props = Astro.props` binding's member reads, and template `{prop}` usage. Whole-file abstains: `interface Props extends X` / `type Props = <ref>` (`has_unharvestable_props`), a rest element in the destructure, a whole-object `Astro.props` use, or a `{...Astro.props}` template spread (`has_props_attrs_fallthrough`). `fallow health` also scores `.astro` complexity: frontmatter functions like a Vue/Svelte `<script>`, plus a synthetic `<template>` entry (`template_complexity/astro.rs`) for markup `{ ... }` expression complexity and `.map` / `.flatMap` / `.forEach` iteration (the loop analog of `v-for` / `{#each}`). No new IssueKinds; reuses `unrendered-component` + `unused-component-prop`. Props consumed via `<style define:vars={{ x }}>` / `<script define:vars>` and via a nested destructure (`const { post: { id } } = Astro.props`) are credited; an array destructure of `Astro.props` abstains. `CACHE_VERSION` bumped 182 to 184 (the FP-fix follow-up also populates standalone `.html` custom-element tags and widens imperative-render capture).
- **Lit / web-component framework health**: building on the Lit registration credit above, the registered tag is now RETAINED. `@customElement('x-foo')` (via `lit_custom_element_tag`, through the existing provenance-validated candidate flow) and `customElements.define('x-foo', C)` record a `RegisteredCustomElement { tag, class_local_name, span_start }` on `ModuleInfo.registered_custom_elements`; `html` tagged templates are scanned for custom-element tag usage (`html::collect_custom_element_tags`, hyphen-gated so native tags never match) into `ModuleInfo.used_custom_element_tags`. The `unrendered-component` rule gains a Lit arm (`find_unrendered_lit_elements`, `framework: "lit"`, gated on `lit` / `lit-element` / `@lit/reactive-element`): a registered element whose tag is in NO `html` template's rendered-tag union project-wide is flagged, anchored at the class span and named by the TAG (`custom element <x-foo>`). Zero-FP ladder (panel-binding): a PUBLISHED element (re-exported from a non-private package entry, or under a non-private exportless `src/**/index.*` surface) is wholesale-abstained via the existing public-API sets, so only a PRIVATE-app internal element is eligible; a project-wide dynamic render (`` html`<${tag}>` ``, recorded as the `DYNAMIC_CUSTOM_ELEMENT_TAG` sentinel) abstains every element; imperative renders (`*.createElement` (any receiver) / `customElements.get` / `whenDefined`) and a tag rendered in a standalone `.html` document credit the tag as rendered; a registered element defined under a docs / dev / demo directory is abstained (rendered by site/dev tooling that cannot be parsed). Separately, a Lit `@state()` reactive property on a direct `LitElement` / `ReactiveElement` subclass is made CHECKABLE in `find_unused_members` (a `lit_active` signal un-skips the otherwise-decorator-skipped member), so a `@state` read NOWHERE via `this.<name>` (method or `html` template) surfaces as `unused-class-member`; `@property` stays skipped (the public attribute API, settable via HTML attribute / parent binding / `setAttribute` / CSS). Reuses `unrendered-component` + `unused-class-member`; no new IssueKinds. The new `ModuleInfo` fields ride the cache (`CACHE_VERSION` 184, sizes 1256 to 1304).
- **Unused component props (`unused-component-prop`)**: a Vue `<script setup>` `defineProps` declared prop referenced by NO code in its own SFC (neither `<script>` nor `<template>`). Single-file dead-INPUT direction; vue-tsc/Volar check caller-side prop correctness, not in-component non-use; eslint `vue/no-unused-properties` is off-by-default and `<script setup>`-flaky. EXTRACTION (`crates/extract/src/sfc_props.rs::harvest_define_props`, wired in `sfc.rs::merge_script_into_module`, gated on `is_setup`): harvests prop names from the inline TS literal (`defineProps<{foo:T}>()` `TSTypeLiteral` members), runtime object (`defineProps({foo:{...}})` keys), array form, and `withDefaults(...)` unwrap; a type-REFERENCE arg (`defineProps<ImportedProps>()`) sets `has_unharvestable_props` (abstain, names unresolvable). Per-prop `used_in_script` (oxc_semantic resolved-references for the destructured local, incl. `AssignmentPattern` defaults; plus `props.<name>` member access where the binding is the `defineProps` return) and `used_in_template` (prop names threaded into the SFC template scanner as a credited set alongside `template_visible_imports`, plus `props.<name>` / `$props.<name>` template member accesses) ride a new `ModuleInfo.component_props: Vec<ComponentProp>`; `CACHE_VERSION` bumped (158). DETECTOR (`crates/core/src/analyze/unused_component_prop.rs`): dep-gated on `vue`/`@vue/runtime-core`/`nuxt`, reachable `.vue` only, emits a prop with `!used_in_script && !used_in_template`. ABSTAIN ladder (whole-file, zero-FP): not `<script setup>`; `has_unharvestable_props`; `v-bind="$attrs"`/`"$props"`/`"props"` fallthrough; whole-object props use (`toRefs(props)`, `{...props}`, `fn(props)`); `defineExpose(...)`; `defineModel(...)`. Default severity `warn`, suppress-only (`// fallow-ignore-next-line unused-component-prop`), no autofix. Validated on 6 real Vue projects: 7 true positives (elk `alt`/`noOverflowHidden`/`inNotification`, vben `border`, vuestic `options`, nuxt `speed`/`src`), zero outright false positives after four FP-tightening rounds (the `props.class` template member access, whole-object use, destructure-with-default local, `$props.<name>`, and `defineModel`-assigned-const cases each closed a real gap). Vue `<script setup>` plus the Vue Options API (W2.3): `harvest_options_api_props` (same file) harvests `props:` array (`['a']`) and object (`{a:{...}}`) forms from `export default {...}` / `defineComponent({...})` in a non-setup `<script>`, with `this.<prop>` reads + template refs as the usage credit, and whole-component abstain on `mixins:` / `extends:` (a mixin/base may read the prop), `this[<computed>]`, an identifier/spread/`defineComponent<Type>()` props value (`has_unharvestable_props`), and a `setup(props)` method (whole-object credit); `CACHE_VERSION` 172. A React/Preact arm (see "React/Preact component health" below) shares the finding kind, rule key, and suppress token. Svelte 5 `$props()` is also covered (the detector processes `.svelte` files gated on `svelte` / `@sveltejs/kit`; `.astro` too); `unused-named-slot` deferred. **Opt-in `unusedComponentProps.ignorePattern`** (issue #1648): a top-level config section `unusedComponentProps: { ignorePattern: "<regex>" }` exempts any prop whose LOCAL destructure binding name (`ComponentProp.local`, e.g. `_stage`, which equals the public `name` when there is no alias) matches the regex, honoring the leading-underscore "accepted-but-intentionally-unused" convention (TS `noUnusedParameters` + ESLint `varsIgnorePattern`). Matching is UNANCHORED (`regex::Regex::is_match`, mirroring ESLint's `RegExp.test`), so `_` (no `^`) matches any prop containing an underscore; users anchor with `^_`. The regex is validated at config load (an invalid pattern fails load with exit 2) and compiled onto `ResolvedConfig.unused_component_props_ignore`; the SFC and React arms both skip a matching prop. Default unset = no exemption (behavior unchanged). When >= 1 prop is exempted, a human-format stderr note reports the count (so a typo'd pattern matching nothing is not a silent no-op). The match is on `local`, but the finding reports the public `name`. Applies to all four arms (Vue / Svelte / Astro / React); the Angular `unused-component-input` / `-output` rules are out of scope (class fields, not destructured locals). No `CACHE_VERSION` bump (extraction unchanged; `local` was already harvested).
- **Unused component emits (`unused-component-emit`)**: a Vue `<script setup>` `defineEmits` declared event EMITTED by NO code in its own SFC (no `emit('<name>')` call in `<script>` and no `emit('<name>')` / `$emit('<name>')` in `<template>`). Single-file dead-OUTPUT direction, the emit-side sibling of `unused-component-prop`; vue-tsc/Volar check caller-side emit/listener correctness, not in-component non-emission. EXTRACTION (`crates/extract/src/sfc_props.rs::harvest_define_emits`, wired in `sfc.rs::merge_script_into_module` directly after `harvest_define_props`, gated on `is_setup`): harvests event names from the array form (`defineEmits(['save','close'])`), the runtime/type object/literal form, and the `emit_binding` capture (`const emit = defineEmits(...)`, tracking the ACTUAL bound local, not a hardcoded `emit`); a type-REFERENCE arg (`defineEmits<MyEmits>()`) sets `has_unharvestable_emits` (abstain, names unresolvable). Per-emit `used` is set from the script-side `emit('<name>')` call walk (through the real binding) plus the template-side `apply_template_emit_usage` (credits `emit('<name>')` / `$emit('<name>')` in the template-only region, scripts/styles/comments masked) and rides a new `ModuleInfo.component_emits: Vec<ComponentEmit>` plus the abstain flags `has_unharvestable_emits` / `has_dynamic_emit` / `has_emit_whole_object_use`; `CACHE_VERSION` bumped (160). DETECTOR (`crates/core/src/analyze/unused_component_emit.rs`): dep-gated on `vue`/`@vue/runtime-core`/`nuxt`, reachable `.vue` only, emits an event with `!used`. ABSTAIN ladder (whole-file, zero-FP): not `<script setup>`; `has_unharvestable_emits` (type-reference / unbound non-literal `defineEmits`); `has_dynamic_emit` (an `emit(<nonLiteral>)` / `$emit(<nonLiteral>)`, event unknowable); `has_emit_whole_object_use` (the emit binding passed / returned / spread); `defineModel` (creates implicit `update:x` emits, reuses the props-side `has_define_model` flag set by `harvest_define_props` which runs first). Default severity `warn`, suppress-only (`// fallow-ignore-next-line unused-component-emit`), no autofix (removing an emit can break a deliberately-stable public component API). IssueKind discriminant 39. Validated on a 10-project real-world corpus: 6 true positives (vbenjs/vue-vben-admin json-viewer `click`/`keyClick`/`toggle`/`valueClick`, element-plus picker-range-trigger `focus`/`blur` which are only `defineExpose`'d) with zero false positives; the template-emit and renamed-binding cases were the FP classes that drove the template-usage credit and the bound-local tracking. Vue `<script setup>` plus the Vue Options API (W2.3): `harvest_options_api_emits` (same file) harvests `emits:` array / object forms from `export default {...}` / `defineComponent({...})`, with `this.$emit('<name>')` script calls + template `$emit` as the usage credit, and whole-component abstain on `mixins:` / `extends:`, `this.$emit(<nonLiteral>)`, an identifier/spread/type-generic emits value, and a `setup(props, { emit })` method (a bare `emit('<name>')` through the context binding is invisible to the `this.$emit` walk, so the component's emits abstain via `has_dynamic_emit`). Svelte event dispatchers and `unused-named-slot` deferred. The prop-side sibling is `unused-component-prop` above.
- **Unused Svelte events (`unused-svelte-event`)**: a Svelte component that dispatches a custom event via a `createEventDispatcher` binding (`const d = createEventDispatcher(); d('<name>')`) whose name is LISTENED nowhere project-wide. Cross-file dead-OUTPUT direction (the dispatched-but-unlistened half); no native tool covers the listener side. IssueKind discriminant 47, default `warn`, gated on `svelte`, suppress-only, no autofix. EXTRACTION (`crates/extract/src/visitor/visit_impl.rs::record_event_dispatch_binding` / `record_svelte_dispatch_call`): tracks `dispatch` bindings whose init is a `createEventDispatcher()` call imported from `svelte`, records each `dispatch('<literal>')` event name + span on `ModuleInfo.svelte_dispatched_events` (`DispatchedEvent { name, span_start }`); a `dispatch(<nonLiteral>)` or a `dispatch` reference used as a value sets `has_dynamic_dispatch` (whole-component abstain). The Svelte template scanner (`sfc_template/svelte.rs::collect_listened_events`) records `on:<name>` listeners on COMPONENT tags (PascalCase / member-expr) into `ModuleInfo.svelte_listened_events`; `on:<name>` on a lowercase DOM element is a native DOM event and is EXCLUDED; event forwarding (`on:<name>` with no handler) counts as a listen. Dispatch spans are body-relative and remapped onto the SFC source by the script byte offset in `sfc.rs` (mirrors the prop/emit remap), so the finding line points at the real `dispatch(...)` call. CACHE_VERSION bumped (174). DETECTOR (`crates/core/src/analyze/unused_svelte_event.rs`): two-pass set-difference mirroring `unprovided-inject`: Pass 1 unions every module's `svelte_listened_events` into a liberal project-wide set (a listener on ANY component credits the name, the safe over-credit direction); Pass 2 flags each reachable `.svelte` module's dispatched events not in that set, abstaining when `has_dynamic_dispatch`. Validated zero-FP on the Budibase monorepo (215 `createEventDispatcher` components, 6 true positives). The Svelte-5 callback-prop direction (a callback prop the parent never passes, caller-side) is deferred; Svelte prop-deadness stays a NON-GOAL (compiler + eslint cover it).
- **Unused server actions (`unused-server-action`)**: a Next.js Server Action (an export of a `"use server"` file) that NO code in the project references: no import-and-call, no `action={fn}` JSX binding, no `<form action={fn}>`. Cross-graph dead-EXPORT direction. eslint-plugin-next is single-file and cannot see cross-file usage. This is NOT a new detection pass: it RECLASSIFIES the server-action subset of `unused_exports`. `crates/core/src/analyze/unused_server_action.rs::reclassify_unused_server_actions` runs AFTER the parallel detectors populate `results.unused_exports` and BEFORE stale-suppression detection (so a `// fallow-ignore-next-line unused-server-action` marker is recorded consumed, not stale). It moves each `unused_exports` finding whose defining file's `ModuleInfo.directives` contains `"use server"` (mapped graph-node-path -> FileId via `graph.modules`) OR whose export name is in the defining file's `ModuleInfo.inline_server_action_exports` (W4.4: inline `"use server"` body actions, see below) into `results.unused_server_actions`, skipping `is_type_only` and `is_re_export` findings. Because it reclassifies the ALREADY-computed `unused_exports` set, it inherits EVERY abstain that detector applies: entry-point skip (Next infra-invoked route files), public-API re-export crediting (a re-export IS a reference), whole-object / namespace opacity, and the reachable-reference predicate. The `action={fn}` and `<form action={fn}>` JSX value bindings are credited as references by `oxc_semantic` (value-position resolution), so wired-up actions never reach `unused_exports` and are never flagged; wrapped action factories (`authenticatedActionClient.action(async()=>{})`, `withAuditLogging(...)`) are credited by the const's binding references regardless of wrapping. The marginal false-positive surface over `unused-exports` (already `error`-trusted) is therefore just the literal `"use server"` directive gate. GATED on `declared_deps.contains("next")` and on the rule being active (`config.rules.unused_server_actions != Off`); when the rule is `off`, NO reclassification runs and these stay reported as `unused-export` (so turning the rule off restores the prior gate). It does NOT mean the endpoint is unreachable: Next still registers a generated action id, so it stays POST-able; it means no project code references it (likely forgotten / dead). Default severity `warn`, suppress-only (`// fallow-ignore-next-line unused-server-action`), no autofix (wire the action to a consumer or delete it, a human decision). IssueKind discriminant 40. Validated zero-FP across 13 real Next.js App Router projects (`/tmp/nextjs-study`: dubinc/dub, formbricks, midday, vercel ai-chatbot/commerce/platforms, etc.) plus local Next projects: one true positive (`saveChatModelAsCookie` in vercel/ai-chatbot, defined once and referenced nowhere), zero false positives. Graduation to `error` is gated on continued zero-FP validation. Inline `"use server"` BODY directives (`export async function f() { "use server" }` in a non-use-server file) are ALSO covered (W4.4): the extract layer records the export local name of every exported function / `const`-arrow / `const`-function-expr whose body carries an inline `"use server"` directive (via `function_body_has_use_server`, captured in `extract_declaration_exports` so only real exports qualify) onto `ModuleInfo.inline_server_action_exports: Vec<String>` (rides the cache mirror, NOT released in `release_resolution_payload`; `CACHE_VERSION` 177 to 178), and the reclassifier moves an unused export whose name matches. It inherits the same unused-export abstains, so the marginal surface over the file-level case is just the inline directive gate; validated zero-FP on vercel ai-chatbot / commerce. The dynamic-action-dispatch abstain (an action registry indexed by a computed id, mirroring the `unprovided-inject` `has_dynamic_provide` whole-project abstain) remains the explicit blocker before graduating to `error`.
- **Unused load data keys (`unused-load-data-key`)**: a SvelteKit route `load()` return-object key (in `+page.{ts,server.ts,js,server.js}`) read by NO consumer. A dead returned key runs a real server-side fetch / DB cost on every request for data nothing renders; `svelte-check` types `data` via generated `$types` but never flags an unread RETURNED key (the unused-INPUT direction). Cross-file dead-input direction. EXTRACTION (`crates/extract/src/visitor/visit_impl.rs::try_harvest_load_export`): harvests the keys of a `load`'s terminal return object literal from `export const load` / `export [async] function load` declarations, peeling `satisfies PageLoad` / `as` / parenthesis wrappers; the harvest fires loosely for any exported `load` and `parse.rs::is_sveltekit_page_load_file` clears it for non-`+page.*` files by basename. ABSTAIN signals on `ModuleInfo` (all set so a key is never falsely flagged): `has_unharvestable_load` (spread return, non-object/non-literal return, >1 top-level `return`, computed key, wrapped / re-exported `load`); `has_load_data_whole_use` (FP-1: a sibling component passing the whole `data` prop opaquely via script `const X = data` / `fn(data)` / `fn(...data)` / destructure-assignment `({a} = data)`, or template `data={data}` / `{...data}`); `has_page_data_store_whole_use` (derived in `release_resolution_payload` from `whole_object_uses`, NOT cached: any module's whole-object use of `page.data` / `$page.data` abstains ALL routes project-wide). DETECTOR (`crates/core/src/analyze/unused_load_data_key.rs::find_unused_load_data_keys`): dep-gated on `@sveltejs/kit`; credits a key through three channels: (1) the sibling `+page.svelte`'s `data.<key>` member accesses (route-pinned), (2) project-wide `page.data.<key>` (Svelte 5 `$app/state`), (3) project-wide `$page.data.<key>` (Svelte 4 `$app/stores`). FP-2 server->universal chain: a `+page.server.ts` whose sibling universal `+page.ts` reads / forwards its `data` param is credited (and abstains wholesale if the universal load forwards `data` opaquely). The detector returns `LoadDataKeyResult { findings, global_abstain }`; the project-wide whole-object abstain sets `results.unused_load_data_keys_global_abstain` (S1 observability: an empty findings list with the flag set is NOT a clean bill, surfaced as a human stderr note). Typed-`data`-prop routes (`export let data: PageData`) keep template `data.<key>` accesses keyed on `data` (the SFC scanner drops `data` from bound-targets for the template scan so they are not remapped onto the generated `$types` alias). Default severity `warn`, suppress-only (`// fallow-ignore-next-line unused-load-data-key`), no autofix (a load fetch can have side effects, so deletion is a human call). `CACHE_VERSION` bumped (164 to 166: 165 for `load_return_keys` + `has_unharvestable_load` + `has_load_data_whole_use`, 166 for the typed-data template fix). IssueKind discriminant 41. SARIF rule id `fallow/unused-load-data-key`. Validated on a 10-project SvelteKit corpus: true positives in syntaxfm-website (`id` returned but only read off `$page.data` for `meta`, not `id`) and sveltejs/realworld (`page` pagination value returned, only `articles` read), zero false positives. Cut A is page loads only; layout loads (`+layout(.server).{ts,js}`) are deferred. Extraction primitives A/B/C land in #1255 / #1257 / #1260.
- **React/Preact component health (`unused-component-prop` React arm, `prop-drilling`, `thin-wrapper`, `duplicate-prop-shape`, render fan-in)**: a JSX structural extraction layer (`crates/extract/src/visitor/react.rs` + `visit_jsx_element` in `visit_impl.rs`, gated to `.jsx`/`.tsx`, no-op on non-JSX files so non-React repos pay nothing) records `ModuleInfo.component_functions` (JSX-returning fns incl. `forwardRef`/`memo` wrappers), `react_props` (reusing `ComponentProp`; for React `used_in_template` is always false and `used_in_script` = used-in-body), `hook_uses` (`useState`/`useEffect`/`useMemo`/`useCallback`/custom `use*`), and `render_edges` (a capitalized or member-expression JSX tag = a component render, with the attr names passed and a `has_spread` flag); `CACHE_VERSION` 169. Two detectors ride it. (1) The **`unused-component-prop` React arm** emits the SAME finding kind / rule key / suppress token as the Vue prop detector, dep-gated on `react`/`react-dom`/`next`/`preact`, default `warn`. v1 scope is inline-destructured literal props; ABSTAIN ladder (whole-component, zero-FP): rest spread (`{...rest}`), props forwarded by spread, props passed wholesale to a hook, `forwardRef`/`memo` whose props are an imported interface (unresolvable per ADR-001), and exported public-API component props (reuses the public-API abstain). (2) **`prop-drilling`** (`crates/core/src/analyze/prop_drilling.rs`, IssueKind discriminant 42, SARIF rule id `fallow/prop-drilling`): a cross-file graph signal flagging a received prop whose identifier appears ONLY as the root of forwarded child-JSX attribute values, threaded unchanged through `>= N=3` pass-through components before a component substantively consumes it (the root-of-forwarded-value test catches `<Child userName={user.name}/>`, not just same-name forwards, which eslint cannot see because it is per-file). Emits located per-chain records (source -> pass-through hops -> consumer, each with file/line/component) in human / JSON / SARIF. OPT-IN, default `off` like `private-type-leak` and the security rules; enable via `rules.prop-drilling: "warn"`. ABSTAIN (whole-chain, zero-FP): any `{...props}`/`{...rest}` spread in the chain, `cloneElement`, element-as-prop / render-prop / children-as-function, a matching context `*.Provider` in the subtree, or any dynamic / unresolvable hop. HEALTH SIGNAL: a small capped penalty in `compute_health_score` (1pt per chain, 5pt cap, sized like the coupling penalty), populated post-construction in `health/mod.rs` from `prop_drilling_chains` (via `FileScoreOutput`) ONLY when the opt-in rule is enabled, so default scores are unchanged. React JSX nesting depth is descriptive `react_jsx_max_depth` context only; hook/prop density still fold into the per-component cognitive contribution and surface as `--complexity-breakdown` axes (`hook-density`/`prop-count`). The public `jsx-depth` contribution kind remains reserved for compatibility but current extraction does not emit it for layout depth. Validated on `tests/fixtures/unused-react-prop` and `tests/fixtures/prop-drilling{,-abstain}` plus real React trees (next.js / query / preact): zero false positives, the drilling chain `Page -> Layout -> Sidebar -> Profile` detected. React + Preact in v1; Solid (props are reactive accessors) and React Native deferred. Three further signals ride the same extraction (all default `off` / descriptive-only, gated on `react`/`react-dom`/`next`/`preact`, no `CACHE_VERSION` bump beyond 170 for the extraction fields they consume): (3) **`thin-wrapper`** (`crates/core/src/analyze/thin_wrapper.rs`, IssueKind discriminant 43, SARIF rule id `fallow/thin-wrapper`): a component whose entire body is `return <Child {...props}/>` (proven via the extraction flag `ComponentFunction.is_pure_passthrough` plus a hook-density / cyclomatic belt-and-suspenders join and the resolved single render edge), a candidate to inline or delete; ABSTAIN ladder (zero-FP): `forwardRef`/`memo` wrappers, exported components, context-provider wrappers, `cloneElement`, render-prop / children-as-function, a complexity mismatch, a self-render, an unresolved / member-expression / `.Provider` child. (4) **`duplicate-prop-shape`** (`crates/core/src/analyze/duplicate_prop_shape.rs`, IssueKind discriminant 44, SARIF rule id `fallow/duplicate-prop-shape`): groups `>= 3` fully-harvestable components across `>= 2` files whose `>= 4`-name significant prop set (after a ubiquitous-DOM-name denylist) is byte-identical, a missing-shared-`Props`-type candidate; exact full-set identity only (superset/subset do not group), one finding per member with `sharing_components`, route-collision suppress model. (5) **render fan-in** (`crates/core/src/analyze/render_fan_in.rs`): a DESCRIPTIVE blast-radius metric (NOT a rule / finding / IssueKind / severity) counting JSX render SITES + distinct parent components per component, the component-graph analogue of module fan-in; rides the hotspot/coupling machinery (`#[serde(skip)]` carrier `AnalysisResults.render_fan_in` -> health surface `p95_render_fan_in` / `render_fan_in_high_pct` / `max_render_fan_in`), undercount-safe. Three of the four signals (prop-drilling, thin-wrapper, duplicate-prop-shape) are excluded from `total_issues` (health signals, not dead-code); only the `unused-component-prop` React arm counts. Deferred follow-ups: rendered-nowhere (React), Solid / React Native. **Deepened prop harvest + editor intelligence:** the React arm now also harvests SAME-FILE typed-interface props (`(props: Props) => props.x`, member-access usage credited) and generic `forwardRef<Ref, Props>` props, not only inline-destructure literals (imported / `extends` / generic / intersection prop types and any whole-object props use still abstain), widening the existing rule's coverage with zero new false positives on the corpus. The same React IR (`react_props` / `render_edges` / `hook_uses`, with each `HookUse` now tagged by its enclosing `component`) additionally feeds a DESCRIPTIVE, LSP-only `AnalysisResults.react_component_intel` carrier: a per-component summary (render fan-in, prop count, hook breakdown) + per-prop usage (read-in-body / passed-from-N-sites) + a prop-drilling trace, surfaced as an editor code lens / hover, NOT a finding, gated on `collect_usages` so bare `fallow` / `audit` never compute it (see `.claude/rules/lsp-server.md`). `CACHE_VERSION` 188.
- **Unused component inputs / outputs (`unused-component-input` disc 45, `unused-component-output` disc 46)**: an Angular component (or `@Directive`) input declared but read NOWHERE in its own component (neither inline/external template nor class body), and an output `.emit()`-ed NOWHERE in its own component. Single-file dead-INPUT and dead-OUTPUT directions, the Angular analogue of the Vue `unused-component-prop` / `unused-component-emit` siblings; no `@angular-eslint` rule covers either, and the Angular compiler never flags a declared-but-unread `@Input` (it only checks caller-side binding correctness). A declared input bound only by a parent but never read in-component IS flagged: the binding is wired to a dead end. EXTRACTION (`crates/extract/src/visitor/helpers.rs::extract_angular_inputs_outputs`, riding `ModuleInfo.angular_inputs: Vec<AngularInputMember>` / `angular_outputs: Vec<AngularOutputMember>` with `{name, span}`; populated in BOTH `into_module_info` and `merge_into`): harvests inputs from the decorator form (`@Input() foo`, `@Input({required:true})`), the signal form (`input()` / `input.required()`), and `model()`; outputs from the decorator form (only `new EventEmitter()`-style initializers are harvested, an observable-stream `@Output` is left as an abstain shape) and the signal `output()` form. `model()` lands on the INPUT list only (its implicit `update:` emit is framework-driven, so it is excluded from the output side). Accessor (get/set) members are skipped at harvest. `inputs:[...]` decorator-metadata and `host:{...}` member refs are sentinel-credited at extraction, so they never reach the flag path. DETECTORS (`crates/core/src/analyze/unused_component_input.rs` / `unused_component_output.rs`, both dep-gated on `@angular/core` in `declared_deps`): the INPUT usage set credits an input USED if it has a template sentinel ref (`object == ANGULAR_TPL_SENTINEL`, inline OR external `templateUrl` via the `SideEffect` edge to the `.html` module), ANY `this.<member>` read, OR ANY member-by-name access (which covers the `ngOnChanges` `changes['foo']` pattern without a blanket abstain); the OUTPUT usage set credits an output USED on a `this.<out>.emit` call site, a template `(event)="x.emit()"` handler, OR any forwarded `this.<out>` value read. Over-crediting can only suppress a finding, never create one. ABSTAIN ladder (whole-component, zero-FP): any `extends` heritage clause (a base class in another file may read `this.<member>`; cross-file inheritance is invisible to the per-module usage scan); a `{...this}` spread; JS-reserved-word member names. (Required inputs / `input.required()` are STILL flagged when unread: a never-read required input is genuinely dead.) Both default severity `warn` (lower-confidence open declaration surface), suppress-only (`// fallow-ignore-next-line unused-component-input` / `-output`), no autofix (wire-or-remove is a human decision). `CACHE_VERSION` bumped to 171 (the `angular_inputs` / `angular_outputs` IR plus the two new suppression tokens). Validated at zero false positives on the angular/components monorepo. Directive selectors / structural-directive dead inputs, the cross-component never-bound direction, and inherited-member-read recovery via `ClassHeritageInfo` are deferred.
- **Duplication skips test files by default** (issue #1386): `DUPES_DEFAULT_IGNORES` excludes `*.test.*`, `*.spec.*`, `__tests__`, and `__mocks__` paths from clone detection before tokenization. These are engine policy defaults, not config-file defaults: `duplicates.ignore` stays empty in round-tripped configs, default-skip counts still surface in the human sidecar, and `duplicates.ignoreDefaults: false` restores test files to the duplication corpus.
- **Duplication ignores module wiring by default** (issues #1224 and #1225): `DuplicatesConfig.ignore_imports` defaults to `true`. The duplication tokenizer (`crates/core/src/duplicates/`) strips ES `import` statements, re-export declarations (`export ... from`, `export * from`), and top-level static CommonJS `require("...")` binding declarations from the token stream before clone detection, so token-identical module wiring is never reported as clone groups. Local exports, side-effect `require()` calls, nested `require()` calls, dynamic require arguments, and mixed declarations are still tokenized as code. Precedence (CLI override > config > default) resolves at the single `build_dupes_config` chokepoint in `crates/cli/src/dupes.rs`: `DupesOptions.ignore_imports: Option<bool>` (`None` defers to config) is set from `--ignore-imports` / `--no-ignore-imports` (standalone) or `--dupes-ignore-imports` / `--dupes-no-ignore-imports` (combined; both pairs are clap `conflicts_with`). Audit reads the resolved config value; LSP/napi default to config via their existing `unwrap_or(config.ignore_imports)`. A human-format-only stderr note (`print_ignore_imports_note`) points users at the opt-out when module wiring was excluded and clone groups were reported. The field serde default uses `default_true` (NOT `bool::default()`), so an omitted `ignoreImports` in `.fallowrc.json` deserializes to `true`.
- **Duplication tokenizes web-format source**: duplicate detection tokenizes `.css`, `.scss`, `.sass`, and `.less` files with a lightweight lexical tokenizer, and also tokenizes authored template and style regions in `.vue`, `.svelte`, and `.astro` files. SFC and Astro region merging inserts synthetic `TokenKind::Boundary` tokens, and clone extraction drops any raw group whose candidate range contains a boundary, so script, template, and style tokens cannot be stitched into one artificial clone. When this coverage changes, bump `DUPES_CACHE_VERSION` in `crates/extract/src/cache/types.rs`; older token caches can otherwise preserve empty CSS streams or script-only SFC/Astro streams.
- **tap and tsd test entry plugins**: the tap plugin activates from `tap` and marks node-tap default test discovery (`test/`, `tests/`, `__tests__/`, `.test.` / `.spec.` files, and top-level `test.*` / `tests.*`) as `EntryPointRole::Test`. The tsd plugin activates from `tsd`, marks `.test-d.ts(x)` declaration tests as test entry points, and reads `package.json#tsd.directory` so custom directories such as `test/types` stay reachable.
- **Wrangler config entries follow Wrangler's selected sibling file**: the Wrangler plugin activates from `wrangler`, keeps `wrangler.toml`, `wrangler.json`, and `wrangler.jsonc` config files always used, and reads `main` / `env.*.main` entries only from the highest-precedence sibling config in the same directory. Current Wrangler source selects `wrangler.json`, then `wrangler.jsonc`, then `wrangler.toml`; presence wins over parse success, so lower-precedence `main` values do not become fallback entries. Config files in different directories do not suppress each other, preserving monorepo worker roots. See issue #630.
- **Supabase Edge Functions**: the `supabase` plugin activates on the `supabase` CLI dependency OR a `supabase/config.toml` file OR a `supabase/functions/` directory (the CLI is often installed globally, so the filesystem signals are the robust activation), and marks `supabase/functions/*/index.{ts,tsx,js,jsx,mts,mjs,cts,cjs}` as `EntryPointRole::Runtime` roots so each deployed function entry is not reported as `unused-file` solely because the Node app graph never imports it. Shared code under `supabase/functions/_shared` and per-function helpers stay reachable through their normal relative imports, so a genuinely-orphaned shared file still reports as unused. The `supabase` CLI is credited as a tooling dependency (invoked from scripts, not imported). Pairs with the Deno-scheme resolution handling above so `jsr:` / `npm:` / URL imports inside function files produce no noise. Per-function custom entrypoints declared via `supabase/config.toml` `[functions.<name>] entrypoint` or per-function `deno.json` import maps are out of scope; non-`index` entrypoints fall back to the user's `entry` config. See issue #624.
- **Duplicate export common-importer partition**: a same-name export group is partitioned into connected components by shared importer (union-find: two exports are connected when a third file imports both, or one directly imports the other), and each component is judged independently. Singleton components (an export sharing no importer with any sibling) are dropped as unrelated leaves (e.g., SvelteKit route modules in different directories that coincidentally export the same name). Value/type self-suppression (a lone value paired with a lone type alias is the normal TypeScript runtime-value + type pattern, not a duplicate) is applied per component, so a cross-package member that shares no importer with the rest of the group can no longer inflate a sibling component's value count and defeat its suppression. A finding is emitted only when at least one component survives with two or more connected locations. Previously this was a whole-group boolean (`has_common_importer`): if any pair in the group shared an importer, every member was emitted, which let an unrelated cross-package export defeat the value/type check (issue #848).
- **Duplicate-exports honors `ignoreExports`**: a file matched by a config `ignoreExports` rule is excluded from duplicate-exports grouping (`exports: ["*"]` excludes the whole file, a name list excludes only those names). Built for shadcn / Radix / bits-ui-style component libraries where many `components/ui/<name>/index.ts` barrels intentionally export the same short names (`Root`, `Content`, `Trigger`). Plugin / framework `used_exports` rules are generally NOT consulted for duplicate-exports gating: they say "this export is framework-managed", not "this export's duplication across barrels is intentional". The exception is TanStack Router route contract exports, where every route file is required to export names such as `Route`; duplicate-exports skips those names only when the file matches the TanStack plugin's route used-export rules, including configured route dirs, lazy routes, ignore prefixes, and virtual route config, or when a nested `routes` module's `Route` export is referenced by `routeTree.gen.*`. Per-file `overrides.rules.duplicate-exports` is a no-op because the rule's severity is project-wide and the finding spans multiple files; the resolver emits a load-time `tracing::warn` for any `overrides.rules.duplicate-exports` or `overrides.rules.circular-dependency` setting pointing users at the working escape hatch. See issues #317 and #947.
- **Decorated class members**: members with decorators (NestJS `@Get()`, Angular `@Input()`, TypeORM `@Column()`) default to NOT being reported unused, because frameworks consume them reflectively. The `MemberInfo.decorator_names: Vec<String>` field captures the FULL dotted path of each decorator (`@step("x")` -> `"step"`; `@ns.foo` -> `"ns.foo"`). The skip predicate in `find_unused_members` consults `FallowConfig.ignore_decorators`: a method whose every decorator path is in the ignore set is checked normally; a method with any non-ignored decorator stays skipped. Default empty list preserves the legacy skip-all behavior. Matching rule: dotted entries match the full path; bare entries match the leftmost segment, so `"decorators"` collapses every `@decorators.*` decorator. Both `"@step"` and `"step"` accepted at config load (leading `@` stripped). Angular signal-initializer properties (`input()`, `output()`, etc.) carry `has_decorator: true` with empty `decorator_names` and always skip; the gate has no name to match against the ignore list. Unmatched ignore-list entries emit a one-time `tracing::warn!` at end of run via `IgnoreDecoratorSet::warn_unmatched`, mirroring `ClassMemberAllowlist::warn_unmatched_patterns`. The gate operates on the DECLARING class's `decorator_names`; inherited members carry their own (or no) decorators. See issue #471.
- **Entry-point public API class members**: class methods/properties on classes exposed through non-private package entry points are not reported as `unused-class-member` by default. The detector requires public-API provenance from an actual non-private package entry point re-export (`main`, root `exports`, subpath `exports`), from a barrel inside the transitive `export *` closure rooted at one, or from `src/**/index.*` source subpath indexes when the package has no `exports` map. Do not key this on `ReferenceKind::ReExport` alone: internal barrels also create re-export references and must keep member-level detection. Named, renamed, default-as-named, package subpath exports, source-first root `index.js` fallbacks, exportless source subpath indexes, and multi-hop star barrels are covered. The skip is class-member-only; enum member behavior is unchanged, private app packages do not get this skip, and reachable internal classes imported by an entry point but not re-exported still report unused public members. See issue #643.
- **Configurable class member allowlist**: the built-in Angular/React lifecycle check (see `predicates::lifecycle`) is extended at analysis time with two user-controlled sources: (1) top-level `FallowConfig.used_class_members`, (2) `Plugin::used_class_members()` / `ExternalPluginDef.used_class_members` on active plugins. Both sources feed `AggregatedPluginResult.used_class_members`, which `analyze/mod.rs` merges with the config list into a single `FxHashSet<&str>` passed to `find_unused_members`. Intended for third-party interface patterns where a library calls consumer methods reflectively (ag-Grid's `agInit`/`refresh`, Web Components' `connectedCallback`). The allowlist is scoped to class members — matching enum member names are still flagged. Member strings containing `*` or `?` compile as `globset::Glob` matchers, supporting `"*"` (every member on a heritage-matching class), `"enter*"` / `"*Handler"` (prefix / suffix), and `"on*Event"` (combined). Globs run alongside exact-name matches and respect any `extends` / `implements` heritage clause on a scoped rule. Glob patterns matching zero members emit a single `WARN` per pattern at the end of the run via `ClassMemberAllowlist::warn_unmatched_patterns`, surfacing dead allowlist entries; exact-name entries do not warn. See issues #98, #254.
- **Lexical custom-node lifecycle members**: the `lexical` plugin (enabler: `lexical` or any `@lexical/`-scoped package) credits the methods Lexical calls reflectively on custom nodes, so classes that `extends DecoratorNode` / `ElementNode` / `TextNode` do not surface their framework-invoked methods as `unused-class-member`. Implemented via heritage-scoped `UsedClassMemberRule::Scoped` rules in `crates/core/src/plugins/lexical.rs`, mirroring the `lit` / `ember` plugins. The shared lifecycle / serialization / DOM-reconciliation set (`getType`, `clone`, `importJSON`, `importDOM`, `exportJSON`, `exportDOM`, `createDOM`, `updateDOM`, `updateFromJSON`, `getTextContent`) is credited on all three bases; `isInline` is additionally credited on `ElementNode` and `DecoratorNode` only (TextNode is inherently inline and has no `isInline()` hook); `decorate` is credited on `DecoratorNode` only. The per-base layering means a genuinely-dead `isInline` on a TextNode subclass or `decorate` on an ElementNode subclass still surfaces. Method names verified against the Lexical custom-node docs (lexical.dev/docs/concepts/nodes). Heritage matching is on the direct superclass name, so an intermediate base between the user class and the Lexical base is not covered; custom nodes extend a Lexical base directly in practice. Non-lifecycle methods on a node subclass are still reported. No `tooling_dependencies()` is declared because Lexical packages are directly-imported runtime deps. Replaces the manual scoped `usedClassMembers` allowlist users hand-write today. See issue #628.
- **Ionic Angular page lifecycle members**: the `ionic` plugin (enabler: `@ionic/angular`) credits Ionic's Angular page lifecycle methods that the router outlet invokes by name: `ionViewWillEnter`, `ionViewDidEnter`, `ionViewWillLeave`, and `ionViewDidLeave`. Implemented as exact active-plugin `used_class_members()` entries in `crates/core/src/plugins/ionic.rs`, matching Ionic runtime behavior where the methods are called by name with or without the optional TypeScript interfaces (`ViewWillEnter`, `ViewDidEnter`, `ViewWillLeave`, `ViewDidLeave`). Other `ionView*` typos still report as `unused-class-member`. The plugin also keeps `ionic.config.json` reachable and treats `@ionic/cli`, `@ionic/angular-toolkit`, and `ionicons` as Ionic tooling/runtime-template dependencies when the Ionic Angular plugin is active. No `CACHE_VERSION` bump: the names are interpreted at analyze time. See issue #868.
- **NestJS framework-dispatched lifecycle / handler members**: the `nestjs` plugin (enabler: `@nestjs/core`) credits the methods Nest invokes reflectively on consumer classes, so they are not reported as `unused-class-member`. Implemented via heritage-scoped `UsedClassMemberRule::Scoped` rules in `crates/core/src/plugins/nestjs.rs` (one rule per Nest interface, all `implements`-scoped, never `extends`), mirroring the `lit` / `lexical` / `ember` plugins. Handler dispatch methods are scoped to their single interface: `configure` (`NestModule`), `canActivate` (`CanActivate`), `intercept` (`NestInterceptor`), `transform` (`PipeTransform`), `catch` (`ExceptionFilter`), `use` (`NestMiddleware`). The five lifecycle interfaces (`OnModuleInit`, `OnModuleDestroy`, `OnApplicationBootstrap`, `BeforeApplicationShutdown`, `OnApplicationShutdown`) each credit ALL five lifecycle method names (`onModuleInit`, `onModuleDestroy`, `onApplicationBootstrap`, `beforeApplicationShutdown`, `onApplicationShutdown`) rather than only the one method the implemented interface declares. This is intentional and more correct: Nest's lifecycle manager dispatches by duck-typed method presence, so a class that implements one lifecycle interface but also defines a sibling hook method has that sibling invoked at runtime too. Heritage matching is on `implements`, so an ordinary class that implements none of these interfaces still surfaces a genuinely-unused `transform` / `configure` / lifecycle method, and non-lifecycle methods on a Nest class are still reported. No `CACHE_VERSION` bump: the rules are interpreted at analyze time against existing `implements` heritage data and never enter the extraction cache. See issue #843.
- **Native Error subclass `name` members**: a class member named `name` declared on a class whose heritage reaches a native ECMAScript error constructor (`Error`, `TypeError`, `RangeError`, `SyntaxError`, `ReferenceError`, `EvalError`, `URIError`, `AggregateError`) is treated as runtime-used and never reported as `unused-class-member`. Custom error classes set `name` so logs, serializers, and `err.name === "..."` discrimination expose the error type, none of which is a static member access fallow can see. Implemented as a built-in heritage-scoped suppression in `crates/core/src/analyze/unused_members.rs` (`is_error_subclass_runtime_member`), mirroring the existing native-Custom-Elements built-in (`is_native_custom_element_lifecycle_method`, `super_class == Some("HTMLElement")`); no plugin or dependency gate because the bases are JavaScript globals. `build_error_subclass_export_keys` seeds on classes that directly extend a native error name, then walks `extends` chains downward via `build_parent_to_children`, so a transitive `class ApiError extends DomainError` where `DomainError extends Error` (resolved through imports, local exports, and re-export origins) is also covered. The rule is heritage-scoped: an ordinary `class Person { name = "x" }` still reports an unused `name`, and members other than `name` on error subclasses (`message` / `stack` / `cause` are inherited and rarely re-declared; arbitrary helper methods) still report. Seeding is by direct super-class name, matching the HTMLElement built-in, so a local class shadowing a native error name is treated as a native error base (pathological in practice). Same-file NON-exported intermediate base classes are not resolved, the same limitation `build_parent_to_children` carries for all heritage-aware member crediting. No `CACHE_VERSION` bump: only analyze-time interpretation of the existing `class_heritage` / `super_class` extraction data changes. See issue #620.
- **OpenLayers framework-dispatched interaction members**: a method OpenLayers invokes by convention on a subclass of an `ol/interaction/*` base (`handleEvent`, the `PointerInteraction` template-method protocol `handleDownEvent` / `handleDragEvent` / `handleMoveEvent` / `handleUpEvent`, and `stopDown`) is treated as runtime-used and never reported as `unused-class-member`. The map dispatches these per browser event with no explicit `instance.method()` call site, so they otherwise surface as dead. Implemented as a built-in heritage-scoped suppression in `crates/core/src/analyze/unused_members.rs` (`is_ol_interaction_dispatched_member`), mirroring the native-error and native-Custom-Elements built-ins. The gate is IMPORT-SOURCE-VERIFIED, not name-only: `build_ol_interaction_subclass_keys` seeds a class only when its `super_class` local name resolves through the module's own imports to a specifier matching `ol/interaction` (the barrel named import) or `ol/interaction/*` (the per-class subpaths), then walks `extends` chains downward via `build_parent_to_children` so a transitive local subclass (`class B extends A` where `A extends PointerInteraction`) is covered. The import-source check IS the dependency proof, so no separate `ol` package gate is needed and it works even before `npm install` (the raw `info.source` survives). A subclass of a same-named LOCAL `PointerInteraction` (not the OpenLayers import) does NOT credit, and a genuinely-dead non-dispatched member on a real OpenLayers subclass still reports. Method names verified against the OpenLayers `interaction/Interaction.js` and `interaction/Pointer.js` sources. No `CACHE_VERSION` bump: only analyze-time interpretation of the existing `class_heritage` / `super_class` extraction data plus resolved import sources. See issue #1638 (GAP 1).
- **Implicit `toString` string coercion**: a class `toString` invoked only through implicit string coercion (template-literal interpolation `` `${x}` ``, `+` with a string operand, or `String(x)`) has no explicit `x.toString()` call site and otherwise surfaces as an `unused-class-member`. When a `new Class()` flows DIRECTLY into a coercion position, the extract layer records a `Class.toString` member access (`record_string_coercion_to_string` in `crates/extract/src/visitor/visit_impl.rs`, called from `visit_template_literal`, `visit_binary_expression`, and the `String(...)` arm of `visit_call_expression`); the analyze layer resolves `Class` through the file's imports / local exports to the class export and credits `toString`. The `+` arm requires the SIBLING operand to be a string literal or template literal (`is_string_coercion_sibling`), so a numeric / ambiguous `+` (`new Num() + 5`) does not over-credit; builtins (`new Date()`) are excluded by `new_expression_class_name`. Scope is intentionally tight to the direct `new Class()` form in a coercion position: a bound-identifier instance (`const m = new Money(); \`${m}\``) and a typed-instance flow are deliberately NOT credited (false-negative-preferring), and a `new Class()` NOT in a coercion position never credits, so a `toString` on a constructed-but-never-coerced class still reports. A TAGGED-template interpolation (`` html`${new Money()}` `` for Lit, styled-components, `gql`) is NOT a coercion position: the tag function receives the raw parts and values and does not call `toString`, so it does not credit. An `in_tagged_template_quasi` guard suppresses the credit for the tagged template's immediate quasi and is consumed per-quasi, so a plain template literal nested inside a tagged interpolation still coerces. `valueOf` / `Symbol.toPrimitive` are out of scope. Bumps the extract `CACHE_VERSION` because the synthesized `toString` member access changes serialized `member_accesses`. See issue #1638 (GAP 2).
- **Cross-package namespace re-export through object literal**: `import * as foo from './bar'; export const API = { foo }` in a barrel followed by `import { API } from '@scope/lib'; API.foo.bar` in another package credits `bar` on `./bar.ts`. The visitor records per-module `NamespaceObjectAlias` entries `{ via_export_name, suffix, namespace_local }` at finalize time (covering shorthand `{ foo }` and nested forms `{ motionNet: { adEngine } }`); both `into_module_info` and the SFC `merge_into` paths populate them. The graph layer threads them onto `ResolvedModule` and a Phase 2b pass (after reference population, before reachability) walks each consumer's `member_accesses` for `object == "<consumer_local>.<suffix>"`, looks up the namespace import's target file via the module's resolved imports filtered to `ImportedName::Namespace`, and runs the `mark_member_exports_referenced` + `create_synthetic_exports_for_star_re_exports` pair from `narrowing.rs` so star-barrel namespace targets (`./foo/index.ts` doing `export * from './bar'`) get a synthetic stub export the Phase 4 chain resolver can carry to the real defining file. Default-export aliases match against `ImportedName::Default`. **Consumer-side multi-hop** (issue #310): the alias-defining file may sit behind 1-3 named-re-export barrels (`pkg/index.ts` -> `pkg/methods/index.ts` -> `pkg/methods/methods.ts`); the matcher does a forward BFS over re-export edges starting from `(alias_file, via_export_name)` to enumerate every `(barrel_file, exported_name_at_barrel)` pair through which the alias is reachable, then matches consumer imports against the full set. Plain `export *` propagates names unchanged; `export { A as B }` propagates the renamed `B`; `export * as ns` is skipped at non-seed barrels (its source identifiers are hidden behind `ns.<name>`, not exposed directly). **Target-side multi-hop** (issue #328): the namespace target itself may chain through `export * as N from './source'` namespace re-exports (`barrel.ts` does `export * as inner from './leaf'`; consumer accesses `API.foo.inner.X`). After the initial Phase 2b credit lands on a name namespace-re-exported on the target, the matcher walks chained `export * as <name> from './source'` edges on the alias-target side and propagates the consumer's deeper accesses (`<expected_object>.<member>.<X>`) onto the underlying source recursively via `ChainWalkCtx`. Cycle-protected by a per-access visited set keyed on module index. Bumps the extract `CACHE_VERSION` because the new `namespace_object_aliases` field changes serialized module state. See issue #303 (cross-package case) building on issue #269 (same-file case which `resolve_bound_member_accesses` already handled by re-emitting the access against the namespace local); #310 (consumer-side multi-hop); #328 (target-side namespace-re-export chain).
- **Cross-package member access through barrels**: `import { Foo } from '@scope/lib'` followed by `Foo.bar` correctly credits `bar` on the originating file even when `@scope/lib`'s entry resolves to a barrel that re-exports `Foo` from a sibling module. Phase 4 chain resolution synthesizes a stub `ExportSymbol` on the barrel for reference tracking, so without further propagation the access map keys at the barrel and the detection loop's origin-keyed lookup misses where `members` are populated. `find_unused_members` walks each access key through `ReExportEdge` chains (named re-exports, renamed re-exports, and `export *` fan-out when no named edge matches) to every defining-site export and copies the access set. Cycle-protected. `export * as ns from './mod'` edges are intentionally skipped because two-level `ns.Foo.member` access isn't tracked at extract time. Same propagation runs for `whole_object_used_exports` (`Object.values(Foo)` on a re-exported enum). Covers both `unused_enum_members` and `unused_class_members`. See issue #178.
- **Inheritance-aware class members**: `extends` clauses are extracted to build an inheritance map. Parent class `this.*` accesses propagate to child class overrides (e.g., `BaseShape.describe()` calling `this.getArea()` credits `Circle.getArea()`). External member accesses propagate bidirectionally through the hierarchy. `super.method()` calls inside a subclass body are attributed to the parent via a `class_super_stack` in the visitor: the super-class identifier from the `extends` clause is pushed on class entry and popped on exit, and `Super.member` expressions record a `MemberAccess { object: <super_local>, member }` which `local_to_imported` then resolves to the parent's export. This covers the common pattern of a base class method only called via `super.method()` from subclasses. Only simple identifier extends are tracked (`class Foo extends Bar`), not mixin patterns. Methods called through parent-typed parameters remain unattributed (requires type inference, out of scope per ADR-001). See issue #130.
- **Private type leaks**: an exported value, function, or class whose public signature references a same-file type that is NOT exported by name is reported as `private-type-leak`. Default severity: `Off` (opt-in API hygiene check). Enable with the `--private-type-leaks` filter for a one-off run, or `private-type-leaks: "warn"` / `"error"` in `rules` for persistent enforcement. The CLI filter promotes the rule from `Off` to `Warn` for that invocation only via `IssueFilters::activate_explicit_opt_ins` in `crates/cli/src/check/mod.rs`. Detection runs in `find_private_type_leaks` (`crates/core/src/analyze/unused_exports.rs`) over `module_info.public_signature_type_references` (collected by the visitor's `SignatureTypeCollector`) cross-checked against `module_info.local_type_declarations`. Same-file private = the referenced name is in `local_type_declarations` AND not in the module's `exports`. References from ECMAScript private fields (`#field: T`) and TypeScript `private` accessibility members are NOT collected (the visitor short-circuits in `collect_class_signature_refs`), so they cannot leak. Qualified type names (`Foo.Bar.Baz`) reduce to the root identifier (`Foo`), since the leak is "the root namespace is private", not "every dotted segment is". Storybook story files (`*.stories.{ts,tsx,js,jsx,mts,cts,mjs,cjs}` and `*.story.*` variants) are skipped via `is_storybook_file`: the canonical `type Story = StoryObj<typeof X>; export const Default: Story = ...` pattern would otherwise generate one leak per story across thousands of UI library files. Framework routing convention files are also skipped via `is_route_convention_file` (Next.js App Router page/layout/template/loading/error/not-found/route/default/global-error/forbidden/unauthorized + the metadata files opengraph-image/twitter-image/icon/apple-icon/manifest/sitemap/robots; Next.js Pages Router; Gatsby `templates/`; Remix v2 + TanStack Router top-level + folder-route entries route/_layout/_index/index/__root; Expo Router `_layout` and `+*` files). All patterns prefixed with `**/` so they match in monorepo subpackages (`packages/web/src/app/.../page.tsx`). Real-world delta: amictus.ai 62 → 59 leaks (3 Next.js convention noise eliminated, 59 remaining are real React `Props`-pattern findings that warrant exporting the Props type). Companion logic in `suppress_signature_backing_types` removes any `unused_types` finding whose export is the type name backing a public signature in the same file, so exporting a backing type to fix one leak does not create a new "unused type" finding.
- **JSDoc visibility tags** (`@public`, `@internal`, `@beta`, `@alpha`, `@expected-unused`): exports annotated with any of these JSDoc/TSDoc tags are never reported as unused. Stored as a `VisibilityTag` enum on `ExportInfo` and `ExportSymbol`, preserving the semantic distinction (Public = API surface, Internal = sister packages/build tools, Alpha = early preview, Beta = unstable, ExpectedUnused = intentionally unused). Priority when multiple tags present: Public > Internal > Alpha > Beta > ExpectedUnused. Only `/** */` block JSDoc comments recognized. `@api public` TSDoc variant also supported. Word-boundary checked (e.g., `@internalizer` does not match). Survives incremental cache roundtrip via `u8` discriminant encoding. `@expected-unused` differs from other tags: when the tagged export IS actually used (has references from reachable modules), a stale suppression is reported instead of silently accepting it. This supports the pattern of marking API-completeness exports that become adopted over time.
- **Same-file value references and unused exports**: `unused-export` reports public export surface, not whether the local declaration is needed by another declaration in the same module. A value that is only referenced by another same-file export, for example `export const child = ...; export const parent = use(child);`, still reports as an unused export unless it has a reachable cross-file reference, a visibility tag such as `@public`, an `ignoreExports` rule, or `ignoreExportsUsedInFile` enabled. The generated fix action removes only the export modifier and leaves the local value available to `parent`. This keeps `dead-code`, `trace`, and stale suppression consumption aligned for local-only helpers and schema constants.
- **Stale suppression detection**: after all detectors run, `SuppressionContext` compares consumed vs. all suppression comments. Unmatched `// fallow-ignore-next-line` and `// fallow-ignore-file` comments are reported as stale. Default severity: `Warn`. CLI-side suppression kinds (complexity, coverage-gaps, feature-flag, code-duplication) are excluded from stale detection to avoid false positives, since their suppression checks run in the CLI layer, not in `find_dead_code_full`. **Suppressions for disabled rules** (`rules.<kind> == "off"`, including per-file `overrides.rules`) **are NOT reported as stale** (issue #482). `SuppressionContext::find_stale` resolves rules once per file via `ResolvedConfig::resolve_rules_for_path` and skips emission when the suppression's target kind has severity `Off`. The suppression documents intentional dormancy and becomes valid again when the rule is re-enabled. Blanket markers (`// fallow-ignore-next-line` with no kind) are not anchored to any specific dormant kind, so they still surface as stale when nothing matches. The `Suppression` struct stores `comment_line: u32` alongside the target `line: u32` so stale reports point at the comment itself, not the suppressed line. `@expected-unused` JSDoc tags produce stale entries via the unused export detector when the tagged export becomes used. Both comment and JSDoc tag stale entries feed into the same `stale_suppressions` vec on `AnalysisResults` with a discriminated `SuppressionOrigin` enum. **Partial-accept on unknown tokens** (issue #449): when a marker carries a multi-kind list and one token does not parse to any `IssueKind`, the recognized tokens are still recorded as suppressions and the unknown token is preserved on `ModuleInfo.unknown_suppression_kinds`. `SuppressionContext::find_stale` emits one extra `StaleSuppression` per unknown token, reusing `SuppressionOrigin::Comment` with `issue_kind: Some(<verbatim token>)`, `is_file_level` mirrored from the marker, and a new `kind_known: false` field (additive, skip-serialize-if `true`). `StaleSuppression::explanation()` branches on `kind_known`: stale-but-known kinds keep the prior `no <kind> issue found <scope>` copy; unknown tokens render `'<token>' is not a recognized fallow issue kind. Did you mean '<suggestion>'? Other tokens on this line still apply.` where `<suggestion>` comes from `closest_known_kind_name` (Levenshtein distance ≤ 2 against `KNOWN_ISSUE_KIND_NAMES` in `crates/types/src/suppress.rs`, dropped when no close match exists). Cache shape grows a sibling `CachedUnknownSuppressionKind` vec; `CACHE_VERSION` bumped 86 to 87. jq scripts in `action/jq/annotations-check.jq`, `action/jq/summary-check.jq`, and `ci/jq/summary-check.jq` branch on `kind_known == false` to emit unknown-kind annotation copy instead of "no longer matches any active issue."
- **Arrow-wrapped dynamic imports**: `React.lazy(() => import('./Foo'))`, `loadable(() => import('./Component'))`, `defineAsyncComponent(() => import('./View'))`, and any function call with an arrow or function expression argument whose body is a sole `import()` expression. The default export of the target module is credited as used (lazy loading wrappers always consume the default export). Handles expression body (`() => import('./x')`), block body (`() => { return import('./x'); }`), and function expressions (`function() { return import('./x'); }`). The import span is added to `handled_import_spans` to prevent duplicate side-effect edges from the generic `visit_import_expression` walker.
- **Route-callback dynamic imports**: object-literal properties named `component`, `loadChildren`, or `loadComponent` whose value is `() => import('./X')` (or a function-expression equivalent) credit the target module's default export as used, even when no `.then(m => m.default)` is spelled. Covers Angular Router's `loadChildren: () => import('./feature.routes')` and Vue Router's `component: () => import('./View.vue')`, where the framework consumes the module's default export internally. Implemented via `try_extract_property_callback_import` in `crates/extract/src/visitor/mod.rs`. Property names are an exact whitelist; other property names with the same shape (e.g., `loader: () => import('./X')`) are not credited. See issue #253.
- **Dynamic import `.then()` callback**: `import('./lib').then(m => m.foo)` tracks `foo` as a named import from `./lib`. The standard Angular lazy-loading pattern (`loadComponent: () => import('./x').then(m => m.Component)`). Three sub-patterns: (1) expression body member access (`m => m.foo`) extracts specific member names as `destructured_names`, (2) destructured parameter (`({ foo, bar }) => { ... }`) extracts names from the object pattern, (3) identifier parameter with block body (`m => { ... }`) registers as a namespace binding for member-access narrowing. Also handles `({ default: m.Foo })` object literal bodies (React.lazy `.then` pattern) and function expression callbacks. The import span is added to `handled_import_spans` to prevent duplicate side-effect edges. See issue #115.
- **Infrastructure entry points**: Dockerfiles, Procfiles, fly.toml scanned for source file refs. Searches root and config/docker/deploy subdirs.
- **TypeScript project references**: tsconfig.json `references` field discovered as workspaces (additive with npm/pnpm workspaces)
- **Nested packages under a bare glob-matched intermediate directory**: a single-level workspace glob (`packages/*`) matches a bare grouping directory (`packages/themes`) that has no `package.json` of its own, while the real package lives one level deeper (`packages/themes/my-theme/package.json`). `expand_workspace_glob_with_diagnostics` (`crates/config/src/workspace/parsers.rs`) previously skipped the bare directory (emitting only a `GlobMatchedNoPackageJson` diagnostic), so the deep package was never discovered, its files fell back to the root manifest, and its declared deps (e.g. `react`) surfaced as `unlisted-dependencies`. The expander now calls `recover_nested_packages` on a no-`package.json` glob match: it descends exactly one level and returns any child directory whose `package.json` loads AND declares a `name`, in the same `(path, canonical_path)` shape the discovery loop (`expand_patterns_to_workspaces` in `mod.rs`) already consumes, so each recovered child is registered as a normal workspace and feeds `ws_dep_map` for `find_unlisted_dependencies`. When at least one child is recovered the `GlobMatchedNoPackageJson` diagnostic is suppressed (no longer a dead end); when none is, the diagnostic still fires. Recovery is conservative to avoid sweeping in non-packages: `node_modules` and conventional skip-list directories (build output, caches, hidden dirs) are excluded, a child whose project-root-relative path matches the user's `ignorePatterns` (`is_ignored_workspace_dir`) is skipped so `ignorePatterns` stays a reliable opt-out, and a manifest without a `name` (fixtures, build artifacts, `__mocks__`) is rejected. Descent is one level only (deeper `packages/<group>/<sub>/<pkg>` layouts are left to recursive `**` globs, which go through `walk_workspace_dirs`, not this path). Note the nameless-rejection is intentionally stricter than the direct-glob path (`expand_patterns_to_workspaces` falls back to the directory name for a nameless matched workspace): a recovered nested package needs a stronger signal. A successful recovery emits a `tracing::debug!` nudge suggesting the one-level-deeper glob (`packages/*/*`-style) the package manager itself would need. The fix lives in workspace DISCOVERY rather than analysis-time dependency attribution, so a recovered nested package is one consistent owning package across all detectors: this is a behavior change beyond `unlisted-dependencies` (a package formerly attributed to the root now also participates in unused-files / unused-exports / boundary checks / internal-dependency crediting). See issue #842.
- **CI file scanning**: `.gitlab-ci.yml` and `.github/workflows/*.yml` scanned for binary invocations (npx, direct binaries) AND positional file arguments (`node scripts/deploy.ts`, `--config foo.cjs`). Binary names map to npm packages (CI-only deps stay flagged-as-used); file-path arguments are seeded as project-relative entry patterns so scripts invoked only from CI are not reported as `unused-files` (issue #195 Case D). Line-based YAML parser; known limitations: variable interpolation (`${{ matrix.env }}/deploy.ts`), `\` line-continuations, and YAML anchors (`<<: *defaults`) are silently skipped.
- **Script-referenced hidden directories**: the file walker's hidden-directory filter normally allows only `.storybook` / `.vitepress` / `.well-known` / `.changeset` / `.github`. When a `package.json#scripts` command references a path inside another hidden directory (e.g. `eslint -c .config/eslint.config.js`, `vitest --config .config/vitest.config.ts`, `tsx ./.scripts/build.ts`), `collect_script_hidden_dir_scopes` in `crates/core/src/discover/mod.rs` extracts the hidden segment names and contributes them to the per-package `HiddenDirScope` mechanism so the walker traverses them. Without this, the referenced file is detected as an entry point but never parsed, and its imports become phantom unused dependencies. Three guardrails: (1) only the structured outputs of `crate::scripts::parse_script` (`config_args` + `file_args`) are inspected, so a logging path like `.nx/cache/result.json` mentioned anywhere in a script body cannot pull `.nx/` into scope; (2) `SCRIPT_SCOPE_DENYLIST` excludes known build-cache / VCS / IDE / package-manager state directories (`.git`, `.next`, `.nuxt`, `.output`, `.svelte-kit`, `.turbo`, `.nx`, `.cache`, `.parcel-cache`, `.vercel`, `.netlify`, `.yarn`, `.pnpm-store`, `.docusaurus`, `.vscode`, `.idea`, `.fallow`, `.husky`) regardless of script content; (3) paths containing `..` segments are skipped to keep scopes from escaping a package root. Scopes are per-package: root scripts contribute a scope rooted at `config.root`, workspace scripts contribute scopes rooted at each workspace root. `collect_hidden_dir_scopes` is the combined entry point that loads each workspace's `package.json` once for both plugin-derived and script-derived scopes. A `tracing::debug!` line per inferred scope (`RUST_LOG=fallow_core=debug`) surfaces support diagnostics. See issue #358.
- **Production mode**: excludes test/dev files, only start/build scripts, skips unused devDeps, detects type-only deps. Config file exclusion (`*.config.*`) is root-anchored only; nested `*.config.*` files (e.g., Angular's `src/app/app.config.ts`) are preserved. All production exclude globs use `literal_separator(true)` so `*` cannot cross path separators. See issue #111.
- **Test-only dependency detection**: `find_test_only_dependencies` identifies production dependencies imported exclusively from test/dev/config files (should be devDependencies). Uses a dual classification: glob patterns from `PRODUCTION_EXCLUDE_PATTERNS` for test/story files (`**/*.test.*`, `**/*.spec.*`, `**/__tests__/**`, etc.) and the curated `is_config_file()` predicate for tooling config files at any depth. The predicate approach avoids false matches on application config files like `app.config.ts` while correctly matching workspace-level configs like `packages/foo/vitest.config.ts`. See issues #111, #112.
- **Platform built-in modules**: Node builtins (`fs`, `node:fs`, and explicit subpaths), Bun builtins (`bun` and `bun:*`), `cloudflare:`, `sass:` prefixed imports, Deno `std`, and k6 runtime modules (`k6` and `k6/*`) are recognized as builtins and never flagged as unlisted dependencies or unresolved imports. The Bun guard is exact for bare `bun` plus the `bun:` namespace, so real packages like `bun-types`, `@types/bun`, `bunyan`, and `bun/*` subpaths remain normal dependencies. The k6 guard is exact for bare `k6` plus the slash namespace, so `k6-tools`, `k6ish`, `k6x/http`, and `@types/k6` remain normal dependencies. The `sass:` prefix covers the Sass compiler's built-in modules (`sass:math`, `sass:string`, `sass:color`, `sass:list`, `sass:map`, `sass:meta`, `sass:selector`) imported via SCSS `@use 'sass:math'`. **Mandatory-`node:`-prefix builtins** (`sqlite`, `sea`, `test`, `test/reporters`) are classified as builtins ONLY when written with the `node:` prefix (`node:sqlite`, `node:test`), because Node refuses to resolve them as bare specifiers and real npm packages share those bare names (`sqlite`, `better-sqlite3`, the `test` package). The bare forms (`sqlite`, `test`, `test/reporters`) are therefore treated as ordinary npm dependencies; `is_builtin_module` keeps these in `NODE_PREFIX_ONLY_BUILTINS` rather than the always-available `builtins` list. `extract_package_name` retains the `node:` prefix on the package-usage key (`node:sqlite` has no `/`), so the single predicate gate covers both the unlisted-dependency and unresolved-import paths. See issues #104, #625, #627, #642, and #634.
- **Known-tooling catalogue (data-driven)**: dev tools used through the CLI or config rather than imported in source (`typescript`, `prettier`, `husky`, `@types/*`, `oxlint`, etc.) are never reported as unused devDependencies. `is_known_tooling_dependency` (`crates/core/src/plugins/tooling.rs`) is the single non-plugin-specific source for this, and its prefix + exact lists now live in `crates/core/data/tooling.toml`, embedded via `include_str!` and parsed once at startup behind a `OnceLock` (no regen step; add a tool by editing one TOML entry). Parse tests reject empty/whitespace prefixes, duplicates, and framework-plugin entries (`vite-plugin-*` / `prettier-plugin-*` / `eslint-plugin-*` / `@rollup/plugin-*`, including scoped tails like `@ianvs/prettier-plugin-sort-imports`). Framework-plugin packages were removed from the catalogue precisely because the exact-name match was a false negative: a `vite-plugin-svgr` or `prettier-plugin-tailwindcss` declared in devDependencies but never wired into the config was silently treated as used. They are now credited only when they actually appear: vite plugins via the normal import graph on the discovered `vite.config.*` (any extension, including `.cjs` `const x = require(...)`, because the config file is itself a parsed source file), and prettier plugins via the Prettier config parser (including the newly-parsed `.prettierrc.{yml,yaml,toml}` `plugins` array, since non-JS configs are invisible to the import graph). A declared-but-unused framework plugin now correctly surfaces as `unused-dev-dependency`. See issue #462.
- **`ignoreDependencies` dual suppression**: listed packages are excluded from both unused dependency AND unlisted dependency detection, making it useful for runtime-provided packages (e.g., `bun:sqlite`).
- **`ignoreUnresolvedImports` raw specifier suppression**: listed glob patterns suppress only `unresolved-import` findings whose raw import specifier matches. They do not affect dependency usage or resolver behavior. `@example/icons/**` matches subpaths but not the bare `@example/icons`, so users must list both when both are expected; parent-relative generated specifiers such as `../generated/**` are valid because the field is not a filesystem glob. Broad patterns such as `**` can hide real missing modules. See issue #726.
- **@types/ unlisted dep suppression**: imports from `pkg` not flagged as unlisted when `@types/pkg` is in deps (TypeScript resolves types from `@types/` and erases the import regardless of `import type` syntax). Scoped packages use DefinitelyTyped convention (`@scope/pkg` → `@types/scope__pkg`).
- **Internal workspace packages treated as first-class deps** (issue #228): a workspace package name (e.g., `@repo/ui`) appearing in another workspace's `dependencies` is checked the same way an external npm package is. `credit_workspace_package_usage` in `crates/core/src/lib.rs` walks resolved imports and re-exports, and for each bare specifier whose resolved target is `ResolveResult::InternalModule` AND whose extracted package name matches a workspace, credits `graph.package_usage` (and `type_only_package_usage` when the import is type-only). Effects: declared-but-unimported workspace deps surface in `unused_dependencies`; imports of a workspace package from a workspace whose own `package.json` does not list it surface in `unlisted_dependencies`. Workspace self-references stay allowed because `find_unlisted_dependencies` inserts each workspace's own name into its dep set before per-file scoping. External plugins also activate per-workspace via `process_external_plugins` inside `run_workspace_fast`, so a workspace dep that enables a custom plugin (`fallow-plugin-*` definitions, inline framework configs) keeps the plugin's `alwaysUsed` and `toolingDependencies` honored for that workspace.
- **Architecture boundary violations**: user-defined zones (glob patterns) with directional import rules. Fires at direct import site using resolved target's zone. Unzoned files are unrestricted in both directions. First-match zone classification. Self-imports always allowed. Inline and file-level suppression supported. Built-in presets: `layered` (presentation/application/domain/infrastructure), `hexagonal` (adapters/ports/domain), `feature-sliced` (app/pages/widgets/features/entities/shared), `bulletproof` (app/features/shared/server, shared zone covers components, hooks, lib, utils, utilities, providers, shared, types, styles, i18n). Presets auto-detect `rootDir` from tsconfig.json for pattern prefix. User zones/rules merge on top of presets (same-name replaces). Zones with zero file matches emit warnings. **`autoDiscover`** (issue #368): a zone declaring `autoDiscover: ["src/features"]` expands at config-resolve time (`expand_auto_discover` in `crates/config/src/config/boundaries.rs`) into one child zone per immediate child directory, named `<parent>/<child>` (e.g. `features/auth`, `features/billing`). Rules referencing the parent (`from: "features"`) expand to each discovered child; explicit child rules (`from: "features/auth"`) override generated parent rules via `dedupe_rules_keep_last` regardless of declaration order. **Bulletproof preset uses autoDiscover for the `features` zone with empty `patterns`** so a top-level `src/features/index.ts` barrel stays unclassified (unrestricted), preventing false-positive `features -> features/<child>` violations when the barrel re-exports children; the trade-off is that other top-level files in `src/features/` (e.g. `features/types.ts`) are also unrestricted. Pinned by `bulletproof_top_level_features_file_is_unrestricted` in `crates/core/tests/integration_test/boundary_violations.rs`. A user-defined zone setting BOTH `patterns` and `autoDiscover` triggers a once-per-process `tracing::warn!` because the parent fallback typically produces false positives on barrels; the warn points at the strict-mode escape hatch. Follow-up #372 tracks differentiated rule expansion that would let the parent rule auto-allow discovered children without re-introducing sibling-feature leaks. **`allowTypeOnly`** (issue #365): a per-rule `Vec<String>` mirroring `allow`'s shape. When `featureB` has `allow: [], allowTypeOnly: ["featureA"]`, type-only imports from `featureB` to `featureA` are admitted: `import type {...}`, `import type * as ns`, inline `import { type Foo }` where every named specifier is type-qualified, AND type-only re-exports `export type { Foo } from "..."`. The predicate is `edge.symbols.iter().all(|s| s.is_type_only)`, identical to cycle detection's exclusion of type-only edges. Mixed-specifier imports (`import { type Foo, Bar }`) still fire because the runtime dependency on `Bar` is real. Type-only re-exports surface as `SideEffect` ImportedSymbols carrying the re-export's `is_type_only` flag (`build.rs:109-118`), so the same predicate covers them; the boundary detector does NOT special-case the import-vs-re-export distinction. Implementation lives in `ResolvedBoundaryConfig::is_type_only_allowed` plus `ModuleGraph::outgoing_edge_summaries` (single-pass walk over the edge range returning target + all-type-only + first-span-start). `tracing::debug!` line on every allowed crossing for support diagnostics; no human-facing log. No preset defaults the field on. Strictly additive via `#[serde(default, skip_serializing_if = "Vec::is_empty")]`. Test matrix at `crates/core/tests/integration_test/boundary_violations.rs::allow_type_only_*` plus the `boundary-type-only` fixture exercising whole-decl, inline-type, namespace-type, mixed, side-effect, plain-value, and type-only re-export shapes.
- **Boundary forbidden-call policy** (issue #1147): `boundaries.calls.forbidden` bans callee patterns per zone (`{ "from": "domain", "callee": "child_process.*" }`; `callee` is a string or array). The extract layer captures every statically flattenable callee path once per module (first occurrence wins) on the config-blind `ModuleInfo.callee_uses` channel (persisted through the extraction cache; `CACHE_VERSION` 146 to 147), so warm caches stay correct when the policy is enabled later. The detector (`crates/core/src/analyze/boundary_calls.rs`) classifies each reachable-or-entry file into a zone (first-match, same as import rules) and matches the zone's patterns against the written callee path AND an import-resolved canonical path: named/namespace/default imports from bare specifiers canonicalize to `<package>.<member>` with the `node:` prefix stripped, so one `child_process.*` pattern covers `import { execSync } from "node:child_process"`, the bare specifier, and `cp.exec(...)` namespace forms; globals like `console.*` match on the written path. Relative/root-relative/`#`-alias sources and type-only imports never canonicalize (zone-to-zone calls are the import rules' job). Matching reuses the security catalogue's segment-aware `CalleePattern`, extended with trailing-`*` prefix matching (`child_process.*` matches `child_process.exec`, never bare `child_process`); no catalogue row uses the trailing form, so security behavior is unchanged. Unzoned files are unrestricted; a rule whose `from` zone classifies zero files emits a `tracing::warn!` naming the silent-zero cause. Optional-chaining call sites (`cp?.exec()`) ARE captured: oxc keeps static member nodes inside chain expressions, so they flatten to the same dotted path as the non-optional form (pinned by `captures_optional_chaining_callee`). Documented false negatives (syntactic check): computed members, dynamic dispatch, re-bound callees (`const run = cp.exec; run()`), and `Function.prototype.call/apply`. Inert patterns (empty, bare `*`, glob-like fragments, mid-path or double wildcards) are rejected at config load via `validate_call_rules`; unknown `from` zones fail like other zone references. Findings reuse `rules.boundary-violation` severity, per-file overrides, and the `boundary-violation` suppression token (file- and line-level; the rule-id-shaped `boundary-call-violation` token is accepted as an alias for the whole family); one finding per unique (file, callee path), SARIF rule id `fallow/boundary-call-violation`.
- **Declarative rule packs** (issue #1148): `rulePacks` lists standalone JSON/JSONC pack files (`{ version: 1, name, rules: [...] }`) of `banned-call` / `banned-import` rules, loaded inside `FallowConfig::resolve()` via `fallow_config::load_rule_packs` (validation also runs as a hard gate in `runtime_support::load_config_for_analysis` and `core::config_for_project`, exit 2 on any error: unknown kind, missing file, inert callee pattern, glob traversal, duplicate rule id within a pack, duplicate pack name, version != 1, cross-kind fields, glob specifiers, ambiguous pack or rule identifiers). The evaluator (`crates/core/src/analyze/policy/`) reuses the EXISTING extraction surfaces: `banned-call` matches `ModuleInfo.callee_uses` (one finding per unique callee path per file, first occurrence wins, literal-only and zero-arg calls included) against the written path AND the boundary-calls `canonical_callee_path` import-resolved form, so `child_process.*` covers named/namespace/default imports of `child_process` / `node:child_process` while a same-named LOCAL `exec` with no matching import never fires; `banned-import` matches the RAW specifier segment-aware (`moment` covers `moment/locale/nl`, never `moment-timezone`; `npm:moment` and aliases are documented misses) over `imports` + `re_exports` (`require()` and dynamic `import()` are documented v1 false negatives), with `ignoreTypeOnly` skipping fully type-only imports/re-exports. Severity model: per-file master = `resolve_rules_for_path(path).policy_violation` (default `warn`, per-file `overrides` apply, `off` = kill switch per file or globally); rule-level `severity` overrides per finding (`off` disables one rule); emitted findings carry effective `error`/`warn`, the CLI exit gate reads the per-finding value (`check/rules.rs::has_error_severity_issues`), and strict runs promote per-finding warns via `promote_policy_finding_warns`. Suppression: scoped `policy-violation:<pack>/<rule-id>` tokens target one rule-pack identity for line+file forms, while bare `policy-violation` and `policy-violations` keep suppressing the whole policy family at that scope. Scoped tokens are consumed only by matching `(pack, rule_id)` findings; unmatched scoped tokens can become stale unless the master policy rule or target rule is disabled. A `files`-scoped rule whose globs match no analyzed file emits a `tracing::warn!` (mirrors the boundary zero-zone warn). SARIF uses the static `fallow/policy-violation` rule id with `<pack>/<rule-id>` in the message and a `policyRule` property; per-rule SARIF rule synthesis remains out of scope.
- **Static test coverage gaps**: module graph reachability from test entry points to runtime files/exports. Not line-level coverage. Test files are identified by plugin patterns and production-mode exclusion rules. Reports runtime files with no test dependency path and runtime exports with no test-reachable reference chain. Gated by `--coverage-gaps` flag on the health command and `coverage-gaps` rule severity.
- **Complexity suppression**: health command complexity findings (functions and synthetic Angular template entries exceeding cyclomatic/cognitive thresholds) support inline suppression via `// fallow-ignore-next-line complexity` and `// fallow-ignore-file complexity`. The blanket `// fallow-ignore-next-line` (no kind) also suppresses complexity findings on the next line. The check happens in `collect_findings()` in `crates/cli/src/health/mod.rs` using `suppress::is_suppressed()` with `IssueKind::Complexity` (discriminant 18). File scores and vital signs are NOT affected by suppressions (they reflect actual complexity, not alerting). Synthetic Angular `<template>` findings in `.html` files get file-level suppression actions because line comments cannot be expressed in HTML; inline template findings in `.ts` files keep line-level suppression actions anchored at the decorator. See issue #108 (till's comment).
- **Synthetic Angular `<template>` complexity**: `health --complexity` emits synthetic `<template>` findings for Angular template control-flow density. External `templateUrl` files (`./foo.component.html`) are scanned at `parse_html_to_module_with_complexity` time and report against the `.html` file's coordinates. Inline `@Component({ template: \`...\` })` decorators are captured during the visitor pass (`ModuleInfoExtractor::inline_template_findings`) and synthesised after the visit by `parse::append_inline_template_complexity`, with the finding's line/col remapped onto the host `.ts` file's `@Component`/`@Directive` decorator start so jump-to-source lands on the decorator. This anchoring also lets `// fallow-ignore-next-line complexity` placed directly above the decorator suppress through the existing health-side check, with no special-case suppression plumbing. The CLI emits `suppress-line` actions for inline-template findings (matching JS/TS function complexity) instead of the HTML `suppress-file` action used for external templates. Template literals containing `${...}` expressions and `template:` properties bound to a variable are skipped (out of scope for the first cut). **CRAP scoring on external `templateUrl` findings inherits from the owning `.component.ts`** (`build_template_inherit_contexts` in `crates/cli/src/health/scoring.rs`): for each `.html` module carrying a `<template>` entry, the scoring loop walks `graph.reverse_deps` to enumerate every non-test `.ts` importer, ORs their `is_test_reachable` bits, unions their `test_referenced_exports`, picks the first test-reachable owner for human-output provenance, and runs `compute_crap_scores_estimated` against that combined context with `CoverageSource::EstimatedComponentInherited`. The resulting per-function CRAP carries a `coverage_source: "estimated_component_inherited"` discriminator and an `inherited_from: "<owner>.component.ts"` provenance field; the human formatter renders `(inherited from foo.component.ts)` after the CRAP score; the JSON `increase-coverage` action targets the `.ts` owner path via a new `target_path` field so AI agents add tests on the component rather than scaffolding tests against the structurally untestable `.html`. Score itself does not move for the common case (BFS already cascades reachability through the `templateUrl` `SideEffect` edge), so existing baselines and `--max-crap` gates are unaffected. Tier 2 (AOT source-map back-mapping) will introduce a `measured_aot_source_map` variant on `CoverageSource`. See issues #183, #186, #187.
- **Synthetic Vue/Svelte SFC `<template>` complexity**: `health --complexity` emits a synthetic `<template>` finding per `.vue` / `.svelte` file from template control-flow density, mirroring the Angular path so a template-heavy SFC is no longer scored as artificially simple (previously only the SFC `<script>` JS functions were scored; the template control flow contributed zero). Health SIGNAL only, NO new rule/IssueKind/threshold/output surface: the entry rides the existing `FunctionComplexity` shape (`name == "<template>"`, `contributions: Vec::new()`), folds into `aggregate_complexity` -> health score + hotspots + `--complexity` findings, and reuses the existing `maxCyclomatic`/`maxCognitive` thresholds and the `complexity` suppression kind. Computed in `sfc::parse_sfc_to_module` under `need_complexity` (AFTER the script merge + `apply_template_usage`) by `template_complexity::compute_vue_template_complexity` / `compute_svelte_template_complexity`, appended to `combined.complexity` alongside the script functions. Each scanner masks `<script>`/`<style>`/`<!-- -->` (replaced with equal-length spaces, so SFC `<script>` control flow is NOT double-counted; it stays owned by `translate_script_complexity`) before scanning the template region. Vue counts `v-if`/`v-else-if`/`v-for`/`v-show` (incl. `<template v-for>`) as control flow with nesting from the HTML tag stack and scores bound-directive (`:x`/`@x`/`v-bind`/`v-on`/`v-model`/`v-html`/`v-text`/`v-memo`) values + `{{ }}` interpolations as expressions; Svelte counts `{#if}`/`{:else if}`/`{#each}`/`{#await}`/`{:then}`/`{:catch}`/`{#key}` with nesting from the logic-block stack and scores block expressions + `{ }` interpolations + `{@const}`/`{@html}`/`{@render}`/`{@debug}` expressions. A bare `v-else` / `{:else}` is a continuation (cognitive +1, no cyclomatic increment, the new branch path is owned by the paired `v-if` / `{#if}`), matching Angular's bare `@else`; `{:else if}` / `{:then}` / `{:catch}` are branches (cyclomatic +1, flat cognitive +1). Drops the trivial baseline (`cyclomatic == 1 && cognitive == 0`) like Angular, and returns `None` on any malformed-template `ScanError` (no finding, no panic). Byte-safe over multibyte template text. `CACHE_VERSION` bumped 176 -> 177 (a warm cache from 176 lacks the SFC `<template>` entry until re-parse). The `FunctionComplexity` shape is unchanged (only an extra Vec element), so no size-assertion change.
- **CRAP score threshold**: `--max-crap` (health and audit, default 30.0, `health.maxCrap` in config) compares each function's CRAP score (`CC^2 * (1 - cov/100)^3 + CC`) against the threshold. Per-function CRAP is produced as a side effect of file scoring (`compute_file_scores` in `scoring.rs`) and merged into `HealthFinding` by `merge_crap_findings` in `health/mod.rs`. Uses Istanbul per-function coverage when `--coverage` resolves; falls back to a graph-based estimator (directly test-referenced = 85%, indirectly reachable = 40%, untested = 0%). Findings triggered by CRAP alone, or alongside cyclomatic/cognitive, carry populated `crap` and (when Istanbul matched) `coverage_pct` fields and share the `complexity` suppression kind. See issue #141.
- **Complexity contribution breakdown** (`--complexity-breakdown`): the `ComplexityVisitor` (`crates/extract/src/complexity.rs`) records one `ComplexityContribution { line, col, metric, kind, weight, nesting }` per increment event during the existing single AST pass, carried on `FunctionComplexity.contributions` (cached via bitcode; `CACHE_VERSION` 119) and surfaced on each `ComplexityViolation.contributions` in `health --format json` only when `--complexity-breakdown` is set (gated at population in `collect_findings` / `merge_crap_findings` via `contributions_for`; default output omits the field via `skip_serializing_if`). `inc_cyclomatic` / `inc_cognitive_with_nesting` / `inc_cognitive_flat` / `handle_logical_operator` fuse record-and-increment so the breakdown can never drift from the aggregate (`sum(cyclomatic weights) + 1 == cyclomatic`, `sum(cognitive weights) == cognitive`, asserted by `contributions_reconstruct_aggregate_metrics`). The else-if cognitive path was refactored from "+1+nesting then subtract nesting" to a flat `+1` recorded as `ElseIf`; this is metric-neutral (verified byte-identical OLD-vs-NEW across all benchmark fixtures). React JSX depth is descriptive `react_jsx_max_depth` context only, so current extraction does not emit `jsx-depth` entries. Angular `<template>` synthetic findings carry an empty breakdown (out of scope). The VS Code extension renders the breakdown as per-line inline decorations + hover; the MCP `check_health` tool exposes it via `complexity_breakdown`. CRAP coverage amplification is explained editor-side from existing fields (no new Rust data).
- **Script binary-to-package resolution**: npm `scripts` binary names are mapped to package names via a 4-step chain: (1) static `BINARY_TO_PACKAGE` table for known divergences (`tsc` -> `typescript`, `ng` -> `@angular/cli`, etc.), (2) `node_modules/.bin/<binary>` symlink target path extraction, (3) dynamic reverse map built from each dependency's `package.json` `bin` field (handles both string `"bin": "./cli.js"` and object `"bin": {"attw": "./cli.js"}` forms), (4) identity fallback (binary = package name). The dynamic map probes `node_modules/` at both the project root and each workspace root, handling non-hoisted setups (pnpm strict, Yarn workspaces). Built once per analysis run. See discussion #107. Positional file arguments (`tsc ./scripts/deploy.ts`, `node ./scripts/deploy.js`) extracted from script values are seeded as entry patterns to keep CI-only / deploy-only sources reachable. Extensionless script file paths also probe `entry/index.*` after exact-file and source-extension fallback, matching Node directory entry behavior for script shapes like `node scripts/process-messages`. For workspace packages, paths are joined with the workspace's project-relative prefix and `..` segments are normalized via `normalize_script_entry_pattern`; `..` segments that escape the project root are skipped instead of producing unmatchable globs (issues #195 Case C and #638). `bun` is handled as both a script runner and a direct executor: leading boolean runtime flags (`--bun`, `--watch`, `--hot`, `--smol`, `--no-clear-screen` in `BUN_RUNTIME_FLAGS`) are skipped so `bun --bun prek install` extracts and credits the `prek` binary (resolving `prek` -> `@j178/prek` through the dynamic bin map), while `bun --watch run dev`, bare `bun <script>`, and any unrecognized leading flag still resolve to script delegations (conservative: an unknown flag may take a value, so it is never guessed at). `bun x <pkg>` is treated as the bun-native alias of `bunx`. See issue #755.
- **Vite `css.preprocessorOptions.{scss,sass,less,stylus}.additionalData`**: the Vite plugin extracts the string value at each preprocessor's `additionalData` key from `vite.config.{ts,js}` and scans it for stylesheet imports via the same regex used by the CSS file parser. SCSS / Sass bodies seed discovered local style paths as entry patterns and use SCSS-aware normalization (extensionless local specifiers become `./X` partials); package imports such as `bootstrap` and `bootstrap/scss/functions`, plus Tailwind CSS `@plugin "pkg"` directives, are credited as referenced dependencies instead of inert entry globs when no matching local style candidate exists. Less / Stylus strings are parsed for `@import "path"` and package dependency credit, but local `.less` / `.stylus` files are not currently part of the discovered source-file set. Function-form `additionalData: (source, file) => ...` is silently skipped (out of static-analysis scope). Specifiers are stripped of leading `./` before push because globset matches against project-relative paths without that prefix. See issue #195 Case A.
- **Path-helper calls in bundler entry config** (issue #604): `config_parser::expression_to_string_or_array` (the shared extractor behind `build.rollupOptions.input`, `build.lib.entry`, Webpack/Rspack/Rsbuild/Rolldown `entry`, Rollup `input`, plus Vitest `setupFiles`/`globalSetup` and Drizzle `schema`) evaluates leaf values through `expression_to_path_string`, so entries written as path-helper calls (`resolve(__dirname, "src/app.ts")`, `path.resolve(...)`, `join(...)`, `fileURLToPath(...)`, `new URL(...)`) are extracted instead of dropped. The change is additive: `expression_to_path_string` falls through to `expression_to_string` for plain string/template literals, so string-literal entries behave exactly as before. The `__dirname` and `import.meta.dirname` leading anchors are dropped (`is_dirname_anchor`), yielding config-directory-relative patterns; CSS inputs (`styles: resolve(__dirname, "src/index.css")`) are preserved like any other entry because entry patterns match the project file index regardless of extension. Non-literal arguments (`resolve(someVar, "x")`) are not statically recoverable and drop the whole call. The same `expression_to_path_string` powers `resolve.alias` replacement extraction, so the `import.meta.dirname` anchor support also lets `path.resolve(import.meta.dirname, ...)` aliases resolve.
- **Cypress `e2e.specPattern` / `component.specPattern` / `supportFile`**: the Cypress plugin reads `cypress.config.{ts,js,mjs,cjs}` and seeds `e2e.specPattern`, `component.specPattern`, `e2e.supportFile`, and `component.supportFile` (string or array of glob patterns / paths) as entry patterns. Static patterns under `**/*.cy.{ts,tsx,js,jsx}`, `cypress/**/*.{ts,tsx,js,jsx}`, and `cypress/support/**/*.{ts,js}` seed Cypress's default component spec discovery and default Cypress 9-style layout; the config-driven extraction handles Cypress 10+ projects whose specs live outside `cypress/` (e.g. `tests/**/*.cy.ts`, `src/**/*.cy.ts`). See issue #195 Case E.
- **Script multiplexer handling**: `concurrently`, `npm-run-all`, `npm-run-all2`, `run-s`, `run-p`, `run-s2`, `run-p2` are recognized as script multiplexers. Their positional arguments are treated as script names (not binary names or file paths) and excluded from `used_packages`. Only the multiplexer binary itself is recorded. This prevents false "unused dependency" reports for packages referenced only via `npm:` prefixed script delegation (e.g., `concurrently "npm:dev" "npm:test"`).
- **pnpm bare binary script credit**: package.json scripts, workspace scripts, and CI run blocks use dependency context to disambiguate `pnpm <token>`. If `<token>` resolves to a declared package name, a known binary mapping, a `node_modules/.bin` symlink target, or a dynamic bin-map entry, fallow credits that package as script-used. Local script names and pnpm built-ins remain script delegation or package-manager commands and are not credited, including `pnpm build`, `pnpm test`, `pnpm start`, `pnpm install`, `pnpm audit`, and `pnpm add`. The public `parse_script` parser stays syntax-only and still skips bare `pnpm <token>` without dependency context. See issue #914.
- **Invalid client exports** (`invalid-client-export`, C.1): a source-file finding for a `"use client"` file (exact-match directive in `ModuleInfo.directives`) that ALSO exports a Next.js server-only / route-segment config name Next.js rejects at build time. Detector in `crates/core/src/analyze/invalid_client_exports.rs::find_invalid_client_exports`, wired into `find_dead_code_full` AFTER the parallel detectors (it is a sequential populate like the catalog/override findings, because it needs the run's `declared_deps`). GATED on the project declaring `next` (`declared_deps.contains("next")`); without Next.js the directive and these names carry no special meaning, so the rule returns empty (no false positives). For each non-type-only export whose rendered name is in `ILLEGAL_CLIENT_EXPORTS` (`metadata`, `generateMetadata`, `viewport`, `generateViewport`, `generateStaticParams`, `dynamic`, `dynamicParams`, `revalidate`, `fetchCache`, `runtime`, `preferredRegion`, `maxDuration`, `getServerSideProps`, `getStaticProps`, `getStaticPaths`, and the route HTTP methods `GET`/`POST`/`PUT`/`PATCH`/`DELETE`/`HEAD`/`OPTIONS`), one finding is emitted. The `default` export is the client component itself and is NEVER flagged. Default severity `warn`. The finding stores the ABSOLUTE path internally (graph node path) and strips root at JSON serialization via `serde_path::serialize`, same as `UnusedExport.path`. There is no safe AUTO-fix (removing a legitimate client export would break the component), so the JSON `actions` are a manual `move-to-server-module` fix (move the server-only export to a non-client module) plus a `// fallow-ignore-next-line invalid-client-export` suppress, both `auto_fixable: false`. Suppression routes through `SuppressionContext` with `IssueKind::InvalidClientExport` (discriminant 30). SARIF rule id `fallow/invalid-client-export`.
- **Mixed client/server barrels** (`mixed-client-server-barrel`, capability B): a finding on a BARREL file (a module with at least one `export ... from` re-export) that re-exports BOTH (a) a `"use client"` origin module AND (b) a SERVER-ONLY origin module. Importing one name from such a barrel drags the other's directive context across the React Server Components boundary (the Next.js App Router footgun). Detector in `crates/core/src/analyze/mixed_barrel.rs::find_mixed_client_server_barrels`, wired into `find_dead_code_full` after `invalid_client_exports` (sequential populate, needs the run's `declared_deps` and the resolved modules). GATED on `predicates::project_uses_rsc_directives(declared_deps)` (W4.3: any RSC bundler, not just `next`: `waku`, `@lazarv/react-server`, `react-server-dom-{webpack,vite,parcel}`, `@vitejs/plugin-rsc`, since the mixed-barrel boundary footgun is universal RSC). Classification is over DIRECT re-export origins only: each `ResolvedReExport` source is resolved one hop to its origin `ModuleInfo` via `target.internal_file_id()`; origins that do not resolve to a LOCAL source module (npm packages, unresolved) are ignored, which naturally sidesteps the `react-server` conditional-exports concern (it only affects npm package re-exports). Transitive re-export chains are NOT walked (keeps it precise). The server-only predicate is the SHARED `crate::analyze::server_only::is_server_only_module` (`"use server"` directive OR an import of `server-only` / `next/server` / `node:fs` / `node:child_process` (and bare forms) OR a named server-only `next/headers` API), factored out of the security detector so the definition never drifts. FP guards: (1) the trigger is client + SERVER-ONLY, NEVER client + plain-util (a barrel re-exporting a client component and an ordinary undirected utility does NOT flag); (2) type-only re-exports (`export type { X } from './client'`, `ReExportInfo.is_type_only`) are skipped (erased at build, no runtime directive context). One finding per barrel, anchored at the earlier of the two offending re-export spans, naming the first client origin and first server-only origin (the source specifiers as written). Default severity `warn`. The finding stores the ABSOLUTE path internally and strips root at JSON serialization via `serde_path::serialize`. No safe AUTO-fix (splitting a barrel is a human decision), so the JSON `actions` are a manual `split-mixed-barrel` fix plus a `// fallow-ignore-next-line mixed-client-server-barrel` suppress, both `auto_fixable: false`. Suppression routes through `SuppressionContext` with `IssueKind::MixedClientServerBarrel` (discriminant 31). SARIF rule id `fallow/mixed-client-server-barrel`.
- **Misplaced directives** (`misplaced-directive`): a source-file finding for a `"use client"` / `"use server"` string that sits AFTER a non-directive statement (an `import`, a `const`), so it is NOT honored. Next.js (and every RSC bundler) honors the directive only in the leading PROLOGUE; oxc places honored directives in `program.directives`, but the moment any statement precedes the string oxc parses it as an ordinary string-literal ExpressionStatement in `program.body` and the bundler SILENTLY treats the file as a server module, the exact opposite of the developer's intent. The extract layer records every such body-position `"use client"`/`"use server"` string on `ModuleInfo.misplaced_directives` (`Vec<MisplacedDirectiveSite>`, 8 bytes; `CACHE_VERSION` 151 to 152); a correctly-positioned leading directive lands in `program.directives` and is NEVER captured (the structural FP guard, verified by an extract unit test `leading_use_client_is_a_prologue_directive_not_misplaced` plus an integration test). Detector in `crates/core/src/analyze/misplaced_directive.rs::find_misplaced_directives`, wired via `populate_misplaced_directive_findings` and GATED on BOTH the rule being enabled (`config.rules.misplaced_directive != Off`) AND `predicates::project_uses_rsc_directives(declared_deps)` (W4.3: widened beyond `next` to any RSC bundler that honors the directive prologue: `waku`, `@lazarv/react-server`, `react-server-dom-{webpack,vite,parcel}`, `@vitejs/plugin-rsc`, since a misplaced directive is silently dropped by all of them). One finding per occurrence, resolved to `(line, col)` via `byte_offset_to_line_col`, anchored at the offending statement, with the directive string rendered verbatim into the finding's `directive` field. Default severity `warn`. No safe AUTO-fix (moving a directive can change module semantics, a human decision), so the JSON `actions` are a manual `hoist-directive` fix plus a `// fallow-ignore-next-line misplaced-directive` suppress, both `auto_fixable: false`. Suppression routes through `SuppressionContext` with `IssueKind::MisplacedDirective` (discriminant 32); serde accepts the kebab singular and the `misplaced-directives` plural alias. SARIF rule id `fallow/misplaced-directive`.
- **Route collisions + dynamic-segment name conflicts** (`route-collision`, `dynamic-segment-name-conflict`): two `next`-gated whole-project App Router checks (both default to `error`: `route-collision` because it mirrors a `next build` failure, so a project hitting it was already red; `dynamic-segment-name-conflict` because it is a deterministic runtime crash on first request that `next build` lets through, so fallow is the only gate that fails on it, graduated from `warn` to `error` after a 22-project corpus proved it false-positive-free) built on a SHARED path-only route-tree primitive (`crates/core/src/analyze/route_tree.rs::classify_route_file`), wired via `populate_route_collision_findings` / `populate_dynamic_segment_name_conflict_findings` after the misplaced-directive populate (they receive `workspaces`). The primitive is PURE FILE-PATH analysis: no AST, no imports, no type info, no extraction (so NO `CACHE_VERSION` bump), reading every file's absolute path from `graph.modules`. It anchors each route file on a DISCOVERED package root (project root + `workspaces[].root`) at `<pkgRoot>/app` or `<pkgRoot>/src/app`, NEVER a bare `app` dirname, decomposes the directory segments, and returns `None` for non-route files, files under a private `_folder`, and files under an intercepting marker (`(.)`/`(..)`/`(...)`). Route files are recognized by basename STEM (`page`/`route` = URL leaves; `layout`/`loading`/metadata/etc. = decorators that still prove a directory) plus a route-capable extension allowlist (`ts/tsx/js/jsx/mjs/cjs/mts/cts/mdx/md`), so `page.test.tsx` / `page.module.css` are rejected and most custom `pageExtensions` work (exotic ones fail toward false-negative). **`route-collision`** (`crates/core/src/analyze/route_collision.rs`): buckets URL-leaf files by `(app_root, slot_key, url)` where route groups `(x)` are stripped, parallel `@slot` segments fork into `slot_key` (so two leaves in different slots never collide), and dynamic segments keep their written name (`[id]` stays distinct from `[slug]`, so a differing-name case is the sibling detector's job, never a double-report). `page` and `route` share ONE URL-owner namespace (two pages, two route handlers, or a page+route at one URL all collide). A bucket of size >= 2 emits one finding per file with `conflicting_paths` (path-sorted) listing the others. Per-app-root scoping is the load-bearing FP gate (a Turborepo with `apps/web/app/about/page.tsx` + `apps/admin/app/about/page.tsx` both at `/about` is NOT flagged). **`dynamic-segment-name-conflict`** (`crates/core/src/analyze/dynamic_segment_name_conflict.rs`): groups every route file's dynamic occurrences by `(app_root, slot_key, parent-position)` and flags any position carrying >= 2 distinct spellings; EVERY convention file participates (a `layout.tsx` under `[id]` proves the `[id]` dir exists), emitting one finding per involved file with `conflicting_segments`. Both are file-level findings anchored at line 1; the PRIMARY action is manual guidance (`FixActionType::ResolveRouteCollision` / `ResolveDynamicSegmentNameConflict`, `auto_fixable: false`) because suppressing the error never fixes it, with a file-level suppress (`// fallow-ignore-file route-collision` / `dynamic-segment-name-conflict`, plural aliases accepted) as escape hatch. `IssueKind` discriminants 35 / 36. SARIF rule ids `fallow/route-collision` / `fallow/dynamic-segment-name-conflict`. **VERIFIED against Next 15.5.0 by real `next build` + `next dev` (2026-06-13, probe in /tmp/nbp):** route-collision IS a hard `next build` error ("You cannot have two parallel pages that resolve to the same path"), INCLUDING the `route.ts`-vs-`page.tsx` same-segment case (CONFIRMED, was previously MEDIUM). dynamic-segment-name-conflict is NOT caught by `next build` (the build SUCCEEDS on `shop/[id]` + `shop/[slug]`, root-level, catch-all-vs-optional, and with `generateStaticParams`); it throws "You cannot use different slug names for the same dynamic path ('slug' !== 'id')" only at dev / production RUNTIME when the position is hit, so fallow's static catch surfaces a route that passes CI and crashes at request time (a STRONGER value case than a build error, not weaker). Out of scope: Pages Router, custom `pageExtensions` beyond the allowlist.
- **Entry export validation** (`--include-entry-exports`): when enabled, entry file exports (package.json main/module/exports, framework convention files) are subject to unused-export detection instead of being automatically marked as used. Catches typos in framework exports (e.g., `meatdata` instead of `metadata` in Next.js pages). Still respects plugin `used_exports` rules for partial checking. CLI-only flag, not in config schema. **Capability E** (route-internal unused exports): on a `next`-enabled App Router page, a stray non-allowlisted export (`export const helper`) and a typo'd route-export name (`meatdata` for `metadata`) surface as `unused_exports` while the real `metadata` / `default` exports are credited by the nextjs plugin's precise used-exports allowlist. This is a precision property of the existing unused-export detector (no dedicated detection code), pinned by `crates/core/tests/integration_test/capability_e_route_exports.rs` against the `capability-e-route-exports` fixture. It proves the knip-can't-but-fallow-does gap: knip credits every Next.js page export wholesale and cannot catch the typo.
- **Single-file filtering** (`--file`): scopes check output to specified file(s). Full graph still builds (correct results require cross-file analysis), but only issues in matching files are reported. Dependency-level issues (unused deps, dev deps, type-only deps) are suppressed in file mode since they are project-wide properties. Warns when a path is not found. Useful for lint-staged pre-commit hooks.
- **Hotspot ownership signals** (`--ownership` on `health --hotspots`): per-file ownership metrics derived from `git log --use-mailmap --format='%at|%ae'`. Computed only when `--hotspots` runs and a file passes the `min_commits` threshold. Signals: bus factor (Avelino truck factor — minimum contributors covering ≥50% of recency-weighted commits), contributor count (after bot-pattern filtering), top contributor with stale-days, recent contributors (top-3), declared CODEOWNERS owner, `ownership_state` (`active`, `unowned`, `declared_inactive`, `drifting`), drift (file age ≥30d AND original author share <10%), tristate `unowned` (true=no rule matches; false=rule matches; null=no CODEOWNERS file). Declared owners suppress vague git-history drift only when fallow can match them to an active contributor offline. Emitted via `OwnershipMetrics` on `HotspotEntry` (`skip_serializing_if = "Option::is_none"` for backwards compat). JSON injects ownership-aware actions: `low-bus-factor`, `unowned-hotspot` (with synthesized CODEOWNERS pattern), `ownership-drift` only for `ownership_state == "drifting"`. Email privacy mode (`ownership.emailMode` config or `--ownership-emails` CLI): `handle` (default, local-part only with GitHub noreply unwrap and deterministic same-handle disambiguation), `anonymized` (`xxh3:` non-cryptographic pseudonym), legacy `hash` (same as `anonymized`), `raw`. Bot patterns are globset matches against the raw author email; defaults cover `*\[bot\]*` (escaped brackets), `dependabot*`, `renovate*`, `github-actions*`, `svc-*`, `*-service-account*`. `*noreply*` is intentionally NOT a default because most human GitHub contributors commit from `<id>+<handle>@users.noreply.github.com` (GitHub's privacy default); the actual bot accounts already match via `\[bot\]`. Author emails are interned in `ChurnResult.author_pool` and per-file maps reference them by `u32` index to keep the bitcode cache compact. Cache version bumped to v2.
- **Feature flag detection**: AST-based detection of feature flag patterns. Three categories: (1) environment variables (`process.env.FEATURE_*` with 12 built-in prefixes), (2) SDK calls (40+ built-in patterns for LaunchDarkly, Statsig, Unleash, GrowthBook, Split, PostHog, Vercel Flags, ConfigCat, Flagsmith, Optimizely, Eppo), (3) config objects (opt-in heuristic for objects named "feature", "flag", "toggle"). Reports flag locations, detection confidence (high/low), guard spans. Cross-references with dead code: flags guarding unused exports are highlighted. Configured via `flags` section in config. Default severity: `off`. CLI: `fallow flags`. Inline suppression: `// fallow-ignore-next-line feature-flag` (line-level) and `// fallow-ignore-file feature-flag` (file-level). The suppression check runs in `crates/cli/src/flags.rs` during both built-in and custom flag collection loops, using `is_suppressed()` / `is_file_suppressed()` with `IssueKind::FeatureFlag`. The file-level check is hoisted outside the per-flag loop for efficiency.
- **Client-server-leak security candidates** (opt-in, `fallow security` only): a graph-structural rule that flags a `"use client"` file (React Server Components / Next.js client boundary) whose transitive static-import cone reaches a module that reads a NON-PUBLIC env secret through `process.env` or `import.meta.env`, plus the direct case where the `"use client"` file reads such a secret itself. Findings are CANDIDATES for downstream agent verification, NOT verified vulnerabilities; there is deliberately no `confidence` / `signal_strength` field, only the structural import-hop `trace`. Implemented in `crates/core/src/analyze/security/mod.rs`: file-level directive capture (`ModuleInfo.directives`, captured by `visit_program` in the extract visitor; `CACHE_VERSION` bumped 104 to 105) identifies client boundaries; secret sources are detected from static `member_accesses` (`process.env.X` surfaces as `MemberAccess { object: "process.env", member: "X" }`; `import.meta.env.X` surfaces as `MemberAccess { object: "import.meta.env", member: "X" }`, issue #877), excluding public-by-convention prefixes (`NODE_ENV`, `NEXT_PUBLIC_`, `VITE_`, `NUXT_PUBLIC_`, `REACT_APP_`, `PUBLIC_`, `GATSBY_`, `EXPO_PUBLIC_`, `STORYBOOK_`) which frameworks inline into the client bundle; a BFS modeled on `reachability::collect_reachable` walks the cone, skipping type-only edges (erased at build, cannot carry a secret). The default rule severity is `off` (`RulesConfig.security_client_server_leak`); `fallow security` forces it to `warn`. The finding lives on `AnalysisResults.security_findings` (`#[serde(skip)]`, excluded from `total_issues`), so it NEVER appears under bare `fallow` or the `audit` gate; the `fallow security` command is the only surface and emits its own `SecurityOutput` envelope (human / JSON / SARIF, SARIF at `level: note` with no CWE). Blind spots are surfaced in-band, never silently dropped: dynamic `import()` patterns the BFS cannot follow bump `security_unresolved_edge_files` (a counted human line: an empty result with a non-zero count is not a clean bill). Inline suppression: `// fallow-ignore-file security-client-server-leak` (file-level), routed through `SuppressionContext::is_file_suppressed` so the marker is recorded as consumed and not later flagged stale; the rule's default-`off` severity also keeps the suppression from being stale under bare `fallow`. Each finding carries an `actions` array with that file-level suppress hint (`auto_fixable: false`; verification is the agent's job). `--changed-since` / `--workspace` retain `security_findings` by anchor path (core `filter_results_by_changed_files`, cli `filter_to_workspaces`). A SECOND sink predicate (capability A) runs over the SAME cone: reaching a SERVER-ONLY module emits a DISTINCT finding (`category: Some("server-only-import")`, same `ClientServerLeak` kind, same `security-client-server-leak` rule and suppress kind, no new IssueKind/RulesConfig field). The server-only sink set is deliberately conservative (false-negative-preferring): a module carrying a `"use server"` directive (`ModuleInfo.directives`), OR importing the `server-only` poison package, `next/server`, or `node:fs` / `node:fs/promises` / `node:child_process` (the `node:` and bare forms BOTH count), OR importing a named server-only API from `next/headers` (`cookies` / `headers` / `draftMode`, or a namespace import of `next/headers`). NO "DB client" or package-name heuristic (the panel dropped it; no clean syntactic sink). The secret-leak and server-only predicates are independent (a client cone that both reads a secret AND reaches a server-only module produces both findings). ssr:false escape hatch: fallow's arrow-wrapped dynamic-import detection DOES resolve `next/dynamic(() => import('./X'))` to a static graph edge, so the ssr:false edge IS in the cone; the extract layer records each `next/dynamic(..., { ssr: false })` `import()` span on the new `ModuleInfo.client_only_dynamic_import_spans` field (`CACHE_VERSION` 150 to 151) and the BFS excludes an edge reached ONLY through such a span via `ModuleGraph::outgoing_edge_summaries_with_exclusions` (an edge also reached by a real static import stays in the cone). Human / SARIF render the server-only variant with distinct wording ("Client imports server-only code", SARIF rule id `security/server-only-import`, still `level: note`, no CWE). ADR-021 amendment (2026-05-30) admits this as a cluster-4 graph-structural candidate; external CVE/OSV/SCA/SBOM stay out of scope.
- **Data-driven security candidate catalogue** (opt-in, `fallow security` only): a category-blind, data-driven extension of the security-candidate surface. Where `client-server-leak` is a bespoke graph-reachability rule, this is a syntactic SINK catalogue: the extract layer captures non-literal call / member-call / member-assign / tagged-template / jsx-attr / new-expression sink sites into `ModuleInfo.security_sinks`, plus narrowly allowlisted literal-aware sites from issues #875, #901, and #892, and issue #928 ReDoS regex application sites.
  Captured sites use `SinkSite { sink_shape, callee_path, arg_index, arg_is_non_literal, arg_kind, arg_literal, regex_pattern, object_properties, object_property_keys, object_property_keys_complete, arg_idents, arg_source_paths, span }`. `arg_kind` discriminates the argument shape (`concat` / `template-with-subst` / `object` / `call` / `literal` / `no-arg` / `other`) so a matcher can require genuinely unsafe shapes and stay category-blind. `arg_source_paths` stores flattened direct member paths from sink arguments, including source-object paths such as `process.env` for `process.env.SECRET`, so source-backed rows can match direct source expressions without requiring a temporary local binding. Local bindings also include recognizable framework handler parameter sources (issue #879), one-hop same-module helper calls that return a source-backed parameter path (issue #878), and same-module bindings whose template literal, string-concat, or object-literal initializer embeds a source-backed member path (issue #1095), chained at extract time through up to three same-module local bindings (`const a = req.query.id; const b = \`x-${a}\`; sink(b)`, issue #1146; cap `MAX_TAINT_BINDING_HOPS = 3`, a non-configurable constant); function declarations, const arrows, and const function expressions are summarized, while aliases, cross-module calls, helper chains beyond #878, and shadowed helper names stay unbacked. The #1146 chain reuses the original `source_path` + `source_span_start` so the analyze layer needs no changes (the chained local matches `source_tainted_locals` and anchors at the original read); only the bare-identifier alias plus the #1095 recursion shapes chain (call / member-root / logical / conditional initializers do not, the FN-preferring drop arms), the per-binding hop is tracked per `(local, source_path)` pair so a deep path cannot ride a shallow sibling under the cap and a direct source capture is never demoted by a chained re-record, an over-cap chain is simply not recorded (degrades to module-level reachability, never a false arg-level) with a `RUST_LOG=debug` line, and cross-SFC-block chains do not form (the hop map is per-extractor working state, not merged across `<script>` blocks). Literal-aware rows opt into `literal_values`, `literal_contains`, `literal_integers`, `object_properties`, `object_missing_or_false`, `object_missing`, or `context_keywords`; existing rows still require `arg_is_non_literal` unless they explicitly opt in. ReDoS rows use `regex_pattern` for evidence and still require source-backed input. Resource-amplification rows require source-backed size arguments and skip directly clamped `Math.min(source, literalCap)` / `Math.max(..., Math.min(source, literalCap))` shapes at extraction time. `object_properties` can match nested static object paths such as `webPreferences.nodeIntegration`; `object_missing` uses complete static top-level key metadata and refuses object literals with spread or non-static keys. `CACHE_VERSION` was bumped again for #876, #898, #895, #901, #892, #879, #878, #928, #929, #1095, and #1146 because warm caches lack the direct source-path metadata, key-presence metadata, exact literal TLS env assignment sink, cleartext transport literal sinks, integer literal metadata, nested option property metadata, static string literals assigned to secret-shaped identifiers or provider credential prefixes, framework parameter source bindings, helper-return taint bindings, ReDoS regex sink sites, clamp-pruned resource-amplification sink sites, expression-binding taint signals, and multi-hop chained taint bindings.
  The analyze-layer `tainted_sink` detector (`crates/core/src/analyze/security/tainted_sink.rs`) matches captured sites against `crates/core/data/security_matchers.toml`, embedded via `include_str!` and parsed once behind a `OnceLock`. Findings are CANDIDATES for downstream agent verification, NOT verified vulnerabilities; like `client-server-leak` there is deliberately no `confidence` / `signal_strength` field, only the sink-site `trace` and the catalogue's evidence string. All catalogue findings carry `SecurityFindingKind::TaintedSink` with `category` (the catalogue id, e.g. `dangerous-html`) and `cwe` (the catalogue's declared CWE number, never fabricated); `client-server-leak` carries neither.
  Seeded categories include `dangerous-html` (CWE-79), `command-injection` (CWE-78), `code-injection` (CWE-94, including `new Function(...)` literal and non-literal constructor capture), `dynamic-regex` (CWE-1333, `RegExp(nonLiteral)` and `new RegExp(nonLiteral)`, issue #896), `redos-regex` (CWE-1333, risky literal regex fragments applied to source-backed input through regex or string methods, issue #928), `resource-amplification` (CWE-400, source-backed `Array`, `Buffer.alloc`, `repeat`, and padding size arguments, issue #929), `sql-injection` (CWE-89), `ssrf` (CWE-918, including literal cloud metadata hosts), `path-traversal` (CWE-22), `open-redirect` (CWE-601), `postmessage-wildcard-origin` (CWE-346), `tls-validation-disabled` (CWE-295, Node HTTPS/TLS `rejectUnauthorized: false` options and the exact `NODE_TLS_REJECT_UNAUTHORIZED = "0"` assignment), `cleartext-transport` (CWE-319, literal `http://` / `ftp://` request URLs and `new WebSocket("ws://...")`), `electron-unsafe-webpreferences` (CWE-1188, static unsafe `BrowserWindow` `webPreferences`), `world-writable-permission` (CWE-732, `fs.chmod(..., 0o777)`), `insecure-temp-file` (CWE-377, literal `/tmp/...` and `/var/tmp/...` fs writes), `mysql-multiple-statements` (CWE-89, mysql/mysql2 `multipleStatements: true`), `permissive-cors` (CWE-942), `insecure-cookie` (CWE-614 / CWE-1004), `weak-crypto` (CWE-327, including literal `md5` / `sha1` / `des` / `rc4` / AES ECB rows), `insecure-randomness` (CWE-338, including token-context `Math.random()` zero-arg capture), `jwt-alg-none` and `jwt-verify-missing-algorithms` (CWE-347), `unsafe-deserialization` (CWE-502, `js-yaml` + `node-serialize`), `secret-pii-log` (CWE-532, source-backed `console.*` / `logger.*` / `log.*` calls for `process.env` and request input, issue #876), `hardcoded-secret` (CWE-798, include-required provider-prefix literals and high-entropy literals assigned to secret-shaped identifiers, never raw entropy alone), and `secret-to-network` (CWE-201, issue #890, INCLUDE-REQUIRED: a non-public `process.env` / `import.meta.env` secret reaching the body/options arg of a network sink (`fetch` / `axios.*` / `got` / `http(s).request`) via same-identifier source-backed flow, NOT module-level co-occurrence). Three mechanics ship with `secret-to-network`: (a) a new `requires_source_kinds` matcher field narrows `requires_source` to specific catalogue source ids (here `process-env` / `import-meta-env`), so request-input-to-fetch stays the `ssrf` category's job; (b) public-by-convention env vars (`NEXT_PUBLIC_`, `VITE_`, `REACT_APP_`, ...) are excluded at extraction time via the shared `fallow_types::extract::is_public_env_var` predicate so they never count as a secret source (this also quiets the latent false positive on `secret-pii-log` #876), and `import.meta.env.X` reads are modeled as a source via a new `flatten_member_path` MetaProperty arm; (c) each candidate carries a destination-host signal (`candidate.network.destination`, the arg-0 URL string literal, or absent when the destination is dynamic, the higher-signal exfil case) captured on `SinkSite.url_arg_literal`, so an agent triages exfil from intended auth (a literal provider host) without re-reading source. Hardcoded-secret-as-source and a provider-match heuristic are reserved follow-ups. The catalogue still prefers false-negatives over false-positives throughout: fully literal calls only fire when a row opts into a deterministic literal signal or source-backed regex application, while examples such as `child_process.exec("ls")`, `new RegExp("^[a-z]+$")`, `/^[a-z]+$/.test(req.query.value)`, `/^(a+)+$/.test("aaaa")`, `"x".repeat(Math.min(req.query.count, 1024))`, `crypto.createHash("sha256")`, `console.log(message)` with no source-backed argument, `jwt.verify(token, key, { algorithms: ["RS256"] })`, `https.request({ rejectUnauthorized: true })`, `fetch("https://...")`, `new WebSocket("wss://...")`, safe Electron options, safer chmod modes, same-named local mysql or fs helpers, and random-looking identifiers with no secret context stay quiet.
  `callee_patterns` are segment-aware (not substring): `fetch` matches `fetch` not `myfetch`, and a leading `*.` matches any object (`*.innerHTML` matches `el.innerHTML`). Some matchers are provenance-gated via `import_provenance` (`command-injection` to `node:child_process`, `code-injection`'s `vm` row to `node:vm`, `path-traversal` to `node:path`, `weak-crypto` to `node:crypto`, `permissive-cors` to `cors`, `electron-unsafe-webpreferences` to `electron`, `world-writable-permission` and `insecure-temp-file` to `node:fs`, `mysql-multiple-statements` to `mysql` / `mysql2`, `jwt-alg-none` and `jwt-verify-missing-algorithms` to `jsonwebtoken`, `tls-validation-disabled` to `node:https` / `node:tls`, and `unsafe-deserialization` to `js-yaml` / `node-serialize`): the module must import the source. Binding-sensitive rows additionally require the leading identifier of the callee path to match the imported binding, including `command-injection`, `permissive-cors`, `electron-unsafe-webpreferences`, `world-writable-permission`, `insecure-temp-file`, `mysql-multiple-statements`, `jwt-alg-none`, `jwt-verify-missing-algorithms`, `tls-validation-disabled`, and literal `weak-crypto`. The default rule severity is `off` (`RulesConfig.security_sink`, kebab `security-sink`, discriminant 28); `fallow security` is the ONLY surface that flips it to `warn`, so it NEVER appears under bare `fallow` or the `audit` gate. `security.categories` include / exclude lists scope which catalogue ids are active. `hardcoded-secret` is additionally include-required: absent `security.categories` or an empty include list admits ordinary catalogue rows, but hardcoded-secret runs only when explicitly listed in `security.categories.include`.
  Candidates anchored in low-value noise files are dropped from generation, matching production-mode dead-code exclusion: `is_low_value_anchor` skips test / spec / story / fixture files and tooling config files. Each `tainted-sink` finding carries a single `TraceHopRole::Sink` trace hop. Blind spots are surfaced in-band, never silently dropped: sink-shaped nodes whose callee cannot be flattened to a static path bump `ModuleInfo.security_sinks_skipped`, summed into `SecurityOutput.unresolved_callee_sites` (a zero finding count with a non-zero value here is NOT a clean bill). Issue #1134 also records bounded unresolved-callee diagnostics for computed members, dynamic dispatch, and unsupported assignment objects; `fallow security --format json` emits samples, top files, and reason counts under `unresolved_callee_diagnostics`, scoped by `--file`, `--workspace`, `--changed-since`, and `--gate new`. Inline suppression: ONE token `// fallow-ignore-file security-sink` covers all categories. Each finding carries an `actions` array with that file-level suppress hint (`auto_fixable: false`; verification is the agent's job). Issue #884 adds a dead-code cross-link pass after dead-code analysis and before final security ranking: `tainted-sink` findings in `unused-file` paths or on matching `unused-export` spans get a `dead_code` context, receive a leading manual `delete-file` or `remove-export` action, and sort behind active-code candidates when other rank signals tie. Client/server leak findings are not annotated. Issue #1096 adds a required `severity` tier (`high`, `medium`, `low`) derived after runtime enrichment from runtime-hot, boundary, entry-reachable source-backed, source-backed, and untrusted-source reachability signals; it is review priority only, not a vulnerability verdict or gate decision. ADR-021 amendment (2026-05-30) admits this as a cluster-4 local code-level candidate layer; external CVE/OSV/SCA/SBOM stay out of scope.
- **Receiver-gated HTTP-input sources + arg-vs-module taint confidence** (opt-in, `fallow security` only): two paired accuracy fixes to the untrusted-source signal, neither changing the #885 doctrine (rank, never gate). **#1092 (receiver gate):** the global HTTP-input `[[source]]` patterns `*.query` / `*.params` / `*.body` in `crates/core/data/security_matchers.toml` carry a `receiver_allowlist = ["req", "request", "ctx", "context", "event"]`. A leading-`*.` pattern fires only when the matched receiver (the segment immediately before the suffix, so `ctx.req.query` matches on `req`) is in the allowlist, case-insensitive, via `SourceMatcher::matches` + `CalleePattern::matched_receiver` (one change point covers every call site: `source_tainted_locals`, `sink_source`, `module_contains_untrusted_source`). This stops ORM / data-access receivers (`db.query` Drizzle, `prisma.query`, knex, `client.query`) from classifying their module as an untrusted source, which #885 cross-module reachability would otherwise propagate onto every importing sink. `*.searchParams` is a SEPARATE ungated source row (its receiver is an arbitrary URL local like `u` / `url`, so a request allowlist would make it a guaranteed false negative). FN-preferring: a missed non-standard request receiver is acceptable; a false DB-client source is not. **#1093 (confidence + trace anchoring):** `SecurityReachability.taint_confidence: Option<TaintConfidence>` (`arg-level` | `module-level`, kebab strings, present exactly when `reachable_from_untrusted_source`) is a structured tier so consumers separate a strong intra-statement arg-level candidate (the sink argument traces to a same-module source read, `source_backed`) from a weak module-level one (only the import graph connects a source module) without parsing the `evidence` prose. The trace source node is anchored at the real source read for arg-level findings via a new `TaintedBinding.source_span_start` (byte offset of the read, captured at the two member-access binding sites; `0` for synthetic framework-param / helper-return bindings, which fall back to the sink line; `CACHE_VERSION` 138 -> 141, sharing the bump window with #1095's 139 and #1094's 140), instead of the module import line 1. The new `TraceHopRole::ModuleSource` (serialized `module-source`) honestly labels a module-level source node (cross-module nodes and same-module non-arg-level nodes), distinct from `untrusted-source` which now means an arg-level read; the diff-aware `--gate new` filter (`retain_gate_new`) includes `ModuleSource` so gate parity holds. Invariant: a `source_backed` finding is always `reachable_from_untrusted_source` with a present `arg-level` tier. See issues #1092 and #1093.
- **Configurable HTTP-input request receivers** (issue #1125): `security.requestReceivers` extends the built-in receiver allowlist for the HTTP-input `*.query`, `*.params`, and `*.body` source rows. The config is additive only, trims and lowercases entries during resolution, rejects empty strings at load time, and applies only to the `http-request-input` source id. Built-in receivers keep working without config, ORM / data-access receivers such as `db.query` still stay non-sources unless explicitly configured, and `*.searchParams` remains ungated because its receiver is usually an arbitrary `URL` local.
- **Sanitizer-aware security sink suppression** (issues #863 and #1136): `ModuleInfo.sanitized_sink_args` records high-confidence sanitizer metadata with a narrow `SanitizerScope` so one domain cannot suppress another. HTML suppression covers DOMPurify-compatible `sanitize(...)` values plus local helpers whose return expression proves `&`, `<`, and `>` escaping. Renderer helpers suppress only when every dynamic HTML text interpolation is sanitizer-backed; dynamic fragments inside tags or mixed with unsanitized expressions still report. URL suppression is limited to same-function or same-block fail-closed guards against literal-backed local `const` arrays or `Set`s and applies only to `open-redirect`, `nextjs-open-redirect`, and `ssrf`. Path suppression is limited to local values from `path.resolve`, `path.normalize`, or `path.join` that are tied to `path.relative(base, resolved)` and guarded by `rel.startsWith("..") || path.isAbsolute(rel)`; plain `resolved.startsWith(base)` remains a candidate because prefix collisions are unsafe. SQL identifier suppression is separate from SQL value escaping: local helpers must wrap the identifier in quotes, double embedded quotes, and be used in identifier positions such as `FROM ${quoteIdent(table)}`. Mutable allowlists, post-use guards, helper predicates, shadowed bindings, domain-cross sanitizer use, SQL value positions, and mixed sanitized plus unsanitized SQL templates intentionally keep firing.
- **Declarative validation controls** (issue #1094): `ModuleInfo.security_control_sites` also records route-boundary validation annotations for framework-gated declarative shapes: Elysia route option `body` / `query` / `params`, Fastify route `schema.body` / `schema.querystring` / `schema.params`, tRPC `.input(...)` procedure chains, Hono `validator(...)` / `zValidator(...)`, NestJS `ValidationPipe` / `UsePipes`, and express-validator `body` / `param` / `query` / `check` middleware. These controls feed `fallow security --surface` defensive-boundary prompts only. They do not suppress sink candidates, prove schema completeness, or change candidate severity. `CACHE_VERSION` is bumped to 140 so stale entries do not miss the new control annotations.
- **File encoding contract for fix + extract** (issue #475): fallow's contract is "UTF-8 with or without BOM; line offsets are computed against the post-BOM view; the BOM, if present on input, is preserved on output by `fallow fix`." `crates/extract/src/lib.rs` strips the leading UTF-8 BOM at every file-read entry point (`parse_single_file_cached`, `parse_single_file`, `parse_from_content`) before computing `content_hash` (xxh3 over post-strip bytes) and before passing source to `parse_source_to_module`. `parse_source_to_module` carries a defense-in-depth `strip_bom` call for out-of-tree callers (fuzzers, integration fixtures, future embedders). `crates/cli/src/fix/io.rs::read_source` returns `Result<Option<(String, EncodingMetadata)>, EncodingError>` where `EncodingMetadata { line_ending, had_bom }` captures the round-trip shape; `stage_fixed_content` re-prepends the BOM bytes (`EF BB BF`) on write when `had_bom`. Mixed CRLF/LF files (both `\r\n` and bare-LF lines present) are rejected with `EncodingError::MixedLineEndings`; the orchestrator translates this into `SkipReason::MixedLineEndings`, surfaces an additive `skipped_mixed_line_endings: M` envelope counter, emits a stderr line naming `dos2unix` and `git config core.autocrlf input` as remediation, and exits with code 2. The skip is NOT self-healing: re-running fallow alone does not normalize the file. Cross-fixer composition preserves the `had_bom` flag because the staged-content fast path re-runs `classify_source` on the staged bytes (which already include the re-prepended BOM from the first fixer). The extraction cache (`CACHE_VERSION` bumped from 88 to 89) invalidates automatically on upgrade because pre-fix caches keyed hashes over BOM-included bytes. The `fix_preview` and `fix_apply` MCP tool descriptions flag the non-self-healing nature for AI agents.

- **Unused package manager catalog entries**: `pnpm-workspace.yaml` catalogs and Bun root `package.json` catalogs are parsed via `crates/config/src/workspace/pnpm_catalog.rs`, using a section-aware line scanner (`crates/config/src/workspace/pnpm_catalog.rs`) that records the 1-based source line for every entry. Workspace `package.json` files are walked for dependency values matching the `catalog:` protocol: a bare `"react": "catalog:"` and an explicit `"react": "catalog:default"` both resolve to the default catalog per pnpm's spec; named references use the catalog name verbatim. The detector (`crates/core/src/analyze/unused_catalog.rs::find_unused_catalog_entries`) emits one `UnusedCatalogEntry` per catalog entry no workspace `package.json` references via `catalog:` for the matching catalog group. Each finding also carries `hardcoded_consumers: Vec<PathBuf>`, the workspace `package.json` paths that declare the same package with a non-`catalog:` version range, so consumers can decide whether to delete the catalog entry or switch the consumer to `catalog:` first (the differentiator vs knip's catalog detection). Output sort is `(path, catalog_name, entry_name)` with the default catalog sorting first via `catalog_sort_key`. Default severity `warn` (not `error`) because catalog entries ship zero bytes; matches the rest of the `unused_dev_dependencies` / `unused_optional_dependencies` family. JSON `actions[]` emits the YAML-style `# fallow-ignore-next-line unused-catalog-entry` suppression action only for `pnpm-workspace.yaml` findings; Bun `package.json` findings are manual because JSON has no comment surface. Filter-to-workspaces clears catalog findings (whole-project concern, not per-workspace). **Auto-fix** (`crates/cli/src/fix/catalog.rs`, issues #335, #360, and #1301): `fallow fix` removes pnpm YAML entries by line-aware deletion (no YAML round-trip writer in the workspace; a full reprint via `serde_yaml_ng` would obliterate comments) and skips Bun `package.json` catalog entries with `skip_reason: "unsupported_catalog_source"`. Object-form entries (`react:\n    specifier: ^18.2.0`) are detected by consuming subsequent lines whose indent is strictly greater than the entry's own; blank lines stop the consumption to preserve inter-entry whitespace. `fix.catalog.deletePrecedingComments` controls contiguous same-indent YAML comment blocks immediately above removed entries: `auto` (default) deletes blocks that directly follow the parent catalog header or a blank separator, `always` deletes any adjacent block, and `never` preserves leading comments. Two escape hatches keep curated comments safe regardless of policy: any line in the block bearing the `# fallow-keep` marker bails out of deletion (mirrors the `fallow-ignore` inline-suppression convention), and the `auto` policy additionally preserves section-banner blocks whose comment body starts with three or more `=`, `-`, `*`, `_`, `~`, `+`, or `#` characters (`# === React 18 production pins ===`). The JSON `remove_catalog_entry` action carries both `line` (first deleted line, the comment when the policy absorbs one) and `entry_line` (the entry's original 1-based line) so CI annotators and dedup caches keep a stable anchor across policy changes; the human stderr summary appends `(+M catalog comment lines)` after the fixed-issue count when at least one preceding comment line was absorbed. Entries with non-empty `hardcoded_consumers` are skipped (a record with `applied: false`, `skipped: true`, `skip_reason: "hardcoded_consumers"` is emitted so the user knows to migrate the consumer first). Multi-document YAML files (`---` separator) are rejected up front with `skip_reason: "multi_document_yaml"`. When removing the last entry of a catalog group (default `catalog:` or a named `catalogs.<name>:`) leaves the header with no children, the fix rewrites the header to `catalog: {}` / `<name>: {}` instead of leaving bare `key:`, because bare-key parses as null in YAML and pnpm rejects null-valued catalogs with `Cannot convert undefined or null to object` at install time (verified against pnpm 10.33.4). The post-edit content is reparsed with `serde_yaml_ng` as a syntactic sanity gate before persisting. The per-instance `auto_fixable` bool in the JSON `actions[]` flips to `false` for entries with hardcoded consumers or non-pnpm catalog sources; agents that filter on the bool no longer apply destructive ops to entries needing consumer-side migration first or JSON manual edits. After a successful fix that touched `pnpm-workspace.yaml`, the human stderr emits a one-line `Run \`pnpm install\` to refresh pnpm-lock.yaml` reminder. The LSP code action (`crates/lsp/src/code_actions/quick_fix.rs::build_remove_catalog_entry_actions`) mirrors the same source and hardcoded-consumer guard and uses anchored key-prefix matching to reject substring collisions between sibling catalog entries with shared prefixes (`react` vs `react-native`), but intentionally does not delete leading comments so editor quick fixes stay conservative. See issues #329 (detection), #335 (auto-fix), #360 (leading comments), and #1301 (Bun package.json catalogs). See also: the `unresolved-catalog-reference` inverse rule (consumer references a catalog that does not declare the package).
- **Empty package manager catalog groups**: named `catalogs.<name>:` groups in `pnpm-workspace.yaml` or Bun root `package.json` that contain no package entries are detected by `crates/core/src/analyze/unused_catalog.rs::find_empty_catalog_groups`. The top-level `catalog:` map is intentionally ignored even when empty, because it is the default-catalog hook and can be a harmless placeholder. Findings are project-root-relative to the catalog source file, sorted by `(path, catalog_name)`, default to `warn`, support YAML line suppressions via `# fallow-ignore-next-line empty-catalog-group` when sourced from `pnpm-workspace.yaml`, and pnpm YAML findings are auto-fixable by removing the empty named group header without reprinting the whole YAML document. Changed-file and audit attribution paths must account for the relative manifest path.
- **Unresolved package manager catalog references**: the inverse of `unused-catalog-entries`. The same catalog parse + consumer walk (`find_unresolved_catalog_references` in `crates/core/src/analyze/unused_catalog.rs`) iterates over every `catalog:` / `catalog:<name>` reference collected in `CatalogConsumers.referenced_with_locations` and emits an `UnresolvedCatalogReference` for any `(catalog_name, package_name)` pair the catalog source does not declare. The consumer-side line number is captured by a section-aware `scan_dep_lines` pass over the raw `package.json` source that tracks brace depth so nested-object keys (`peerDependenciesMeta.<pkg>.optional`) cannot be misread as direct dep entries. Each finding carries `available_in_catalogs: Vec<String>`: other catalogs in the same workspace that DO declare the package, sorted lexicographically; agents flip the reference to one of those when non-empty, otherwise add the missing entry to the named catalog. Default severity `error` (matches `unresolved-imports`): the package manager install fails on unresolved catalog references, so the static finding catches the same failure pre-install. Default-catalog rendering uses a special-case "default catalog" phrasing in human / SARIF / LSP / CodeClimate output instead of `catalog 'default'` because users who write bare `catalog:` think of it as "the catalog", not as a named one. Suppression is config-only via the new `IgnoreCatalogReferenceRule { package, catalog?, consumer? }` array on `FallowConfig` (consumer is a glob, all three fields AND together when set); `package.json` does not support comments so inline suppression is structurally impossible. JSON `actions[]` is discriminated by the `available_in_catalogs` shape: `update-catalog-reference` (primary when alternatives exist), `add-catalog-entry` (primary when none do), `remove-catalog-reference` (fallback from the generic spec), plus an `add-to-config` for `ignoreCatalogReferences` with a paste-ready value scoped to the consumer. Filter-to-workspaces retains findings under the active set because each finding is anchored at a consumer `package.json` (unlike unused-catalog-entries which lives in the root YAML). The LSP diagnostic mirrors the catalog-entries pattern with `root.join(&finding.path)` so the URI built from the project-relative path is absolute. See issues #334 and #1301.
- **Unused pnpm dependency overrides**: pnpm's `overrides:` section (top-level in `pnpm-workspace.yaml` for pnpm 9+, or legacy `pnpm.overrides` in the root `package.json`) lets a workspace force a specific transitive dependency version (typically for a CVE patch). The detector (`crates/core/src/analyze/unused_overrides.rs::find_unused_dependency_overrides`) parses both source forms via `crates/config/src/workspace/pnpm_overrides.rs`, walks every workspace `package.json` to compute the union of declared package names across `dependencies`, `devDependencies`, `peerDependencies`, and `optionalDependencies`, and ALSO parses `pnpm-lock.yaml` to collect every package name appearing in `packages:` / `snapshots:` keys plus any nested `dependencies` / `optionalDependencies` / `devDependencies` / `peerDependencies` map. An `UnusedDependencyOverride` is emitted only for overrides whose target package is absent from BOTH the declared set AND the lockfile-resolved set. Parent-chain shapes (`react>react-dom: ^17`) are USED when EITHER the parent OR the target is declared OR resolved in the lockfile, covering the CVE-fix pattern where the override forces a transitive version that only appears in `pnpm-lock.yaml`. When the lockfile is missing or malformed (`serde_yaml_ng::from_str` fails), the detector degrades to the package.json-only fallback and the `hint` field reminds consumers to verify the override before removal. Lockfile keys are normalized via `package_name_from_lock_key`, which strips the leading `/` used by pnpm 6 keys, the version selector at `@`, and pnpm peer suffixes in parentheses, correctly handling scoped packages (`@scope/pkg@1.0.0` → `@scope/pkg`). Default severity `warn`. Each finding decomposes the raw key into `target_package`, `parent_package` (Option), `version_constraint` (Option, e.g. `Some("<18")` for `@types/react@<18`), and `version_range` (the forced version). Suppression is config-only via `ignoreDependencyOverrides: [{ package, source? }]` on `FallowConfig`: `package` matches the override's `target_package` exactly, optional `source` scopes to `"pnpm-workspace.yaml"` or `"package.json"`. JSON `actions[]` emits `remove-dependency-override` as primary plus an `add-to-config` for `ignoreDependencyOverrides`. Filter-to-workspaces clears findings (whole-project concern). See also: `misconfigured-dependency-overrides` (the entry-shape variant). See issues #336 (detector) and #371 (lockfile-aware check).
- **Misconfigured pnpm dependency overrides**: same parser as `unused-dependency-overrides`, different gate. `find_misconfigured_dependency_overrides` emits `MisconfiguredDependencyOverride` for entries whose key cannot be parsed (`DependencyOverrideMisconfigReason::UnparsableKey`: empty key, dangling separators like `react>`, malformed selectors like `@types/react@<<18`) or whose value is missing/empty (`EmptyValue`). pnpm itself refuses to honor these at install time. The pnpm idiom values `-` (delete), `$ref` (self-reference), and `npm:alias@^1` (npm alias) are explicitly allowlisted and never flagged. Default severity `error`: a malformed override breaks `pnpm install` for every contributor. Findings carry the raw key, raw value, the reason discriminant (serialized as kebab-case `unparsable-key` / `empty-value`), source, path, and line. Suppression: same `ignoreDependencyOverrides` config-only rule as `unused-dependency-overrides`. JSON `actions[]` emits `fix-dependency-override` as primary plus the same `add-to-config` suppress. Filter-to-workspaces clears findings. See also: `unused-dependency-overrides` (the consumer-not-declared variant). See issue #336.
- **commit-and-tag-version updater / bump-file crediting** (issue #1640): the `commit-and-tag-version` plugin (legacy enabler `standard-version`) reads `bumpFiles[]` and `packageFiles[]` from the package.json `commit-and-tag-version` key and from standalone `.versionrc` / `.versionrc.{json,js,cjs}` configs. Each entry's `updater` (the load-bearing custom JS module the tool loads at runtime: `readVersion` / `writeVersion`) and `filename` (the bump target) is credited as a support setup file via `extract_config_array_object_strings`, resolved relative to the config directory (the package.json case resolves against the project root). Crediting is gated downstream on the path existing on disk (with a source-extension retry in `push_plugin_setup_files`), so non-source `filename` targets (gradle, plist, version.txt) and phantom paths are never over-credited; the load-bearing case is the custom `updater`, which previously surfaced as `unused-files` because it has no static importer. Limitation: the `Plugin` trait reads a single package.json key (`commit-and-tag-version`), so a legacy `standard-version` user whose bump config is inlined under the `standard-version` package.json key is not covered (its `.versionrc` configs are, and the dependency itself is still credited). Config-plugin level only, no extraction change, no `CACHE_VERSION` bump.
