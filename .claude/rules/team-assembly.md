---
paths:
  - "**"
---

# Team assembly matrix

Use `.agents/skills/team-assembly/SKILL.md` as the canonical reviewer-selection
workflow. Use `docs/development/review-routing.md` for path and risk routing.

Do not duplicate the matrix or verdict rules here. Update the canonical skill
and shared review route when coverage changes.
