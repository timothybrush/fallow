export const metadata = {
  title: 'Default slot',
};

export const viewport = {
  width: 'device-width',
};

export default function DefaultSlot() {
  return <div>Default Slot</div>;
}

export const unusedDefaultHelper = 'still-dead';
